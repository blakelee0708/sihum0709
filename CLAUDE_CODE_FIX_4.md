# Claude Code 수정 지시 4차

FIX_3 이후 결정된 사항이다.
[1]번부터 [8]번까지 순서대로 진행하고, 각 항목마다 검증한 뒤
다음으로 넘어가라.

내 확인을 기다리지 말고 끝까지 진행해라.
중간에 멈추거나 질문하지 마라. 판단이 필요한 지점은
아래에 다 적어뒀다.

===========================================================
[1] 운 지수 범위 조정 (가장 중요)
===========================================================

지금 20~30점이 나오면 사용자가 낙담한다. 시험을 앞둔
사람에게 희망을 주려고 만든 서비스인데 반대로 작용한다.

계산값 0~100을 표시값 40~95로 매핑한다.

```typescript
function toDisplayScore(raw: number): number {
  return Math.round(40 + (raw / 100) * 55)
}
```

  계산 0    표시 40
  계산 20   표시 51
  계산 50   표시 68
  계산 80   표시 84
  계산 100  표시 95

상대적 차이와 순위는 그대로 유지된다. 0~100이 임의의
범위였듯 40~95도 임의이고, 이 지표는 절대값이 아니라
상대적 차이가 의미를 갖는다.

1-1. 적용 대상

  시험 당일 운 지수
  오늘의 운 지수
  7일 흐름의 각 날짜 점수

  7일 흐름을 빼먹지 마라. 안 그러면 차트에 20점짜리
  막대가 나온다.

1-2. 잠재력 발휘 지수는 85~120으로

  현재 70~120인데 하한을 85로 올린다.
  100 아래가 나와도 덜 나쁘게 보인다.

1-3. 판정 문구 구간 재조정

  PRD 6.4의 verdict 조각 5개를 아래 구간에 맞춘다.

    85~95   기운이 크게 밀어주는 날
    75~84   흐름이 좋은 날
    62~74   무난한 날
    50~61   준비량이 그대로 나오는 날
    40~49   컨디션 관리가 결과를 정하는 날

  40~49 문구가 가장 중요하다. "나쁜 날"이 아니라
  "관리하면 되는 날"로 써야 한다.

  나쁜 예
    기운이 낮은 날입니다. 조심하세요.

  좋은 예
    기운이 크게 도와주지는 않는 날이에요.
    대신 흔들지도 않습니다.
    이런 날은 컨디션이 결과를 정합니다.
    실력이 부족해서가 아니라 몸 상태에 따라
    갈리는 날이라는 뜻이에요.

  fragments.json의 verdict 조각을 이 방향으로 다시 써라.

1-4. 캐릭터 표정 구간 재조정

  PRD 7.2의 5단계를 아래로 바꾼다.

    char-05  85-95
    char-04  75-84
    char-03  62-74
    char-02  50-61
    char-01  40-49

  char-01은 주먹 쥐고 응원하는 표정이라 40~49에 맞는다.

1-5. 낮은 점수 해석에 발휘 지수를 활용

  두 지표가 다르게 나오면 할 말이 생긴다.

    당일 운 48 · 발휘 지수 108
    "기운이 크게 밀어주는 날은 아니에요. 대신 흔들지도
     않습니다. 시작 시간과 시험 방식이 잘 맞아서 준비한
     것은 그대로 나오는 조건이에요."

  "운이 안 좋다"가 아니라 "실력대로 나온다"로 만든다.

  유료 리포트 프롬프트에도 이 지침을 넣어라.

1-6. PRD 기록

  6장에 이 결정과 이유를 기록해라. 나중에 왜 이렇게
  했는지 알 수 있어야 한다.

===========================================================
[2] 결과 전환 연출
===========================================================

FIX_3의 [10]번을 아래로 대체한다. 버튼 로딩 800ms 대신
5초 연출로 바꾼다.

2-1. 결과 보기 버튼

  탭하면 버튼이 점으로 변하며 회전한다.
  motion.dev의 dots morph button 스타일이다.
  (AnimatePresence, motion, animate, whileTap 전부
   무료 API다. 소스를 사지 않고 직접 구현해라.)

  버튼 폭을 min-width로 고정해라. 텍스트가 점으로
  바뀌면서 폭이 줄면 덜컥거린다.

2-2. 대기 시간과 문구

  계산은 즉시 끝나지만 의도적으로 대기한다.
  그 시간에 볼 것이 있어야 기다림이 아니라 콘텐츠가 된다.

  첫 방문   5초
  재방문    2초

  ```javascript
  const seen = localStorage.getItem('seen_result')
  const wait = seen ? 2000 : 5000
  ```

  대기 중에 quotes.json의 응원 문구가 1.5초마다 바뀐다.

  ```
  0.0s  사주를 보고 있어요
  1.5s  "떨리는 건 진심이라는 뜻입니다"
  3.0s  "오늘도 늦게까지 앉아 있던 당신"
  4.5s  거의 다 됐어요
  ```

  문구 전환도 애니메이션으로 처리한다.

  ```jsx
  <AnimatePresence mode="wait">
    <motion.p key={index}
      initial={{ opacity: 0, y: 8 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -8 }}
      transition={{ duration: 0.3 }}>
      {quotes[index]}
    </motion.p>
  </AnimatePresence>
  ```

2-3. 바텀시트로 완료 알림

  대기가 끝나면 아래에서 시트가 올라온다.
  motion.dev의 sheet modal 스타일이다.
  (AnimatePresence, useMotionValue, useTransform, drag
   전부 무료 API다.)

  ```
  ┌──────────────────────────────┐
  │           ▂▂▂▂               │  드래그 핸들
  │        [합격이 char-04]       │
  │    내 시험운이 나왔어요         │
  │  ┌────────────────────────┐  │
  │  │      보러 갈까요?        │  │
  │  └────────────────────────┘  │
  └──────────────────────────────┘
  ```

  점수를 여기서 보여주지 마라. 결과 화면에서 처음
  보는 것이 더 극적이다.

  버튼을 누르거나 위로 끌면 결과로 간다.
  드래그가 가능하다는 걸 모르는 사용자가 많으므로
  버튼이 기본이다.

  spring 물리로 올라온다.
    { type: "spring", stiffness: 300, damping: 30 }

2-4. 결과 화면

  진입 후 점수를 카운트업한다.

  ```jsx
  animate(0, score, {
    duration: 1.2, ease: "easeOut",
    onUpdate: v => setDisplay(Math.round(v))
  })
  ```

2-5. 유료 리포트에도 재사용

  유료는 실제로 150초가 걸린다.

    결제 완료
    말풍선 9개 + 명식 + 오행 (150초)
    바텀시트 "리포트가 완성됐어요"
    리포트 화면

  같은 바텀시트 컴포넌트를 재사용해라.

===========================================================
[3] 리포트 후 AI 대화 기능 (신규)
===========================================================

유료 리포트를 읽은 뒤 합격이와 대화할 수 있게 한다.
3,900원에 포함한다. 별도 결제를 만들지 마라.

3-1. 진입

  리포트 하단에 배치한다.

  ┌────────────────────────────┐
  │  합격이와 더 자세하게         │
  │  이야기하러 가기              │
  └────────────────────────────┘

  합격이의 기운이 떨어질 때까지
  대화를 나눌 수 있어요

3-2. 원가 상한 방식

  턴 수가 아니라 누적 원가로 제한한다.

  ```typescript
  const cost = (inputTokens * 2 + outputTokens * 10) / 1_000_000 * 1400
  session.totalCost += cost

  const LIMIT = 250   // 원
  const remaining = Math.max(0, 1 - session.totalCost / LIMIT)
  ```

  사용자에게 원가를 숫자로 보여주지 마라.
  "합격이의 기운" 게이지로 표시한다.

  ```
  합격이의 기운  ▓▓▓▓▓▓▓░░░
  ```

  상단에 고정해 남은 정도가 항상 보이게 한다.

  최소 4턴은 보장한다. 긴 질문을 하면 3턴에 끝날 수
  있는데 사용자가 "왜 벌써?" 한다.

  직접 입력은 200자로 제한한다. 편차를 줄이기 위함이다.

3-3. 추천 질문 버튼

  필기

    1. 내 학습 유형을 더 자세히 알려줘
    2. 시험 당일 실수를 줄이려면
    3. 남은 기간 뭐부터 해야 할까?
    4. 결과 보고서 이외에 내가 더 주의해야 할 건?
    5. 다음 시험은 언제가 좋을까?
    6. 내 행운의 숫자, 색깔 더 추천해줄 게 있어?
    7. 직접 물어볼래요

  면접

    1. 내 답변 유형을 더 자세히
    2. 이 기업과 궁합을 더 자세히
    3. 예상 질문에 어떻게 답할까
    4. 면접에서 조심할 점
    5. 다음 면접은 언제가 좋을까?
    6. 내 행운의 숫자, 색깔 더 추천해줄 게 있어?
    7. 직접 물어볼래요

  "7일 플랜을 더 자세히"는 넣지 마라. 리포트와 겹치고
  D-1 사용자에게는 성립하지 않는다.

3-4. D-day 구간별 질문 교체

  필기 3번과 5번을 구간에 맞게 바꾼다.

    D-8 이상   3. 남은 기간 뭐부터 해야 할까?
    D-2~D-7    3. 지금 뭘 버려야 할까?
    D-1        3. 오늘 밤 뭘 하면 좋을까?
    D-DAY      3. 지금 당장 뭘 하면 좋을까?
               5. 시험 끝나고 뭘 하면 좋을까?

3-5. 답변 후에도 버튼을 계속 표시

  이미 물어본 항목은 제외하고 남은 버튼을 다시 보여준다.

  ```
  [답변]

  더 궁금한 게 있으신가요?
    [시험 당일 실수를 줄이려면]
    [남은 기간 뭐부터 해야 할까?]
    ...
    [직접 물어볼래요]
  ```

  자유 입력만 두면 사용자가 뭘 물을지 모르고 답변
  품질도 흔들린다.

3-6. 리포트와 겹치지 않게 (핵심)

  이 기능의 성패가 여기 달렸다. 리포트와 같은 말을
  반복하면 돈 값을 못 한다.

  프롬프트에 아래를 넣어라.

    아래 리포트는 사용자가 이미 읽었다.
    같은 내용을 반복하지 마라.

    [리포트 전문]

    답변 규칙
      1. 리포트에 쓴 문장을 그대로 반복하지 마라
      2. 리포트에서 결론만 말한 것을 여기서 근거와
         함께 풀어라
      3. 리포트에서 다루지 않은 각도로 답하라
      4. "리포트에서 말씀드린 대로"로 시작하지 마라
         그렇게 시작할 것 같으면 다른 각도를 찾아라

  두 번째 안전장치로, 리포트에 안 쓴 계산값을 대화
  재료에 넣어라.

    일지(배우자궁) 해석
    시주 상세
    월지와 일간의 관계
    십신 중 재성, 인성 (리포트는 관성·식상·비겁 위주)

  "결과 보고서 이외에 내가 더 주의해야 할 건?" 질문이
  여기에 쓰인다.

3-7. 행운의 숫자와 색은 2순위 값으로

  리포트   약한 오행 기준 숫자 1개, 색 계열
  대화     2순위 숫자, 보조 색, 상황별 활용

  예시

    리포트에서는 1을 알려드렸는데, 6도 맞는 숫자예요.
    수 기운을 채우는 숫자가 1과 6인데, 1은 시작하는
    힘이고 6은 마무리하는 힘이에요.
    시험 초반에 페이스를 잡을 땐 1을, 후반에 검토할 땐
    6문항씩 나눠서 보시면 리듬이 맞습니다.

3-8. 금지 사항 프롬프트

    너는 사주 리포트를 만든 합격이다.
    리포트 내용과 사주 계산 결과를 근거로만 답한다.

    금지
      합격 여부나 결과를 예측하지 마라
      확률을 말하지 마라
      시험과 무관한 상담(연애, 재물, 건강)을 하지 마라
      리포트에 없는 사주 정보를 지어내지 마라

    시험 외 질문이 오면
      "저는 시험 준비를 돕는 역할이라 그 부분은
       도와드리기 어려워요. 시험에 대해 궁금한 게
       있으면 물어보세요."

    답변은 3~5문장으로 짧게.

  "합격할까요"가 가장 위험하다. 사용자가 계속 밀어붙여도
  결과를 단정하지 않는다.

3-9. 소진 시

  [합격이] 오늘은 여기까지 봐드릴 수 있을 것 같아요.

          시험 잘 보시고, 결과 나오면
          또 이야기해요.

          ┌────────────────────┐
          │   리포트 다시 보기   │
          └────────────────────┘

  리포트로 돌려보내면 다시 읽게 되고, 대화가 리포트를
  보완했다는 인상이 남는다.

3-10. DB

  ```sql
  create table conversations (
    id uuid primary key default gen_random_uuid(),
    report_id uuid references reports on delete cascade,
    user_id uuid references auth.users on delete cascade,
    messages jsonb default '[]',
    total_cost numeric default 0,
    turn_count int default 0,
    created_at timestamptz default now(),
    updated_at timestamptz default now()
  );
  ```

  대화 내용을 저장해 재진입 시 이어갈 수 있게 한다.

3-11. 원가

  턴당 약 40~50원, 250원 상한이면 5~6턴이다.
  리포트 200원 + 대화 250원 = 450원
  3,900원 대비 11.5퍼센트다.

  PRD 8.14 마진 계산을 갱신해라.

===========================================================
[4] 로그인 정리
===========================================================

이메일 매직 링크는 로그인할 때마다 메일을 받아야 해서
번거롭다. 다만 세션이 60일 유지되므로 실제로는 자주
하지 않는다.

4-1. 카카오 로그인을 기본으로 배치

  ┌────────────────────────┐
  │   카카오로 3초 만에      │   ← 크게, 위에
  └────────────────────────┘
  ┌────────────────────────┐
  │   구글로 계속하기        │
  └────────────────────────┘

  이메일로 로그인          ← 작게, 아래 텍스트 링크

  취준생 대부분이 카카오 계정을 갖고 있다.
  이메일은 예외 상황용으로만 남긴다.

4-2. talk_message 스코프는 분리

  로그인 시점에는 기본 권한만 요청한다.
  나에게 보내기 권한은 리포트를 다 읽고 "카톡으로
  저장하기"를 누를 때 추가로 요청한다.

  동의 화면에 항목이 많으면 이탈한다.

  PRD 11.3을 이 방향으로 수정해라.

4-3. 쿠폰이 적용된 경우 안내

  ┌────────────────────────────┐
  │  무료권이 적용됐어요           │
  │                            │
  │  로그인하시면 바로            │
  │  리포트를 만들어드려요         │
  │                            │
  │  결제 금액 0원              │
  └────────────────────────────┘

  "0원"을 명시해야 한다. 로그인 화면에서 결제가
  연상되면 멈춘다.

===========================================================
[5] 457deep 쿠폰 연동
===========================================================

내가 운영하는 457deep(취준생 구독 사이트)에서 시험사주
무료권을 발급한다.

5-1. 시험사주에 발급 API 신설

  ```
  POST /api/coupon/issue
  Header: X-API-Key

  Body
    { source: '457deep', externalUserId: 'xxx' }

  처리
    1. API 키 검증
    2. 같은 externalUserId로 이미 발급했으면 기존 코드 반환
    3. 없으면 새 코드 생성
       discount_type: 'percent'
       discount_value: 100
       max_uses: 1
       source: '457deep'
       external_user_id: 'xxx'
       valid_until: 발급일 + 30일
    4. 코드 반환
  ```

  coupons 테이블에 source, external_user_id 컬럼을 추가해라.

5-2. 링크로 자동 적용

  457deep에서 아래 형태로 링크를 만든다.

    https://www.sajudday.com?c=A7K2M9

  시험사주는 진입 시 쿼리 파라미터를 읽어 세션에 저장한다.

  ```javascript
  const code = searchParams.get('c')
  if (code) sessionStorage.setItem('coupon', code)
  ```

  결제 화면에서 자동으로 입력된 상태로 표시한다.
  사용자가 코드를 복사하고 붙여넣을 필요가 없다.

5-3. 유입 기록

  ```sql
  alter table profiles add column source text;
  ```

  가입 시 어디서 왔는지 기록한다.
  '457deep', 'organic', 'share' 등.

  나중에 코호트 분석에 쓴다.

===========================================================
[6] 가격
===========================================================

3,900원을 유지한다. 정가를 올리고 상시 할인을 거는
방식은 쓰지 않는다.

  8,900원 정가 + 5,000원 가입 쿠폰 = 3,900원

  이 방식은 세 가지 문제가 있다.
    정가에 판매한 적이 없으면 거짓 할인 표시가 된다
    모두가 3,900원에 사면 정가가 무의미하다
    457deep 무료권의 특별함이 사라진다

신규 가입 혜택은 할인 대신 기능으로 한다.

  무료 결과 저장
  오늘의 운 매일 확인
  시험 D-7, D-1 알림

가격 실험은 출시 후 PRD 12.8의 A/B로 진행한다.
지금 코드에 가격 실험 준비만 해두고 실제로 켜지는 마라.

===========================================================
[7] 검증
===========================================================

각 항목마다 아래를 확인한다.

  npx tsc --noEmit 통과
  npm run build 통과

전체 수정 후 확인할 것

  운 지수가 40 미만으로 나오지 않는지
  7일 흐름 차트에도 적용됐는지
  발휘 지수가 85 미만으로 나오지 않는지
  캐릭터 표정이 새 구간대로 나오는지
  40~49 구간 문구가 절망적이지 않은지
  결과 보기 5초 연출과 문구 전환이 도는지
  재방문 시 2초로 줄어드는지
  바텀시트가 올라오고 드래그도 되는지
  대화 기능이 리포트와 겹치지 않는지
  게이지가 줄어들고 소진 시 종료되는지
  카카오 로그인이 위에 크게 배치됐는지
  쿠폰 링크로 들어오면 결제 화면에 자동 적용되는지

대화 기능은 실제 API로 3회 이상 돌려서 확인해라.
리포트와 겹치는 문장이 나오는지 직접 읽고 판단해서
보고해라. 겹치면 프롬프트를 더 조여라.

===========================================================
[8] 하지 말 것
===========================================================

taste-skill 설치나 전면 디자인 개편은 하지 마라.
기능이 확정된 뒤에 별도로 진행한다.

Motion+ 유료 소스를 사지 마라. 필요한 API는 전부 무료다.

가격을 바꾸지 마라. 3,900원 그대로다.

내가 확정한 질문 목록과 카피를 고치지 마라.

중간에 멈추고 질문하지 마라. 판단이 필요한 지점은
위에 다 적어뒀다. 그래도 막히면 PROGRESS.md에 기록하고
다음 항목으로 넘어가라.
