# Claude Code 수정 지시 3차

배포된 화면을 확인하고 정리한 수정 사항이다.
[0]번부터 [9]번까지 순서대로 진행하고, 각 항목마다 검증한 뒤
다음으로 넘어가라. 내 확인을 기다리지 마라.

디자인 전면 개편(taste-skill)은 이번에 하지 않는다.
기능과 문구가 확정된 뒤에 별도로 진행한다.

===========================================================
[0] 프리셋 버튼이 안 나오는 문제 (먼저 확인)
===========================================================

배포된 화면에서 대분류를 고르면 시험명 버튼이 나와야 하는데
입력창만 뜬다. PRD 10.3의 확정 사항이 반영되지 않았다.

  대분류 선택
    → 해당 분류의 시험명 버튼 (6~10개)
    → 마지막에 "직접 입력할게요"
    → 그것을 누를 때만 입력창

lib/content/preset-exams.json이 화면에서 실제로 쓰이는지
확인하고, 안 쓰이면 연결해라.

그리고 입력창 placeholder가 대분류와 무관하다. 기업 필기를
골랐는데 "국가직 9급 공무원, LEET, 토익"이 예시로 나온다.
해당 분류의 시험명 3개를 가져다 써라.

아래도 함께 확인해서 보고해라.
  자격증·어학에서 subGroups 2단계가 나오는지
  기타에서 freeInputOnly로 바로 입력창이 뜨는지
  대학교 시험에서 시험 기간을 묻는지
  오디션 방식이 선택되고 오디션 조각이 나오는지

===========================================================
[1] 대화 문구 전면 수정
===========================================================

1-1. 첫 인사를 말풍선 3개로 나눈다

  [합격이] 안녕하세요, 여러분의 합격을 응원하는
          합격이예요.

  [합격이] 꼭 합격하시길 바라며,
          시험 보는 날의 운을 봐드릴게요.

  [합격이] 어떤 시험 준비하세요?

  한 말풍선에 세 문장이 들어가면 길어 보인다.
  순차로 뜨면 대화 느낌이 살아난다.

1-2. 대분류별 공감 문구를 넣는다

  대분류를 고르면 한 줄 공감 후 다음 질문으로 간다.
  두 문장을 넘기지 않는다.

  수능 · 모의고사
    수능 준비하시는군요.
    그동안 고생 많으셨죠?
    어떤 시험이에요?

  공무원 · 고시
    공무원 시험 준비하시는군요.
    긴 싸움이라 힘드시죠?
    어떤 시험이에요?

  자격증 · 어학
    자격증이신가요, 어학이신가요?
    (2단계 후) 어떤 시험이에요?

  기업 필기
    기업 필기 준비하시는군요.
    인적성은 준비가 까다롭죠.
    어떤 시험이에요?

  기업 면접
    네! 기업 면접을 앞두고 있군요.
    많이 긴장되시죠?
    (이후 1-3 참조)

  편입 · 대학원
    편입이나 대학원 준비하시는군요.
    쉽지 않은 길 택하셨네요.
    어떤 시험이에요?

  대학교 시험
    학교 시험이시군요.
    시험 기간이 제일 정신없죠.
    어떤 시험이에요?

  승진 · 사내시험
    승진 시험 준비하시는군요.
    일하면서 준비하시느라 힘드시겠어요.
    어떤 시험이에요?

  오디션 · 실기
    오디션 준비하시는군요.
    무대에 서기까지가 제일 떨리죠.
    어떤 오디션이에요?

  기타
    어떤 시험인지 알려주시겠어요?

1-3. 면접 흐름을 바꾼다

  기존
    기업 면접시군요! 어떤 시험이에요?
    → 대기업 1차 면접
    면접이시군요! 어떤 회사예요?
    → 대기업
    어떤 일에 가까워요?
    → 사람을 만나는 일
    직무 이름을 알려주실래요?

  변경
    네! 기업 면접을 앞두고 있군요.
    많이 긴장되시죠?
    → (다음 말풍선)

    어떤 업무 성격에 가까운지 물어봐도 될까요?
    [사람을 만나는 일] [분석하고 만드는 일]
    [조율하고 운영하는 일] [현장에서 움직이는 일]
    → 사람을 만나는 일

    직무 이름도 알려주시겠어요?
    영업관리처럼 적어주시면 돼요.
    → 영업직

  바뀐 점
    "기업 면접시군요"는 조사가 어색해 삭제
    "면접이시군요" 중복 확인 삭제
    기업 규모 질문 삭제 ([2] 참조)
    업무 성격을 먼저 묻고 직무명을 나중에
    직무 예시를 "반도체 공정기술"에서 "영업관리"로

1-4. 개별 문구 교체

  "어떤 일에 가까워요?"
    변경  "어떤 업무 성격에 가까운지 물어봐도 될까요?"

  "이제 사주를 볼게요. 생년월일이 어떻게 되세요?"
    변경  "이제 사주와 그날의 기운 흐름을 볼게요.
       생년월일이 어떻게 되세요?"

1-5. 유료 결제를 입력 단계에서 언급하지 마라

  입력 중에 "유료 결제하면 궁합도 봐드려요" 같은 문구를
  넣지 않는다. 광고로 읽히고 무료 결과의 가치가 미리
  깎인다.

  유료는 무료 결과의 잠금 영역과 CTA에서만 언급한다.

===========================================================
[2] 기업 규모 질문 삭제
===========================================================

"어떤 회사예요?" 질문과 6개 선택지(대기업, 중견기업,
공기업, 금융권, 스타트업, 기타)를 없앤다.

이유
  선택지 6개로 얻는 것이 문장 하나뿐이다
  취준생 대부분이 중소·중견인데 대기업이 첫 버튼이라
  해당 없다고 느끼기 쉽다
  유료에서 기업명을 받아 검색하면 규모까지 나오므로
  무료에서 따로 물을 이유가 없다

처리
  fragments.json의 companyScale 6개는 파일에 두되
  조립 함수에서 호출하지 않는다
  무료 조각 총 개수 222개에서 216개로

  queries.company_scale 컬럼은 그대로 둔다
  값이 null로 들어갈 뿐이고, 나중에 되살릴 때
  마이그레이션이 필요 없다

  면접 카드 2 조립에서 기업 규모를 뺀다
    변경 전  방식 특성 + 기업 규모 + 일의 성격×강오행 + 방식×약오행
    변경 후  방식 특성 + 일의 성격×강오행 + 방식×약오행

PRD 수정
  10.8 기업 규모 6분류 섹션 삭제
  3.6 카드 2 조립 규칙 수정
  3.7 무료 조각 216개로
  14.7 면접 입력 단계에서 제거
  21.11 "어떤 회사예요?" 삭제
  8.16 프롬프트 재료에서 companyScale 제거

===========================================================
[3] 입력 위젯 교체
===========================================================

3-1. 생년월일을 숫자 직접 입력으로

  기본 달력으로 1990년을 찾으려면 420번 넘겨야 한다.
  여기서 이탈한다.

  ┌────────┬──────┬──────┐
  │  1990  │  05  │  15  │
  │   년    │  월   │  일   │
  └────────┴──────┴──────┘

  inputmode="numeric"으로 숫자 키패드를 띄운다
  년 4자리를 채우면 월로, 월 2자리를 채우면 일로 자동 이동
  각 칸 높이 52px, 글자 20px

3-2. 양력·음력 선택을 추가한다

  [합격이] 이제 사주와 그날의 기운 흐름을 볼게요.
          생년월일이 어떻게 되세요?

          [양력] [음력]

  음력을 고르면 윤달을 묻는다.

  [합격이] 윤달이신가요?
          [아니요] [네, 윤달이에요]

  음력 날짜를 받아 양력으로 변환한 뒤 확인시킨다.

  [합격이] 양력으로 1990년 5월 9일이시네요.
          이 날짜로 봐드릴게요.

  구현
    korean-lunar-calendar 라이브러리를 쓴다
    한국 음력 규칙에 맞고 파일이 작다
    변환된 양력 날짜로 사주를 계산한다
    queries에 원본 음력 정보도 저장한다
      is_lunar boolean
      is_leap_month boolean
      lunar_date date

  PRD 4.1.2가 "음력 변환은 필요하지 않다"고 되어 있는데,
  사주 계산에는 필요 없지만 입력 받을 때는 필요하다.
  이 부분을 수정해라.

3-3. 태어난 시간

  [합격이] 태어난 시간도 아세요?
          모르셔도 결과는 나와요.
          [알아요] [모르겠어요]

  [합격이] 몇 시쯤 태어나셨어요?
          [새벽] [오전] [오후] [밤]
          → 오후
          [ 2 ] 시  [ 30 ] 분

  4구간으로 나누는 이유는 "오전 12시"와 "오후 12시"가
  헷갈리기 때문이다. 배포 화면에서 "오전 12시 53분"이
  선택되는 것을 확인했다.

    새벽  00~05
    오전  06~11
    오후  12~17
    밤    18~23

  분은 선택으로 두고, 안 넣으면 0분으로 처리한다.

3-4. 시험 시작 시간

  자주 쓰는 시각을 버튼으로 둔다.

  [합격이] 몇 시에 시작해요?
          [오전 9시] [오전 10시]
          [오후 1시] [오후 2시]
          [직접 입력] [모르겠어요]

  시험 시작 시각은 9시, 10시가 압도적으로 많다.
  대부분 탭 한 번으로 끝난다.

3-5. 오입력 검증

  범위를 벗어나면 그 자리에서 되묻는다.

    년   1900 이상, 올해 이하
    월   1~12
    일   해당 월의 일수

  [합격이] 1890년이 맞으세요?
          1990년을 적으신 건 아닐까요?
          [맞아요] [다시 입력할게요]

===========================================================
[4] 랜딩 카피 교체
===========================================================

4-1. 히어로 버튼 영역

  기존
    [합격이랑 이야기하기]
    1분이면 끝나요

  변경
    내 시험운은 어떨까?
    [합격이에게 내 시험운 물어보기]
    1분이면 끝나요 · 로그인 없이

  버튼 위에 "내 시험운은 어떨까?"를 두어 궁금증을 만들고
  버튼이 답하는 구조로 만든다. 버튼과 간격을 좁게 둔다.

  하단의 두 번째 버튼도 같은 문구로 통일한다.

4-2. 차별점 카드를 4개로

  현재 3개를 4개로 늘리고 내용을 교체한다.

  시험 날짜를 계산합니다
    나의 사주 오행과 십신 흐름을
    시험 날짜에 맞춰 봅니다

  필기와 면접이 다릅니다
    공무원, 어학, 자격증, 기업 면접까지
    시험 특성과 내 사주 오행의 흐름을
    파악해 합격 맞춤 전략을 알려드려요

  시험 전 7일을 봅니다
    본인 사주 오행에 따라
    어느 날 집중이 되고 어느 날 쉬어야 하는지

  AI와 대화하며 봅니다
    사주 명리에 기초해
    합격이가 묻고 답해드려요

  네 번째가 신규다. "챗GPT처럼"이라는 표현은 쓰지 마라.
  실제로는 버튼 선택 방식이라 기대와 다르면 실망한다.

  두 번째 카드만 네 줄이라 다른 카드보다 커진다.
  그대로 두어도 된다. 정보량이 많은 카드가 커 보이는 것은
  자연스럽다.

4-3. 미리보기 카드를 실제 결과 조각으로

  현재 목록 8개 나열은 정보량이 없다. 제목만 봐서는
  무엇을 받는지 감이 오지 않는다.

  아래 구성으로 바꾼다.

    상단  합격이 + 유형 뱃지 + 당일 운 78 (현재 유지)

    1. 7일 기운 흐름 막대 차트
       8개 막대, 마지막(당일)만 진한 색
       아래 D-7과 당일 라벨

    2. 2열 그리드
       왼쪽  행운의 숫자 (큰 숫자 하나)
       오른쪽 피해야 할 색 (색 원 2개)

    3. 시험 유형 궁합 막대
       면접 81, 필기 74 두 줄만

    4. 블러 처리한 문장 한 줄
       "화가 강한 구성이라 초반 몰입도가 높습니다.
        다만 그 속도가 중반에 한 번 꺾이는데..."
       filter: blur(3.5px), user-select: none

    5. 하단
       "카드 8개 전부 보기" + 자물쇠 아이콘

  차트로 차별점을 보여주고, 블러 문장 하나로 글도
  이만큼 나온다는 것을 암시하는 구성이다.

  숫자와 데이터는 예시로 고정한다. 랜딩은 비로그인
  상태라 계산할 입력이 없다.

===========================================================
[5] 마퀴 섹션 신규 추가
===========================================================

히어로 바로 아래, 차별점 카드 위에 배치한다.
시작 버튼을 누르지 않고 스크롤한 사람에게 "여기가 뭐 하는
곳인지" 먼저 보여준다.

  세상 모든 시험

  [마퀴 1행, 왼쪽으로]
  공무원 시험 · 자격증 · 대기업 면접 · 수능 · 오디션

  [마퀴 2행, 오른쪽으로]
  편입 · 어학 · 승진 시험 · 모의고사 · 실기

  내 인생의 터닝 포인트가 되는 날
  그날의 운과 잠재력 발휘 지수를
  AI 합격이가 봐드려요

구현

  ```jsx
  <div style={{
    overflow: "hidden",
    maskImage: "linear-gradient(90deg, transparent, black 12%, black 88%, transparent)"
  }}>
    <div style={{
      display: "flex", gap: 24,
      width: "max-content",
      animation: "scroll 28s linear infinite"
    }}>
      {[...items, ...items].map((t, i) => (
        <span key={i} style={{ whiteSpace: "nowrap" }}>{t}</span>
      ))}
    </div>
  </div>
  ```

  ```css
  @keyframes scroll {
    from { transform: translateX(0); }
    to   { transform: translateX(-50%); }
  }
  ```

핵심 세 가지
  목록을 두 번 반복해야 끊김 없이 이어진다
  maskImage로 양끝을 흐리게 한다
  width: max-content가 있어야 flex가 안 줄어든다

속도
  1행 28초, 2행 34초
  방향을 반대로, 속도를 다르게 하면 자연스럽다

prefers-reduced-motion에서는 애니메이션을 끄고
줄바꿈으로 전부 표시한다.

===========================================================
[6] 모션 강화
===========================================================

지금 랜딩 모션이 약하다. 진입 애니메이션이 없어 페이지에
들어오면 모든 게 이미 떠 있고, 값이 소극적이다.

6-1. 히어로 진입 스태거

  ```jsx
  const container = {
    hidden: {},
    show: { transition: { staggerChildren: 0.08, delayChildren: 0.1 } }
  }

  const item = {
    hidden: { opacity: 0, y: 32, filter: "blur(4px)" },
    show: {
      opacity: 1, y: 0, filter: "blur(0px)",
      transition: { type: "spring", stiffness: 260, damping: 24 }
    }
  }
  ```

  blur가 핵심이다. 흐릿하다가 선명해지면 훨씬 부드럽고
  고급스럽게 보인다.

  적용 순서
    캐릭터, 제목, 설명, 응원 문구, 버튼 순서

  전체가 0.7초 안에 끝나야 한다. 버튼이 늦게 나오면
  조급한 사용자가 답답해한다.

6-2. 캐릭터만 별도 값

  ```jsx
  hidden: { opacity: 0, y: 40, scale: 0.88 },
  show: {
    opacity: 1, y: 0, scale: 1,
    transition: { type: "spring", stiffness: 200, damping: 18 }
  }
  ```

  damping 18이면 도착할 때 살짝 튕긴다. 캐릭터라 어울린다.

6-3. 차별점 카드와 마퀴도 variants 방식으로

  부모에 whileInView, 자식에 variants를 주면 스태거가
  자동으로 걸린다.

  ```jsx
  <motion.div
    variants={container}
    initial="hidden"
    whileInView="show"
    viewport={{ once: true, margin: "-80px" }}
  >
  ```

  스크롤 리빌에는 blur를 쓰지 마라. 요소가 많으면
  프레임이 떨어진다. y와 opacity만 쓴다.

6-4. 기존 값 상향

  y 24에서 32로
  tween에서 spring으로
  duration 기반보다 물리 기반이 눈에 띈다

===========================================================
[7] 프레스 스테이트 강화
===========================================================

효과음은 넣지 않는다. 시각으로 확실하게 만든다.

7-1. 모든 버튼

  ```jsx
  whileTap={{ scale: 0.94, backgroundColor: "#DCE9FA" }}
  transition={{ type: "spring", stiffness: 500, damping: 25 }}
  ```

  세 가지가 겹쳐야 확실하다.
    크기  scale 0.94 (0.97보다 확실)
    색    배경이 살짝 진해짐
    속도  stiffness 500으로 즉각 반응

7-2. 선택 후 잠깐 강조

  ```jsx
  animate={selected ? { scale: [0.94, 1.02, 1] } : {}}
  transition={{ duration: 0.25 }}
  ```

  눌렀다가 살짝 커졌다 제자리로 돌아온다.
  "선택됐다"가 명확해진다.

7-3. 시작 버튼 클릭 유도

  숨쉬기
  ```jsx
  animate={{ scale: [1, 1.02, 1] }}
  transition={{ duration: 2.4, repeat: Infinity,
                ease: "easeInOut", repeatDelay: 1 }}
  ```

  빛 훑기
  ```jsx
  <motion.div
    animate={{ x: ["-120%", "220%"] }}
    transition={{ duration: 1.6, repeat: Infinity, repeatDelay: 2.5 }}
    style={{
      position: "absolute", width: "40%", height: "100%",
      background: "linear-gradient(90deg, transparent, rgba(255,255,255,0.25), transparent)",
      transform: "skewX(-20deg)"
    }}
  />
  ```

  손가락 아이콘은 쓰지 마라. 촌스럽고 사주 서비스 톤에
  맞지 않는다.

7-4. 햅틱

  ```javascript
  navigator.vibrate?.(8)
  ```

  대화형 선택지 탭에만 적용한다.
  iOS는 미지원이므로 optional chaining으로 처리하고
  지원 여부를 확인하지 않아도 된다.

===========================================================
[8] 대화 말풍선 모션
===========================================================

카카오톡처럼 프로필에서 튀어나오는 방식으로 바꾼다.
지금보다 확실하고, 좀 느려도 된다.

8-1. 합격이 말풍선

  ```jsx
  const bubble = {
    hidden: { opacity: 0, scale: 0.3, x: -20, y: 8 },
    show: {
      opacity: 1, scale: 1, x: 0, y: 0,
      transition: { type: "spring", stiffness: 400,
                    damping: 22, mass: 0.8 }
    }
  }
  ```

  ```css
  .bot-bubble { transform-origin: left bottom; }
  ```

  transform-origin이 핵심이다. 왼쪽 아래를 기준으로
  커져야 프로필에서 나오는 느낌이 난다.

8-2. 사용자 답변

  ```jsx
  hidden: { opacity: 0, scale: 0.3, x: 20, y: 8 }
  ```

  ```css
  .user-bubble { transform-origin: right bottom; }
  ```

8-3. 프로필 아이콘 반응

  ```jsx
  animate={isNewMessage ? { scale: [1, 1.08, 1] } : {}}
  transition={{ duration: 0.3 }}
  ```

  프로필이 먼저 반응하면 "합격이가 말을 시작한다"는
  느낌이 난다.

8-4. 타이핑 인디케이터

  첫 인사에만 넣거나 0.3초로 짧게 한다.
  매 질문마다 0.6초씩 넣으면 9단계에서 5.4초가 늘어난다.

8-5. 자동 스크롤

  ```javascript
  element.scrollIntoView({ behavior: 'smooth', block: 'end' })
  ```

===========================================================
[9] 차트 수정
===========================================================

9-1. 7일 흐름 그래프가 막대가 아니라 원형으로 보인다

  배포 화면에서 알약 모양이 공중에 떠 있다.

  원인
    align-items: flex-end가 없어 바닥 정렬이 안 됨
    border-radius가 과해서 원형이 됨
    높이 차이가 작아 흐름이 안 보임

  수정
  ```css
  .chart {
    display: flex;
    align-items: flex-end;
    gap: 5px;
    height: 60px;
  }
  .bar {
    flex: 1;
    border-radius: 4px 4px 2px 2px;
    background: #B5D4F4;
  }
  .bar.today { background: #378ADD; }
  ```

  최소 높이를 보장해라. 점수가 20점이면 높이가 12px밖에
  안 되어 안 보인다.

  ```javascript
  const h = 25 + (score / 100) * 75   // 0~100을 25~100%로
  ```

9-2. 막대 순차 애니메이션

  ```jsx
  <motion.div
    initial={{ height: 0 }}
    whileInView={{ height: `${h}%` }}
    viewport={{ once: true, margin: "-40px" }}
    transition={{ duration: 0.5, delay: i * 0.08,
                  ease: [0.22, 1, 0.36, 1] }}
  />
  ```

  마지막(당일) 막대는 한 박자 쉬고 스프링으로 올라온다.

  ```jsx
  transition={
    isToday
      ? { type: "spring", stiffness: 200, damping: 12, delay: 0.7 }
      : { duration: 0.5, delay: i * 0.08 }
  }
  ```

  막대가 다 오른 뒤 숫자가 뜬다.

9-3. 같은 애니메이션을 다른 차트에도

  오행 분포 막대 (ElementBar)
  시험 유형 궁합 막대
  랜딩 미리보기의 차트

===========================================================
[10] 결과 전환 연출
===========================================================

커튼 효과는 쓰지 않는다. 토스처럼 자연스러운 페이드가
낫다. 커튼은 화려하지만 매번 보면 무겁다.

10-1. 결과 보기 버튼 로딩 상태

  ```jsx
  {loading ? <DotsLoader /> : "결과 보기"}
  ```

  점 3개가 순차로 커졌다 작아진다.

  ```jsx
  animate={{ opacity: [0.3, 1, 0.3], scale: [0.85, 1, 0.85] }}
  transition={{ duration: 1, repeat: Infinity,
                delay: i * 0.15, ease: "easeInOut" }}
  ```

  버튼 폭을 min-width로 고정해라. 텍스트가 점으로 바뀌면서
  폭이 줄면 덜컥거린다.

10-2. 최소 로딩 시간

  계산은 즉시 끝나지만 버튼이 반응한 걸 인지할 시간이
  필요하다. 800ms를 최소로 둔다.

  ```javascript
  const MIN_LOADING = 800
  const start = Date.now()
  const result = calculateResult(input)
  const elapsed = Date.now() - start
  if (elapsed < MIN_LOADING) {
    await new Promise(r => setTimeout(r, MIN_LOADING - elapsed))
  }
  ```

10-3. 화면 전환

  ```jsx
  <AnimatePresence mode="wait">
    <motion.div key="chat" exit={{ opacity: 0, y: -8 }}
      transition={{ duration: 0.2 }} />
    <motion.div key="result"
      initial={{ opacity: 0, y: 12 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.35, ease: [0.22, 1, 0.36, 1] }} />
  </AnimatePresence>
  ```

  mode="wait"가 핵심이다. 나가는 화면이 완전히 사라진 뒤
  들어오는 화면이 시작된다. 없으면 두 화면이 겹쳐 보인다.

10-4. 결과 진입 후

  0.0s  결과 화면 진입
  0.1s  캐릭터 + 뱃지
  0.2s  운 지수 숫자 카운트업 시작
  0.3s  카드 1
        이후는 스크롤 리빌

  카운트업
  ```jsx
  animate(0, score, {
    duration: 1.2, ease: "easeOut",
    onUpdate: v => setDisplay(Math.round(v))
  })
  ```

===========================================================
[11] 접근성
===========================================================

useReducedMotion 훅으로 [5][6][7][8][9][10] 전부를
제어한다.

  const shouldReduceMotion = useReducedMotion()

prefers-reduced-motion이 켜져 있으면 애니메이션 없이
최종 상태로 바로 표시한다.

마퀴는 정지하고 줄바꿈으로 전부 표시한다.

===========================================================
[12] 검증
===========================================================

각 항목마다 아래를 확인한다.

  npx tsc --noEmit 통과
  npm run build 통과

전체 수정 후 배포하고 실제 모바일에서 확인할 것을
목록으로 정리해서 알려줘라.

  대분류 10개가 나오는지
  시험명 프리셋 버튼이 나오는지
  자격증·어학 2단계가 나오는지
  대학교 시험 기간을 묻는지
  기업 규모 질문이 사라졌는지
  생년월일 숫자 입력이 되는지
  음력 선택과 변환이 되는지
  마퀴가 끊김 없이 흐르는지
  히어로 진입 애니메이션이 보이는지
  버튼 탭 반응이 확실한지
  말풍선이 프로필에서 튀어나오는지
  7일 차트가 막대로 나오는지
  결과 전환이 매끄러운지

PROGRESS.md에 수정 내역을 기록해라.

===========================================================
[13] 하지 말 것
===========================================================

taste-skill 설치나 전면 디자인 개편은 이번에 하지 마라.
기능과 문구가 확정된 뒤에 별도로 진행한다.

PRD에 확정된 색상, 폰트, 하단 탭 구조를 임의로 바꾸지 마라.

내가 확정한 카피 문구를 고치지 마라.
