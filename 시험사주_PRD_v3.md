# 시험사주 (siheomsaju) - 제품 요구사항 정의서

문서 버전 3.1
작성일 2026-08-22
대상 개발 도구 Claude Code

---

## 1. 서비스 개요

### 1.1 정의

생년월일시와 시험 정보를 입력받아, 사주 기반의 시험 대비 운세와 준비 가이드를 제공하는 모바일 웹서비스입니다.

### 1.2 기존 사주 서비스와의 차이

기존 사주 서비스는 생년월일시만 입력받아 범용 운세를 제공합니다. 이 서비스는 시험명, 시험 날짜, 시험 방식, 시작 시간을 추가로 입력받고 이 값들을 계산에 직접 사용합니다.

그 결과 다음 항목이 가능해집니다.

1. 시험 전 7일간의 날짜별 기운 흐름 계산
2. 사용자에게 유리한 시험 방식(필기, 논술, 면접, 실기) 산출
3. 시험 시작 시간과 사용자 기운의 관계 분석
4. 면접의 경우 기업 법인 설립일 기반 궁합 계산

이 항목들은 시험 정보를 입력받지 않는 서비스에서는 만들 수 없습니다.

### 1.3 서비스명 및 도메인

- 서비스명 시험사주
- 화면 카피 오늘의 시험운
- 도메인 후보 siheomsaju.com

### 1.4 타깃

- 1차 공무원, 자격증, 기업 채용 준비생 (20-30대)
- 2차 수능, 편입, 대학원 준비생

### 1.5 수익 구조

```
유료 리포트  3,900원 일회성
필기 리포트 / 면접 리포트 2종
```

3,900원 결제는 매출 자체보다 세 가지 역할을 합니다. 첫째, 이 서비스가 돈을 받을 만한 상품인지 검증합니다. 둘째, 결제자 계정과 연락처를 확보합니다. 셋째, 이후 제휴 광고 협상에서 지갑을 여는 트래픽이라는 증명으로 사용합니다.

실제 수익의 중심은 20장의 확장 항목에 있습니다.

---

## 2. 사용자 흐름

```
[홈 /]  탭 1
  비로그인: 서비스 설명, 차별점, 미리보기
  로그인:   오늘의 운, 진행 중인 시험 D-day
     |
[입력 /start]  탭 2
  방식에 따라 8-9단계, 탭바 숨김
     |
[무료 결과 /result]
  로그인 불필요, 즉시 응답
  카드 8개, AI 호출 없음
     |
  [친구에게 공유] 이미지 1장 생성
  [내 결과 저장하기] 로그인 유도
     |
[결제 유도 CTA]
  면접인 경우 기업명 추가 입력 요구
     |
[로그인 /login]
  카카오 / 구글 / 이메일
     |
[결제] 3,900원 일회성
     |
[유료 리포트 /report/:id]
  AI 생성, 78~100초
  Supabase 저장
  생성 실패 시 재시도 버튼 노출
     |
  [카카오톡으로 나에게 보내기]
     |
[마이페이지 /my]  탭 3
  계정 정보
  리포트 목록 (유료 + 무료)
  내 정보 수정 / 결제 내역
  문의하기 / 자주 묻는 질문
  설정 / 약관 / 로그아웃 / 탈퇴
```

AI 호출은 유료 리포트 생성 시 1회만 발생합니다. 그 외 모든 구간은 계산과 문장 조각 조립으로 처리합니다.

---

## 3. 무료 구간 상세

### 3.1 원칙

- 로그인 없이 이용 가능
- AI API 호출 없음
- 검색 호출 없음
- 응답 시간 즉시
- 같은 입력에는 항상 같은 결과 (결정론적)

무료 구간을 문장 조각 조립으로 설계한 이유는 원가와 속도입니다. 무료를 AI로 생성하면 월 방문 1만 명 기준 약 15만원이 나가고, 응답에 3-8초가 붙습니다.

### 3.2 무료 결과 화면 구성

```
[상단 요약]
  합격이 캐릭터 (표정 5단계)
  말풍선 (D-day 구간별)
  유형 뱃지 (오행 5종)
  이름 / 시험명 / 방식 / D-day
  당일 운 지수 | 오늘의 운 지수
  잠재력 발휘 지수 (잠금, 숫자 가림)

━━ 무료로 보는 내 시험운 ━━

[카드 1] 4월 11일, OOO님에게 어떤 날인가     전체 공개
[카드 2] OOO님의 사주                       부분 잠금
[카드 3] 시험 전 7일 기운 흐름               부분 잠금
[카드 4] 오전 10시는 맞는 시간일까            부분 잠금
[카드 5] OOO님은 어떤 시험에 강한가           전체 공개
[카드 6] 시험장에서 주의할 점                 전체 공개
[카드 7] 행운의 숫자와 피해야 할 색            부분 잠금
[카드 8] 시험 전날 밤에는                     전체 공개

[응원 문구]
[버튼] 친구에게 공유
[버튼] 내 결과 저장하기

━━ 유료 리포트에서만 볼 수 있어요 ━━

[잠금 목록] 제목 + 한 줄 설명
[CTA] 3,900원으로 전체 보기

[하단] 오락 목적 고지
```

### 3.3 카드 순서를 차별화 우선으로 배치한다

카드 1부터 4까지가 시험 정보를 계산에 사용한 항목입니다. 일반 사주 서비스가 만들 수 없는 내용을 앞에 둡니다.

기존 순서는 일반적인 사주 해석이 앞에 있어, 사용자가 카드 1과 2를 읽으며 "일반 사주와 같다"고 판단할 위험이 있었습니다.

| 카드 | 근거 | 차별화 |
|---|---|---|
| 1 | 시험일 일진 × 사용자 일간 | O |
| 2 | 8글자 명식 + 오행 분포 | O (명식 표시) |
| 3 | 7일간 일진 계산 | O |
| 4 | 시작 시간 지지 × 사용자 오행 | O |
| 5 | 오행별 방식 궁합 | O |
| 6 | 방식 × 강약 오행 | 부분 |
| 7 | 약한 오행 숫자, 강한 오행 색 | 부분 |
| 8 | 강약 오행 야간 특성 | 부분 |

### 3.4 두 종류의 잠금

| 유형 | 위치 | 보이는 것 | 목적 |
|---|---|---|---|
| 부분 잠금 | 카드 안 하단 | 앞부분 공개, 뒤가 잠김 | 읽다가 막히게 |
| 전체 잠금 | 무료 카드 뒤 | 제목 + 한 줄 설명 | 목록으로 보여주기 |

#### 부분 잠금 4곳

```
카드 2  명식과 오행 분포 공개
        🔒 십신으로 본 시험 패턴

카드 3  차트와 요약 3줄 공개
        🔒 날짜별 상세 플랜

카드 4  시작 시각 해석 공개
        🔒 하루 전체 시간대별 운용

카드 7  행운 숫자 1개 + 피해야 할 색 공개
        🔒 좋은 색, 방위, 시간대
```

부분 잠금은 차별화 카드에만 겁니다. 일반적인 내용(카드 6, 8)은 전부 열어둡니다. 잠가도 유인이 되지 않고, 열어두면 공유 가치가 생깁니다.

잠긴 영역에도 한두 줄 설명을 둡니다. 제목만 있으면 무엇인지 모르고, 설명이 있으면 궁금해집니다.

#### 카드 7의 무료와 유료 배분

무료가 "하지 말 것", 유료가 "할 것"입니다. 이 대비가 명확해 잠금이 자연스럽습니다.

| 항목 | 무료 | 유료 |
|---|---|---|
| 숫자 | 1개 + 활용법 | 2개 전부 |
| 피해야 할 색 | 있음 (강한 오행 기준) | 없음 |
| 좋은 색 | 없음 | 있음 + 적용 위치 |
| 방위 | 없음 | 있음 |
| 시간대 | 없음 | 있음 |

피해야 할 색은 강한 오행 기준입니다. 강한 것을 더 키우는 색을 피하는 논리이므로 약한 오행이 아니라 강한 오행을 봅니다.

#### 전체 잠금 목록

필기 8개, 면접 9개입니다.

```
필기
  🔒 잠재력 발휘 지수
  🔒 내 사주가 말하는 시험 패턴
  🔒 놓치기 쉬운 3가지
  🔒 시험 전날 상세 타임라인
  🔒 남은 기간 어떻게 쓸까
  🔒 좌석과 방위
  🔒 이 기간 피해야 할 것
  🔒 2026년 시험운 캘린더
  🔒 내 사주로 본 시험 전략

면접
  🔒 기업 설립일로 본 궁합
  🔒 이 직무와 나
  🔒 들어올 가능성이 높은 질문
  🔒 잠재력 발휘 지수
  🔒 내 사주가 말하는 면접 패턴
  🔒 내가 조심해야 할 3가지
  🔒 복장과 소지품
  🔒 면접 전날 상세
  🔒 2026년 면접운 캘린더
  🔒 내 사주로 본 면접 전략
```

각 항목에 한 줄 설명을 붙입니다. 이 목록이 상품 설명 역할을 합니다.

면접의 헤더는 "실제 면접 보는 기업을 알려주시면"으로 두어, 기업명 입력이 결제 트리거임을 명확히 합니다.

### 3.5 카드 제목의 방식별 변화

```
카드 1
  공통            {시험일}, OOO님에게 어떤 날인가

카드 2
  공통            OOO님의 사주

카드 3
  필기 · 실기      시험 전 7일 기운 흐름
  면접            면접 전 7일 기운 흐름
  오디션          오디션 전 7일 기운 흐름

카드 4
  공통            {시작시각}은 맞는 시간일까

카드 5
  공통            OOO님은 어떤 시험에 강한가

카드 6
  필기 · 실기      시험장에서 주의할 점
  면접            면접장에서 주의할 점
  오디션          심사장에서 주의할 점

카드 7
  공통            행운의 숫자와 피해야 할 색

카드 8
  필기 · 실기      시험 전날 밤에는
  면접            면접 전날 밤에는
  오디션          오디션 전날 밤에는
```

카드 1의 날짜는 시험 방식과 무관하게 같은 표현을 씁니다. 면접이든 오디션이든 "그날이 어떤 날인가"는 동일합니다.

### 3.6 카드별 조립 규칙

| 카드 | 조각 구성 |
|---|---|
| 상단 | 말풍선 (D-day 구간 5) |
| 1 | 일간 + 강오행 + 약오행 + 일진 + 종합 판정 |
| 2 | 오행 요약 (강오행 5) + 잠금 티저 |
| 3 | 7일 흐름 요약 (패턴 6) + 잠금 티저 |
| 4 | 시간대 관계 × 방식 + 잠금 티저 |
| 5 | 계산 결과 차트 (문장 없음) |
| 6 | 방식 특성(변형 7) + 방식×강오행 + 방식×약오행 + (면접) 일의 성격 |
| 7 | 약오행 숫자 + 방식별 활용 + 강오행 피할 색 + 잠금 티저 |
| 8 | 강오행 전날 + 약오행 전날 + 방식별 추가 |

### 3.7 문장 조각 총 개수

| 구분 | 조각 | 개수 |
|---|---|---|
| 상단 | 말풍선 (D-day 구간) | 5 |
| 뱃지 | 유형 설명 (오행) | 5 |
| 1 | 일간 소개 | 10 |
| 1 | 강한 오행 성향 | 5 |
| 1 | 약한 오행 보완점 | 5 |
| 1 | 일진 관계 | 5 |
| 1 | 종합 판정 | 5 |
| 2 | 오행 요약 | 5 |
| 3 | 7일 흐름 요약 | 6 |
| 3 | 일별 기운 라벨 | 6 |
| 4 | 시간대 관계 × 방식 4종 | 20 |
| 6 | 방식 특성 (4종 × 변형 7) | 28 |
| 6 | 방식 × 강한 오행 | 20 |
| 6 | 방식 × 약한 오행 | 20 |
| 6 | 일의 성격 × 강한 오행 (면접) | 20 |
| 7 | 약오행 숫자 설명 | 5 |
| 7 | 방식별 숫자 활용 | 4 |
| 7 | 강오행 피할 색 | 5 |
| 8 | 강오행 전날 | 5 |
| 8 | 약오행 전날 | 5 |
| 8 | 방식별 전날 추가 | 4 |
| 잠금 | 잠금 티저 | 4 |
| 유료 이관 | 약오행 좋은 색 | 5 |
| 유료 이관 | 방식별 복장 | 4 |
| 유료 이관 | 약오행 방위 | 5 |
| 유료 이관 | 약오행 시간대 | 5 |
| | **합계** | **222** |

유료 전용 조각은 8.18에서 별도로 정의합니다.

응원 문구 17개는 /lib/content/quotes.json에서 관리합니다.

### 3.8 조립 시 일관성 규칙

조각을 독립적으로 작성하면 한 카드 안에서 좋다와 나쁘다가 섞여 결론이 흐려집니다. 사용자가 "그래서 잘 본다는 건가 못 본다는 건가"를 묻게 되면 실패입니다. 다음 규칙을 지킵니다.

1. 개별 조각에서 좋다, 유리하다, 불리하다 같은 단정을 쓰지 않습니다
2. 모든 조각은 "이런 면이 있고, 대신 이런 면이 있다" 구조로 작성합니다
3. 판정은 카드 1의 마지막 조각(종합 판정)에서만 합니다
4. 낮은 점수 판정에는 반드시 행동 조언을 붙입니다
5. 조각끼리 같은 단어를 반복하지 않도록 어휘를 배분합니다

이 방식은 사주 해석 원리와도 맞습니다. 사주에서 오행이 강하다는 것은 좋은 것도 나쁜 것도 아니고 특성입니다.

### 3.9 변형 선택 알고리즘

방식 특성 조각은 7개 변형 중 하나를 고릅니다. 사주에서 이미 계산된 값을 사용해 결정론을 유지합니다.

```typescript
const variantIndex = saju.dayPillarIndex % 7
```

dayPillarIndex는 일주의 60갑자 인덱스(0-59)입니다. 60을 7로 나누므로 분포가 비교적 고릅니다.

### 3.10 변수 치환

문장 조각에는 다음 변수가 들어갑니다.

```
{name}   사용자 이름
{exam}   시험명
{job}    직무명 (면접, 없으면 일의 성격 표현으로 대체)
```

한글 받침에 따라 조사가 달라지므로 조사 처리 함수가 필요합니다.

```typescript
function attachParticle(word: string, type: '은는' | '이가' | '을를'): string {
  const last = word.charCodeAt(word.length - 1)
  if (last < 0xAC00 || last > 0xD7A3) {
    return word + (type === '은는' ? '은' : type === '이가' ? '이' : '을')
  }
  const hasJongseong = (last - 0xAC00) % 28 > 0
  if (type === '은는') return word + (hasJongseong ? '은' : '는')
  if (type === '이가') return word + (hasJongseong ? '이' : '가')
  return word + (hasJongseong ? '을' : '를')
}
```

영문 시험명(GSAT, LEET 등)은 한글이 아니므로 받침 있음으로 처리합니다. 영문 약어는 대부분 자음으로 끝나 이 처리가 자연스럽습니다.

이름이 없는 경우 호명 부분을 제거합니다.

```typescript
function render(template: string, name?: string): string {
  if (name) return template.replace(/\{name\}님/g, `${name}님`)
  return template.replace(/\{name\}님은/g, '')
                 .replace(/\{name\}님/g, '')
                 .replace(/\s+/g, ' ')
                 .trim()
}
```

직무명이 없는 경우 일의 성격 표현으로 대체합니다.

```typescript
const jobPhrase = job || WORK_TYPE_LABEL[workType]
// job이 있으면: "반도체 공정기술"
// 없으면: "분석하고 만드는 일"
```

직무명 뒤에 "직무"를 붙이지 않습니다. "수의사 직무 면접은"이 어색하기 때문입니다.

---

## 4. 만세력 계산 로직

### 4.1 필요 데이터

#### 4.1.1 24절기 시각 테이블

파일 위치 /lib/saju/solarterms.json

범위 1940년 - 2030년 (91년)
항목 연당 절기 12개 (중기 제외)
용량 약 60KB

```json
{
  "2026": {
    "소한": "2026-01-05 23:52",
    "입춘": "2026-02-04 05:02",
    "경칩": "2026-03-06 00:04",
    "청명": "2026-04-05 05:01",
    "입하": "2026-05-05 22:12",
    "망종": "2026-06-06 02:15",
    "소서": "2026-07-07 12:28",
    "입추": "2026-08-07 22:16",
    "백로": "2026-09-08 01:14",
    "한로": "2026-10-08 17:02",
    "입동": "2026-11-07 20:19",
    "대설": "2026-12-07 13:06"
  }
}
```

생성 방법은 파이썬 ephem 또는 skyfield로 태양황경을 계산해 JSON으로 출력합니다. 스크립트는 /scripts/gen-solarterms.py에 두고 1회만 실행합니다. 런타임에는 조회만 합니다.

기업 설립일 계산에도 같은 테이블을 사용하므로 범위를 1940년부터 잡습니다. 그보다 오래된 기업은 년주와 일주만 정확히 나오면 되므로 절기가 없어도 처리 가능하지만, 안전하게 1900년까지 확장해도 파일 크기 부담은 작습니다.

#### 4.1.2 일주 기준일

음력 변환은 필요하지 않습니다. 기준일 하나로 경과일수를 계산합니다.

```
기준일 1900-01-01 = 갑술일 (60갑자 인덱스 10)
일주 인덱스 = ((대상일 - 1900-01-01).days + 10) % 60
```

### 4.2 계산 순서

경도 보정과 서머타임 보정은 **시주 계산에만** 적용합니다. 년주와 월주는 원본 KST 시각으로 절기와 비교하고, 일주는 원본 날짜를 씁니다.

절기 테이블이 KST 기준으로 생성되므로, 출생 시각만 보정해서 비교하면 기준이 섞입니다. 같은 시간 체계끼리 비교해야 합니다.

```typescript
function calculateSaju(birthDate, birthTime, hasBirthTime) {
  // 절기 판정 - 보정 없음
  const yearPillar = getYearPillar(birthDate, birthTime)
  const monthPillar = getMonthPillar(birthDate, birthTime)
  const dayPillar = getDayPillar(birthDate)

  if (!hasBirthTime) return { yearPillar, monthPillar, dayPillar }

  // 시주 - 보정 적용
  const corrected = applyTimeCorrection(birthDate, birthTime)
  const hourPillar = getHourPillar(corrected, dayPillar.stem)

  return { yearPillar, monthPillar, dayPillar, hourPillar }
}
```

#### 1단계: 년주

```
원본 KST 시각으로 입춘과 비교합니다.
입춘 시각 이전이면 전년도로 처리합니다.

  예: 2026년 입춘이 02-04 05:02일 때
      2026-02-04 05:00 출생  전년도인 2025년 (을사)
      2026-02-04 05:05 출생  당년도인 2026년 (병오)

년간 인덱스 = (연도 - 4) % 10
년지 인덱스 = (연도 - 4) % 12
```

#### 2단계: 월주

```
원본 KST 시각으로 절입 구간을 찾습니다.

  입춘 ~ 경칩 = 인월 (지지 인덱스 2)
  경칩 ~ 청명 = 묘월 (3)
  청명 ~ 입하 = 진월 (4)
  입하 ~ 망종 = 사월 (5)
  망종 ~ 소서 = 오월 (6)
  소서 ~ 입추 = 미월 (7)
  입추 ~ 백로 = 신월 (8)
  백로 ~ 한로 = 유월 (9)
  한로 ~ 입동 = 술월 (10)
  입동 ~ 대설 = 해월 (11)
  대설 ~ 소한 = 자월 (0)
  소한 ~ 입춘 = 축월 (1)

월간은 년간에 따른 5패턴 표로 결정합니다.
  갑기년: 병인월부터 시작 (월간 시작 인덱스 2)
  을경년: 무인월부터 (4)
  병신년: 경인월부터 (6)
  정임년: 임인월부터 (8)
  무계년: 갑인월부터 (0)

월간 인덱스 = (시작 인덱스 + 월 순번) % 10
```

#### 3단계: 일주

```
원본 날짜를 씁니다. 보정된 시각으로 날짜가 넘어가지 않습니다.

일주 인덱스 = ((대상일 - 1900-01-01).days + 10) % 60
일간 인덱스 = 일주 인덱스 % 10
일지 인덱스 = 일주 인덱스 % 12
```

00:29 출생이 보정 후 23:59가 되어도 일주는 당일입니다. 4.3.1의 조자시 규칙과 함께 지켜야 합니다.

#### 4단계: 시주

시간 보정을 이 단계에서만 적용합니다.

```
4-1. 시간 보정

한국 표준시는 동경 135도 기준이나 실제 경도는 약 127.5도입니다.
출생 시각에서 30분을 뺍니다.

서머타임 시행 기간에는 추가로 60분을 뺍니다.
  1948-05-31 ~ 1948-09-12
  1949-04-03 ~ 1949-09-10
  1950-04-01 ~ 1950-09-09
  1951-05-06 ~ 1951-09-08
  1955-05-05 ~ 1955-09-08
  1956-05-20 ~ 1956-09-29
  1957-05-05 ~ 1957-09-21
  1958-05-04 ~ 1958-09-20
  1959-05-03 ~ 1959-09-19
  1960-05-01 ~ 1960-09-17
  1987-05-10 ~ 1987-10-11
  1988-05-08 ~ 1988-10-09

4-2. 보정된 시각으로 시지 판정 (2시간 단위 12구간)

  23:00 - 00:59  자 (0)
  01:00 - 02:59  축 (1)
  03:00 - 04:59  인 (2)
  05:00 - 06:59  묘 (3)
  07:00 - 08:59  진 (4)
  09:00 - 10:59  사 (5)
  11:00 - 12:59  오 (6)
  13:00 - 14:59  미 (7)
  15:00 - 16:59  신 (8)
  17:00 - 18:59  유 (9)
  19:00 - 20:59  술 (10)
  21:00 - 22:59  해 (11)

보정 때문에 실질 자시 시작이 23:30이 됩니다. 23:01 출생은
보정 후 22:31이므로 해시입니다. 이것은 의도한 동작입니다.
경도 보정을 도입한 이상 시지 판정에 적용하는 것이 일관됩니다.

4-3. 일간에 따라 시간 결정 (5패턴 표)

  갑기일: 갑자시부터 (시작 인덱스 0)
  을경일: 병자시부터 (2)
  병신일: 무자시부터 (4)
  정임일: 경자시부터 (6)
  무계일: 임자시부터 (8)

시간 인덱스 = (시작 인덱스 + 시지 인덱스) % 10
```

태어난 시간을 모른다고 선택한 경우 4단계를 건너뜁니다. 기업 설립일 계산도 시각이 없으므로 건너뜁니다.

### 4.3 예외 처리 규칙

#### 4.3.1 자시 처리

23:00 - 23:59 출생은 당일 유지로 통일합니다(조자시 방식). 야자시 방식과 결과가 다르지만 일관성만 지키면 됩니다.

경도 보정과 함께 적용될 때 주의할 점이 있습니다. 00:29 출생은 보정 후 23:59가 되는데, 이때 일주가 전날로 넘어가면 안 됩니다. 일주는 4.2의 3단계에서 원본 날짜로 계산하므로 이 문제가 발생하지 않아야 합니다.

이 규칙을 코드 주석에 명시합니다.

#### 4.3.2 입춘 경계

원본 KST 시각으로 시각 단위까지 비교합니다. 경도 보정을 적용하지 않습니다.

절기 테이블이 KST 기준으로 생성되므로, 출생 시각만 보정해서 비교하면 기준이 어긋납니다. 두 값을 같은 체계로 두어야 경계가 정확해집니다.

```
2026년 입춘 05:02 (KST)
  2026-02-04 05:00 출생  전년도
  2026-02-04 05:05 출생  당년도
```

#### 4.3.3 태어난 시간 모름

시주를 계산하지 않고 3기둥으로 진행합니다. 오행 점수 계산에서 시간, 시지 가중치를 제외합니다. 강한 오행과 약한 오행은 정상적으로 산출됩니다.

결과 화면 하단에 안내 문구와 재입력 버튼을 노출합니다.

```
태어난 시간을 입력하시면 더 정확한 결과를 볼 수 있습니다.
[시간 입력하기]
```

실제로 출생 시간을 모르는 사용자가 30-40퍼센트에 이르므로, 이 처리가 없으면 그 비율만큼 이탈합니다.

### 4.4 기업 사주 계산

면접 리포트의 궁합 계산에 사용합니다. 법인 설립일 기준이며 시각 정보가 없으므로 3기둥(년주, 월주, 일주)만 계산합니다. 4.2의 1-3단계를 그대로 재사용합니다.

```typescript
function getCompanySaju(foundedDate: Date): CompanySaju {
  return {
    yearPillar: getYearPillar(foundedDate),
    monthPillar: getMonthPillar(foundedDate),
    dayPillar: getDayPillar(foundedDate),
    dayStem: getDayStem(foundedDate),
    elements: getElementDistribution3(foundedDate)
  }
}
```

설립일 기준 규칙:

1. 법인 설립일을 사용합니다
2. 창립일과 법인 설립일이 다르면 법인 설립일을 사용합니다
3. 외국계 한국법인은 한국법인 설립일을 사용합니다
4. 공공기관은 설립 근거 법률 시행일 또는 법인 설립일을 사용합니다
5. 확인되지 않으면 궁합 섹션을 생략합니다. 추측 날짜로 계산하지 않습니다

---

## 5. 오행 산출

### 5.1 천간 지지 오행 매핑

```typescript
const STEM_ELEMENT = ['목','목','화','화','토','토','금','금','수','수']
// 갑 을 병 정 무 기 경 신 임 계

const BRANCH_ELEMENT = ['수','토','목','목','토','화','화','토','금','금','토','수']
// 자 축 인 묘 진 사 오 미 신 유 술 해
```

### 5.2 가중치

```
일간   3   사주에서 본인을 뜻하는 자리
월지   3   계절을 결정하는 자리
년간   1
년지   1
월간   1
일지   1
시간   1
시지   1
```

태어난 시간을 모르는 경우 시간과 시지를 제외하고 계산합니다. 기업 사주도 동일합니다.

### 5.3 강한 오행 / 약한 오행

```
5개 오행의 점수를 합산합니다.
최고점 오행 = 강한 오행
최저점 오행 = 약한 오행

동점 처리
  강한 오행 동점: 일간 오행 우선
  약한 오행 동점: 강한 오행이 극하는 오행 우선
```

### 5.4 오행 파생 값

```
행운의 숫자 (약한 오행 기준)
  목  3, 8
  화  2, 7
  토  5, 10
  금  4, 9
  수  1, 6
  화면 표시는 첫 번째 숫자만

행운의 색 (약한 오행 기준)
  목  청색, 초록
  화  적색
  토  황색, 베이지
  금  흰색, 은색
  수  검정, 남색

방위 (약한 오행 기준)
  목  동
  화  남
  토  중앙
  금  서
  수  북

유리한 시간대 (약한 오행 기준)
  목  인묘시 (03-07)
  화  사오시 (09-13)
  토  진술축미시
  금  신유시 (15-19)
  수  자해시 (21-01)
```

### 5.5 오행 상생 상극

```typescript
// 상생: 목생화, 화생토, 토생금, 금생수, 수생목
const GENERATES = { 목:'화', 화:'토', 토:'금', 금:'수', 수:'목' }

// 상극: 목극토, 토극수, 수극화, 화극금, 금극목
const OVERCOMES = { 목:'토', 토:'수', 수:'화', 화:'금', 금:'목' }
```

관계 판정 함수:

```typescript
type Relation = '상생' | '비화' | '상극' | '설기' | '아극'

function getRelation(mine: Element, other: Element): Relation {
  if (mine === other) return '비화'
  if (GENERATES[other] === mine) return '상생'   // 상대가 나를 생함
  if (OVERCOMES[other] === mine) return '상극'   // 상대가 나를 극함
  if (GENERATES[mine] === other) return '설기'   // 내가 상대를 생함
  return '아극'                                   // 내가 상대를 극함
}
```

이 함수는 시험 당일 운, 오늘의 운, 시작 시간 궁합, 기업 궁합에 모두 재사용됩니다.

### 5.6 십신 계산

유료 리포트 섹션 2와 마지막 섹션에 사용합니다. 십신은 일간과 나머지 글자의 관계이며, 5.5의 관계 판정을 오행 단위로 적용한 것입니다.

```typescript
type Shipsin = '비겁' | '식상' | '재성' | '관성' | '인성'

function getShipsin(dayElement: Element, target: Element): Shipsin {
  if (target === dayElement) return '비겁'           // 같은 오행
  if (GENERATES[dayElement] === target) return '식상' // 내가 생하는
  if (OVERCOMES[dayElement] === target) return '재성' // 내가 극하는
  if (OVERCOMES[target] === dayElement) return '관성' // 나를 극하는
  return '인성'                                       // 나를 생하는
}
```

각 십신의 의미와 시험 맥락에서의 해석입니다.

| 십신 | 관계 | 시험 맥락 |
|---|---|---|
| 비겁 | 같은 오행 | 경쟁, 자기 주장, 고집 |
| 식상 | 내가 생함 | 표현, 전달, 말하기 |
| 재성 | 내가 극함 | 결과, 성과, 실리 |
| 관성 | 나를 극함 | 규율, 평가, 시험 자체 |
| 인성 | 나를 생함 | 학습, 흡수, 이해 |

시험 서비스에서 중요한 것은 관성(평가받는 자리), 식상(표현), 인성(학습)입니다.

```typescript
function getShipsinDistribution(saju: Saju): Record<Shipsin, number> {
  const dayEl = STEM_ELEMENT[saju.dayStem]
  const dist = { 비겁: 0, 식상: 0, 재성: 0, 관성: 0, 인성: 0 }

  for (const [element, score] of Object.entries(saju.elements)) {
    const s = getShipsin(dayEl, element as Element)
    dist[s] += score
  }
  return dist
}
```

5.2의 가중치를 그대로 사용하므로 오행 분포에서 바로 환산됩니다.

프롬프트 재료에 십신 분포와 천간·지지 위치를 함께 넣습니다. 천간에 있는지 지지에만 있는지에 따라 해석이 달라지기 때문입니다.

```
관성이 천간에 있음    겉으로 드러나는 규율을 따름
관성이 지지에만 있음   스스로 정한 기준으로 움직임
관성이 없음           외부 평가에 무관심, 준비 방식이 자유로움
관성이 과다           평가에 위축, 압박에 약함
```

paid-fragments.json의 shipsinByDayStem 조각 10개가 일간별 관성 해석을 담고 있습니다. 이것을 먼저 두고 AI가 실제 분포를 반영해 확장합니다.

---

## 6. 운 지수 계산

### 6.1 시험 당일 운 지수

```
기본 점수 50

[1] 시험일의 일주를 계산합니다 (4.2의 3단계 재사용)

[2] 사용자 일간 오행 vs 시험일 일간 오행 관계
      상생  +30
      비화  +15
      아극   +5
      설기  -10
      상극  -20

[3] 시험일 일지 오행이 사용자의 약한 오행과 같으면
      +20

[4] 사용자 강한 오행과 시험 방식의 궁합 (6.3 참조)
      궁합 점수를 20으로 나눈 값을 가산

[5] 0-100 사이로 클램프
```

### 6.2 오늘의 운

6.1과 동일한 로직에서 대상 날짜만 오늘로 바꿉니다. 매일 값이 변경되어 재방문 요인이 됩니다. 로그인 사용자는 랜딩에서 입력 없이 바로 확인할 수 있습니다.

### 6.3 시험 방식 궁합 점수

카드 7에 사용합니다.

```
강한 오행별 방식 적합도 (0-100)

         객관식필기  서술논술   면접    실기
목          70        75       72      68
화          74        58       81      62
토          82        70       65      78
금          85        80       70      75
수          72        85       78      60
```

각 오행의 성질에서 도출한 값입니다. 금은 정리와 판별에 강해 객관식에 유리하고, 수는 이해와 응용에 강해 논술에 유리하며, 화는 표현력이 있어 면접에 유리하고, 토는 반복 숙련이 필요한 실기에 유리합니다.

이 카드는 여러 시험을 준비하는 사용자에게 전략 판단 재료가 됩니다. 면접 준비생이 자신의 면접 적합도를 확인하는 심리적 효과도 큽니다.

### 6.4 7일 기운 흐름

시험일로부터 역산하여 7일간 각 날짜의 기운 지수를 계산합니다. 6.1의 [1][2][3] 단계만 사용합니다(방식 궁합 제외).

```typescript
function getWeekFlow(saju: Saju, examDate: Date): DayFlow[] {
  const result = []
  for (let i = 7; i >= 0; i--) {
    const target = subDays(examDate, i)
    const pillar = getDayPillar(target)
    const score = calcDayScore(saju, pillar)
    result.push({
      date: target,
      dday: i,
      score,
      label: getFlowLabel(score)
    })
  }
  return result
}
```

날짜별 라벨은 점수 구간에 따라 6개 중 하나를 사용합니다.

```
80 이상   집중이 붙는 날
65-79     흐름이 좋은 날
50-64     평소대로 가는 날
35-49     피로가 몰리는 날
20-34     쉬어가야 하는 날
20 미만   무리하면 안 되는 날
```

### 6.5 시작 시간 궁합

카드 8에 사용합니다.

```
[1] 시작 시각을 12지지 구간으로 변환 (4.2의 4단계 시지 표 재사용)
[2] 해당 지지의 오행 확인
[3] 사용자 강한 오행과의 관계 판정 (5.5)
[4] 관계 5종 × 방식 3종 = 15개 조각 중 선택
```

시작 시간을 모른다고 선택한 경우 이 카드를 표시하지 않고 안내 문구로 대체합니다.

```
시험 시간이 나오면 다시 입력해보세요.
시간대에 따라 유리한 조건이 달라집니다.
```

면접은 개인별로 시간을 통보받으므로 이 안내가 재방문 요인이 됩니다.

### 6.6 월별 시험운 (유료 전용)

유료 리포트의 연간 캘린더에 사용합니다.

```
[1] 각 월의 월지를 절기 기준으로 확인
[2] 월지 오행과 사용자 일간 오행의 관계 판정
[3] 관계별 점수를 5단계 막대로 표시
```

여러 시험을 준비하는 사용자에게 일정 조정 판단 재료가 됩니다.

### 6.7 기업 궁합 점수 (유료 면접 전용)

```
기본 50

[1] 사용자 일간 오행 vs 기업 일간 오행 관계
      상생  +30
      비화  +15
      아극  +10
      설기   -5
      상극  -15

[2] 기업 3기둥 오행 분포에서 사용자의 약한 오행이
    가장 많은 비중이면 +20

[3] 사용자 강한 오행과 기업 강한 오행이 같으면 +10

[4] 0-100 사이로 클램프
```

---

## 7. 캐릭터 및 유형 시스템

### 7.1 구조

캐릭터 이름은 "합격이"입니다. 화면에서 사용자에게 말을 거는 주체이며, 말풍선과 로딩 문구에서 이 이름을 사용합니다. 확정된 디자인 사양은 21.5를 참조합니다.

캐릭터와 유형을 분리합니다.

| 요소 | 결정 기준 | 의미 | 에셋 |
|---|---|---|---|
| 캐릭터 | 시험 당일 운 지수 | 그날의 상태 | 이미지 5개 (표정 변화) |
| 유형 뱃지 | 강한 오행 | 사용자 고유 정체성 | 아이콘 + 색상 5종 |

캐릭터는 1종이며 표정만 5단계로 바뀝니다. 오행별로 캐릭터를 나누지 않습니다. 유형 뱃지가 캐릭터 옆에 나란히 배치되어 개인화를 담당합니다.

### 7.2 캐릭터 표정 5단계

| 단계 | 운 지수 | 표정 | 파일명 |
|---|---|---|---|
| 5 | 80-100 | 초승달 눈, 만세, 반짝임 | char-05.png |
| 4 | 65-79 | 미소 | char-04.png |
| 3 | 50-64 | 기본, 차분 | char-03.png |
| 2 | 35-49 | 살짝 걱정, 처진 눈, 땀방울 | char-02.png |
| 1 | 0-34 | 결의에 찬 눈, 주먹 쥔 파이팅 자세 | char-01.png |

1단계를 우는 표정으로 만들지 않습니다. 낮은 점수를 받은 사용자가 슬픈 캐릭터를 보면 공유하지 않고 서비스 인상도 나빠집니다. 주먹을 쥐고 응원하거나 손을 내미는 정도로 제작합니다. "힘내자"로 읽혀야 하며 "망했다"로 읽히면 안 됩니다.

### 7.3 유형 뱃지 5종

강한 오행으로 결정합니다. 전부 강점 이름이므로 어느 유형을 받아도 부정적으로 읽히지 않습니다.

| 오행 | 아이콘 | 유형명 | 색상 | 성격 |
|---|---|---|---|---|
| 목 | 새싹 | 흡수형 | #4CAF7D | 새 내용 습득이 빠름 |
| 화 | 불꽃 | 몰입형 | #E5533D | 붙으면 끝까지 밀어붙임 |
| 토 | 산 | 지구력형 | #C9A227 | 꾸준함, 계획 준수 |
| 금 | 칼날 | 정리형 | #8E9AAF | 요약과 원칙에 강함 |
| 수 | 물방울 | 응용형 | #3D5AE5 | 이해와 응용에 강함 |

아이콘은 1차에 이모지로 시작하고 이후 SVG로 교체합니다.

### 7.4 뱃지 디자인

```
┌─────────────┐
│    아이콘    │
│             │
│    몰입형    │
│             │
│    화(火)    │
└─────────────┘

배경    오행 색상 10퍼센트 투명도
테두리  오행 색상 1px
텍스트  오행 색상
모서리  20px
```

오행 색상은 뱃지와 공유 이미지 배경에만 사용합니다. 나머지 화면은 브랜드 색상으로 통일합니다.

### 7.5 뱃지 탭 동작

뱃지를 누르면 유형 설명 모달이 열립니다.

```
┌────────────────────────┐
│        아이콘           │
│       몰입형            │
│                        │
│ 화(火) 기운이 강한 유형   │
│                        │
│ 몰입이 붙으면 끝까지     │
│ 밀어붙이는 힘이 있어요.  │
│ 대신 한 번 흐트러지면    │
│ 회복이 더딘 편입니다.    │
│                        │
│ 전체 이용자 중 21%       │
│                        │
│ [내 유형 공유하기]       │
└────────────────────────┘
```

"전체 이용자 중 21%" 항목은 데이터가 충분히 쌓인 후 활성화합니다. 초기에는 숨깁니다. 유형에 소속감이 생기면 공유 동기가 됩니다.

### 7.6 상단 레이아웃

```
┌──────────────────────────────────┐
│  안녕하세요, 김민준님              │
│  2026년 8월 22일                  │
│                                  │
│  ┌────────────────────────────┐  │
│  │  ┌──────────────────┐      │  │
│  │  │ 시험까지 15일     │      │  │
│  │  │ 남았어요!         │      │  │
│  │  └──────────────────┘      │  │
│  │                            │  │
│  │    [캐릭터]      ┌───────┐ │  │
│  │                 │ 아이콘 │ │  │
│  │                 │ 몰입형 │ │  │
│  │                 │ 화(火) │ │  │
│  │                 └───────┘ │  │
│  │                            │  │
│  │  ┌──────────┬───────────┐ │  │
│  │  │ 당일 운   │  오늘의 운 │ │  │
│  │  │   78     │    64     │ │  │
│  │  └──────────┴───────────┘ │  │
│  └────────────────────────────┘  │
│                                  │
│  카드 1 시작                      │
└──────────────────────────────────┘
```

캐릭터는 왼쪽, 뱃지는 오른쪽에 배치합니다. 말풍선은 캐릭터 상단에 CSS로 겹칩니다. 말풍선 문구가 D-day에 따라 5종으로 바뀌므로 이미지에 넣지 않습니다.
---

## 8. 유료 리포트

### 8.1 가격 및 상품 정의

- 가격 3,900원 일회성
- 결제 후 계정에 영구 저장, 재열람 무제한
- 인쇄 기능과 이미지 저장 기능은 제공하지 않습니다

가격 근거: 990원대 범용 사주와 경쟁하지 않기 위해, 상품 인식을 운세가 아니라 시험 준비 도구로 위치시킵니다. 대체재는 합격 부적(5,000-30,000원), 시험용 필기구 세트(10,000-20,000원)입니다. 동시에 4,000원 아래로 두어 소액 결제 인식을 유지합니다.

### 8.2 상품 2종

| 상품 | 대상 | 섹션 수 | 검색 |
|---|---|---|---|
| 필기 리포트 | 방식이 필기인 사용자 | 14 | 0회 |
| 면접 리포트 | 방식이 면접인 사용자 | 15 | 2회 |

실기와 오디션은 1차 출시에서 유료 상품을 제공하지 않습니다. 종류가 다양해 일반화가 어렵고 사용자 비중이 작습니다. 무료 결과까지만 제공하고, CTA 자리에는 안내 문구를 노출합니다.

```
실기와 오디션 상세 리포트는 준비 중입니다.
[알림 받기]
```

무료 결과는 네 방식 모두 동일하게 제공됩니다. 오디션 전용 문장 조각이 fragments.json에 이미 포함되어 있으므로 카드 8개가 정상적으로 나옵니다.

19장의 방식별 분포를 보고 실기와 오디션 유료 상품 개발 여부를 판단합니다.

### 8.3 필기 리포트 구성

```
표지
  이름 / 시험명 / 시험일 / D-day
  당일 운 지수 · 오늘의 운 지수 · 잠재력 발휘 지수
  유형 뱃지

1.  내 사주 명식과 오행 분포        계산 + AI     180~300자
2.  내 사주가 말하는 시험 패턴       조각 + AI     350~450자
3.  나의 학습 유형 진단             AI           350~450자
4.  시험 당일 시간대별 운용          계산 + AI     500~650자
5.  시험 전 7일 데일리 플랜          계산 + AI     600~800자
6.  놓치기 쉬운 3가지               AI           300~400자
7.  시험 전날 상세 타임라인          AI           300~400자
8.  남은 기간 어떻게 쓸까            AI           300~400자
9.  좌석과 방위                     계산 + AI     200~280자
10. 나의 행운 색과 방위             계산 + AI     250~330자
11. 이 기간 피해야 할 것            AI           200~280자
12. 2026년 시험운 캘린더            계산 + AI     180~250자
13. 시험 이후와 다음 기회           AI           280~360자
14. 내 사주로 본 시험 전략          조각 + AI     300~550자

합계 4,000~5,000자
```

#### 범위를 어떻게 읽어야 하나

**범위는 목표치이며 상한은 절대 기준이 아닙니다.** 같은 입력에서도 회차마다 편차가 있어 상한을 넘는 섹션이 나옵니다. 150퍼센트 안이면 정상으로 봅니다.

실측에서 이 폭이 얼마나 큰지 확인했습니다. 같은 입력, 같은 설정입니다.

```
기업과 나의 궁합   376  487  602      (범위 380~480)
내 사주로 본 전략  397  537  636      (범위 300~550)
```

프롬프트로 좁힐 수 있는 폭이 아닙니다. 그래서 상한 초과를 실패로 돌리지 않고 로그만 남깁니다. 이미 쓴 원가를 버리고 다시 쓰면 손해가 두 배입니다.

**하한 미달은 다릅니다.** 얇은 섹션은 사용자가 즉시 알아챕니다. 합계가 하한을 넘겨도 섹션별 편차가 크면 어떤 부분은 얇게 읽힙니다. 계산값을 정확히 인용해도 두 문장으로 끝내면 3,900원짜리로 읽히지 않습니다.

줄이려 노력하되 0개를 보장할 수는 없습니다. 현재 기준은 아래와 같습니다.

| 항목 | 기준 |
|---|---|
| 하한 미달 | 필기 0개 유지, 면접은 회당 2개 이하면 정상 |
| 상한 초과 | 150퍼센트 안이면 정상, 목표 없음 |
| 소요 | 필기 180초, 면접 200초 이내 |
| 원가 | 건당 300원 이내 |

분량에 하한과 상한을 함께 둡니다. 상한이 없으면 목표의 130퍼센트를 생성해 시간과 원가가 크게 늘어납니다.

모바일에서 읽는 서비스이므로 분량보다 밀도가 중요합니다. 9,000자는 A4 6장 분량이라 끝까지 읽는 사용자가 거의 없습니다.

섹션 1과 마지막 섹션은 상한이 다릅니다. 섹션 1은 300자입니다. 250자로 뒀을 때 7회 중 6회가 넘겼습니다(248~302). 네 기둥의 뜻과 오행 격차 해석을 250자에 넣기 어렵습니다.

마지막 섹션은 상한이 550자입니다. 실측 7건 중 6건이 400자를 넘겨 424~567자가 나왔습니다. 이 섹션은 `paid-fragments.json`의 조각(250자 내외)이 맨 앞에 그대로 실리고 AI가 그 뒤를 확장하는 구조라, 조각과 확장분을 합쳐 400자에 넣기가 어렵습니다. 프롬프트를 조여 잘라내는 것보다 범위를 현실에 맞추는 편이 낫습니다.

#### 분량은 글자 수가 아니라 구성으로 지시합니다

프롬프트에서 모델에게 글자 수를 세라고 지시하지 않습니다. 모델은 자기 출력의 글자 수를 정확히 셀 수 없어, 추정과 재추정이 반복되며 thinking 토큰이 크게 늘어납니다.

실측에서 확인했습니다. 자가 점검을 지시한 상태로 같은 입력을 두 번 돌렸더니 출력이 17,708 토큰과 28,823 토큰으로 갈렸고, 본문은 오히려 토큰을 더 쓴 쪽이 짧았습니다(4,859자 대 4,445자). 늘어난 토큰이 본문으로 가지 않았다는 뜻입니다.

그렇다고 범위만 주면 지시가 되지 못합니다. 셀 수 없는 것을 지시했기 때문입니다. 실측 8건에서 필기 14섹션 중 7섹션이 3회 모두 하한 미달이었습니다.

```
시험 당일 시간대별 운용   500~650  →  387  431  446
시험 전 7일 데일리 플랜   600~800  →  491  515  539
시험 전날 상세 타임라인   300~400  →  234  235  244
좌석과 방위              200~280  →  155  170  190
```

그래서 섹션마다 **구성**을 함께 지시합니다. 문단이 몇 개인지, 항목이 몇 개인지, 각 문단에 무엇이 들어가야 하는지는 모델이 정확히 지킵니다.

```
현재  "350~450자"
변경  구성: 문단 3개. 관성 / 식상 / 인성 순서로 한 문단씩.
           각 문단은 그 십신이 몇 점이고 천간인지 지지인지 없는지를
           먼저 밝히고, 그것이 시험 준비와 시험장에서 어떻게 나타나는지,
           그래서 무엇을 하면 되는지까지 씁니다.
      분량: 350~450자 (구성을 지키면 대체로 이 안에 들어옵니다)
```

프롬프트에서 구성이 글자 수보다 먼저 나옵니다. 구현은 `lib/ai/spec.ts`의 `SectionSpec.structure`입니다.

**brief 안에 글자 수를 적지 않습니다.** 이전에는 구성 지시와 범위가 서로 어긋났습니다. 7일 플랜은 "하루당 150자 이상"인데 8일이면 1,200자로 상한 800자를 넘고, 조심할 3가지는 "한 가지당 160자"인데 상한이 400자, 질문 유형은 "유형당 250자"인데 상한이 700자였습니다. 모순된 숫자를 주면 숫자 전체가 무시됩니다.

#### 분량 검증은 서버가 담당합니다

생성 결과의 글자 수는 서버가 세어 하한의 70퍼센트 미만이면 실패로 돌리고, 상한의 150퍼센트를 넘으면 경고 로그를 남깁니다.

**섹션별 글자 수도 남깁니다.** 합계가 하한의 98퍼센트여도 섹션 절반이 하한을 밑돌면 사용자는 어떤 부분을 얇게 느낍니다. 합계만 봐서는 그것이 보이지 않습니다.

- 로그 — 생성마다 `[분량] 필기 합계 4,213자 / 하한 4,290자 (98%) · 미달 8개 초과 1개 · seat 155/200↓ ...`
- `reports.content.lengths` — 섹션별 글자 수, 미달·초과 목록, 그때 쓴 effort

어느 섹션이 반복해서 미달인지 운영 중에도 볼 수 있어야 프롬프트를 고칠 수 있습니다.

### 8.4 면접 리포트 구성

```
표지
  이름 / 기업명 / 직무 / 면접일 / D-day
  당일 운 지수 · 오늘의 운 지수 · 잠재력 발휘 지수
  기업 궁합 점수 · 유형 뱃지

1.  내 사주 명식과 오행 분포        계산 + AI     180~300자
2.  내 사주가 말하는 면접 패턴       조각 + AI     350~450자
3.  나의 답변 유형 진단             AI           380~480자
4.  면접 당일 시간대별 운용          계산 + AI     500~650자
5.  이 기업은 어떤 곳인가           AI + 검색     280~380자
6.  기업과 나의 궁합                계산 + 조각 + AI 380~480자
7.  이 직무와 나                    AI           380~480자
8.  들어올 가능성이 높은 질문         AI           550~700자
9.  내가 조심해야 할 3가지          AI           300~400자
10. 나의 행운 색과 방위             계산 + AI     250~330자
11. 복장과 소지품                   AI           250~330자
12. 면접 전날 밤                    AI           300~400자
13. 2026년 면접운 캘린더            계산 + AI     180~250자
14. 면접 이후와 다음 기회           AI           280~360자
15. 내 사주로 본 면접 전략          조각 + AI     300~550자

합계 4,500~5,500자
```

면접에는 7일 데일리 플랜이 없습니다. 면접이 하루 이벤트라 준비 기간의 날짜별 흐름이 필기만큼 중요하지 않고, 대신 기업과 직무에 분량을 씁니다.

### 8.5 모든 섹션에 사주 근거를 둔다

각 섹션의 조언은 계산 결과에 근거를 두어야 합니다. 근거 없이 일반론을 쓰면 다른 사주 서비스와 구분되지 않습니다.

| 섹션 | 근거로 쓸 계산값 |
|---|---|
| 1 | 8글자 오행 분포, 일간 |
| 2 | 십신 관계 (관성, 식상, 비겁) |
| 3 | 강한 오행 + 약한 오행 |
| 4 | 12지지 오행 × 사용자 오행 |
| 5 | 일별 일진 × 사용자 일간 |
| 6 | 약한 오행이 만드는 취약점 |
| 7 | 강한 오행의 야간 특성 |
| 8 | 약한 오행 보완 순서 |
| 9 | 약한 오행 방위 |
| 10 | 약한 오행 색상, 방위, 시간대 |
| 11 | 강한 오행을 더 키우는 요소 |
| 12 | 월지 오행 × 일간 |
| 13 | 방식 궁합 + 월별 흐름 |
| 14 | 십신 관계에서 나오는 반복 패턴 |

면접의 5, 6, 7, 8번은 기업 사주와 직무 성격을 추가 재료로 씁니다.

프롬프트에 이 매핑을 명시합니다.

```
조언의 근거를 사주 계산 결과에서 가져옵니다.
오행의 강약, 명식의 글자, 십신 관계 중 그 섹션에
자연스러운 것을 씁니다. 수치를 강제하지 않습니다.

단, 재료에 없는 오행 수치나 관계를 만들지 마라.
```

수치 언급을 강제하지 않는 이유가 있습니다.

처음에는 `"화가 8인데 조절할 수가 3뿐입니다" 처럼 실제 수치를 언급하라`고 지시했습니다. 근거 없는 일반론을 막으려는 의도였는데, 문장이 뻑뻑해집니다. 모든 섹션에서 점수를 끼워 넣으려다 보면 같은 숫자가 리포트 전체에 반복되고 읽기가 나빠집니다.

`"화 기운이 강한 김민준님은"` 정도면 근거가 드러납니다. 실측에서도 수치를 인용하지 않는 섹션이 회당 1~3개 나왔는데, 그 섹션들의 내용 자체는 계산값과 어긋나지 않았습니다. 지킬 수 없는 규칙을 두는 것보다 지킬 수 있는 규칙을 두는 편이 낫습니다.

지어내기 금지는 그대로입니다. 재료에 없는 수치나 관계를 만드는 것은 근거를 흐리는 것이 아니라 거짓말입니다.

### 8.6 시간대별 운용은 12지지로 구성한다

섹션 4는 하루를 12지지로 나누어 각 시간대의 오행과 사용자 오행의 관계를 해석합니다.

```
23:00-00:59  자시(子時)  수
01:00-02:59  축시(丑時)  토
03:00-04:59  인시(寅時)  목
05:00-06:59  묘시(卯時)  목
07:00-08:59  진시(辰時)  토
09:00-10:59  사시(巳時)  화
11:00-12:59  오시(午時)  화
13:00-14:59  미시(未時)  토
15:00-16:59  신시(申時)  금
17:00-18:59  유시(酉時)  금
19:00-20:59  술시(戌時)  토
21:00-22:59  해시(亥時)  수
```

시험 시작 시각을 기준으로 기상부터 종료까지의 구간을 다룹니다. 종료 시각은 받지 않으므로 시작 이후는 상대 표현을 씁니다.

```
시작 직후 20분
시작 후 40분 지점
후반
마지막 10분
```

면접은 진행 시간을 알 수 없으므로 대기 시간 관리에 무게를 둡니다.

### 8.7 잠재력 발휘 지수

당일 운 지수와 다른 축입니다. 그날 조건에서 평소 실력의 몇 퍼센트가 나오는지를 나타냅니다.

```
기본 100

시험일 일진 관계
  상생 +10 / 비화 +5 / 아극 +3 / 설기 -5 / 상극 -10

시작 시간 궁합
  상생 +8 / 비화 +4 / 아극 +2 / 설기 -3 / 상극 -8

방식 궁합
  (해당 방식 궁합점수 - 70) / 5

70~120 클램프
```

두 지수가 다르게 나오면 해석이 명확해집니다.

```
당일 운 62 · 발휘 지수 108
  흐름이 밀어주는 날은 아니지만 준비한 것은 잘 나오는 조건

당일 운 85 · 발휘 지수 92
  기운은 좋으나 집중이 흩어지기 쉬운 조건
```

무료에서는 잠금으로 표시하고 숫자를 가립니다. 유료 전용 지표입니다.

### 8.8 D-day 구간별 구성 변화

시험 하루 전이나 당일에 결제하는 사용자가 있습니다. 7일 플랜이 전제인 구성을 그대로 두면 성립하지 않습니다.

| 구간 | 섹션 5 | 섹션 8 |
|---|---|---|
| D-8 이상 | 시험 전 7일 데일리 플랜 | 남은 기간 어떻게 쓸까 |
| D-2 ~ D-7 | 남은 날짜 데일리 플랜 | 지금 무엇을 버릴까 |
| D-1 | 오늘 밤부터 내일 입실까지 | 오늘 밤 시간표 |
| D-DAY | 지금부터 시험 종료까지 | 섹션 5에 통합 |

#### D-1 구성

섹션 5가 날짜별에서 시간 단위로 바뀝니다.

```
오늘 (D-1) 기운 지수
  18:00 이후 무엇을 할지
  22:00 책을 덮어야 하는 이유
  23:00 취침

내일 (D-DAY) 기운 지수
  기상, 아침, 이동, 입실
```

#### D-DAY 구성

당일 아침 결제자는 읽을 시간이 없습니다. 긴 글을 앞에 두지 않습니다.

```
[최상단] 지금 바로 할 3가지     3문장, 신규
1.  지금부터 시험 종료까지      결제 시각 기준
2.  놓치기 쉬운 3가지
3.  좌석과 방위
4.  나의 행운 색과 방위
5.  내 사주 명식과 오행 분포
6.  내 사주가 말하는 시험 패턴
7.  2026년 시험운 캘린더
8.  시험 이후와 다음 기회
```

"지금부터"를 만들려면 결제 시각과 시험 시작 시각의 차이를 계산해야 합니다. 프롬프트 재료에 현재 시각과 남은 시간을 넣습니다.

시험이 이미 시작했거나 끝난 경우도 처리합니다. 이 경우 시험 이후 섹션 중심으로 구성하고 톤을 전환합니다.

#### 구현

구간 판정과 구성 선택은 코드가 합니다. AI가 D-day를 보고 판단하게 두면 구성이 흔들립니다.

```typescript
function getReportSpec(dday: number, type: ExamType): ReportSpec {
  if (dday >= 8) return SPEC_NORMAL[type]
  if (dday >= 2) return SPEC_SHORT[type]
  if (dday === 1) return SPEC_EVE[type]
  return SPEC_DDAY[type]
}
```

결제 화면에 미리 안내합니다.

```
시험이 내일이네요.
7일 플랜 대신 오늘 밤과 내일 아침에 집중한
내용으로 만들어 드립니다.
```

### 8.9 기업 궁합 (면접 전용)

#### 계산

6.7의 로직으로 점수를 산출하고, 5.5의 관계 판정으로 5종 중 하나를 정합니다.

#### 문장 구성

관계별 기본 해석은 미리 작성한 조각을 사용하고, 기업 정보와 결합한 확장 해석만 AI가 씁니다.

미리 쓰는 이유는 일관성입니다. AI에 전부 맡기면 같은 상극 관계인데 어떤 사용자에게는 위험하다고, 다른 사용자에게는 괜찮다고 나올 수 있습니다. 사주 해석의 일관성이 무너집니다.

```
[코드 계산]
  기업 3기둥, 일간, 오행 분포
  관계 판정, 궁합 점수
     |
[미리 쓴 조각]
  관계별 기본 해석 5개
     |
[AI 생성]
  기업 정보와 결합한 확장 해석
  구체적 면접 대응
```

#### 출력 예시

```
## 4. 삼성전자와 김민준님의 궁합

궁합 점수  72

삼성전자 설립  1969년 1월 13일 (법인 설립 기준)
기업 일간      경금(庚金)
김민준님 일간   병화(丙火)
관계          화극금

[조각 - 아극 관계]
내가 상대를 극하는 관계입니다. 사주에서 이 자리는 주도권을
잡기 쉬운 위치를 뜻합니다. 면접에서는 이 회사 앞에서
위축되지 않는다는 의미로 나타납니다.

다만 극하는 관계는 부딪히는 관계이기도 합니다. 압박 질문에
정면으로 반박하면 충돌로 읽힙니다. 한 박자 쉬고 답하는
연습이 필요합니다.

[AI 생성 - 기업 맥락]
경금은 단단하고 정제된 기운입니다. 삼성전자는 1969년 설립
이래 제조 기반의 정밀함을 축적해온 회사라, 기업의 기운과
사업 성격이 어긋나지 않습니다.

김민준님의 병화는 확장하고 밀어붙이는 기운입니다. 정제된
조직 안에서 이 기운은 두 가지로 작용합니다. 새로운 시도를
제안하는 쪽에서는 강점이 되고, 정해진 절차를 지키는
쪽에서는 마찰이 생깁니다.
```

#### 설립일 미확인 시 대체

궁합 섹션을 생략하고 대체 섹션을 넣습니다. 자리를 비워두면 리포트가 얇아집니다.

```
[제목 변경]
4. 이 조직에서 김민준님의 위치

[내용]
사용자 사주와 일의 성격, 직무명만으로 작성
강한 오행 5종 조각 + AI 확장
```

사용자에게 "정보를 찾지 못했습니다"를 크게 표시하지 않습니다. 제목만 다르고 분량은 같습니다.

### 8.10 예상 질문 생성 (면접 전용)

검색으로 기출 질문을 가져오지 않습니다. 세 가지 이유입니다.

첫째, 면접 후기는 신뢰도가 낮습니다. 오래됐거나 다른 직군 내용인 경우가 많습니다.
둘째, 기출 질문을 옮기면 저작권 문제가 있습니다.
셋째, 사주 서비스가 사주로 답하는 것이 정체성에 맞습니다.

#### 논리 구조

```
내 사주
  |
내 성향
  |
그 성향에서 약한 지점
  |
면접관이 파고들 가능성이 높은 지점
  |
그 지점을 확인하는 질문 유형
  |
나에게 맞는 대응 방식
```

사주에서 질문을 도출하는 것이 아니라, 사주가 알려주는 약점에서 질문을 역산합니다.

#### 출력 형식

질문 원문이 아니라 유형과 대응 전략을 제시합니다.

```
### 유형 1. 지속성 확인

강한 인상을 주는 지원자에게 면접관이 먼저 갖는 의문은
"오래 갈까"입니다.

들어올 수 있는 형태
- 힘들었던 일을 어떻게 견뎠는지
- 오래 지속한 활동이 있는지
- 성과가 안 나올 때 어떻게 했는지

김민준님의 대응
짧고 강한 사례보다 길게 이어간 사례를 준비하세요.
"열정적으로 했습니다"는 이 질문에 답이 되지 않습니다.
```

강점이 드러날 질문 유형도 함께 제시해 균형을 맞춥니다.

### 8.11 직무 분석 (면접 전용)

무료는 일의 성격 4개로 조각을 고르지만, 유료는 직무명을 그대로 AI에 넘겨 분석시킵니다.

```
[프롬프트 재료]
  jobTitle: "반도체 공정기술"
  workType: "분석하고 만드는 일"
  company: "SK하이닉스"
  companyInfo: (검색 결과)

[AI 지시]
직무명에서 업무 성격이 파악되면 그 수준까지 쓴다.
파악되지 않으면 workType 수준으로만 쓴다.
업무 내용을 구체적으로 지어내지 않는다.
확신이 없으면 "이런 성격의 일이라면" 형태로 조건을 단다.
```

직무 설명을 검색으로 보강하지 않습니다. 결과가 부정확한 경우가 많고 원가만 올라갑니다.

### 8.12 검색 정책

| 방식 | 검색 대상 | 횟수 |
|---|---|---|
| 필기 | 없음 | 0 |
| 실기 · 오디션 | 없음 | 0 |
| 면접 | 기업 법인 설립일, 기업 정보 | 2 |

필기 리포트는 검색을 하지 않습니다. 과목 정보를 쓰지 않기로 했기 때문입니다. 섹션 8(남은 기간 어떻게 쓸까)은 과목명 대신 학습의 성격(암기, 이해, 반복)으로 배분을 제시하며, 이것은 사주 계산 결과만으로 작성됩니다.

검색이 안 되는 시험이 많다는 것도 이유입니다. 사내 승진시험, 소규모 자격증, 사용자가 이름을 다르게 입력한 경우 전부 실패하고, 그때 나오는 일반 배분 원칙은 다른 곳에서도 볼 수 있는 내용입니다.

#### 구현 방법

별도 검색 API(Brave Search, Serper 등)를 직접 호출하고 결과를 잘라 프롬프트에 주입합니다. Claude API의 web_search 도구를 쓰면 검색 횟수와 토큰량 통제가 어려워 원가가 예측되지 않습니다.

```typescript
const results = await searchAPI(query)
const context = results.slice(0, 3)
  .map(r => r.snippet)
  .join('\n')
  .slice(0, 6000)   // 토큰 상한 통제
```

#### 검색어

```
면접
  1: {기업명} 법인 설립일 연혁
  2: {기업명} 사업 인재상 최근
```

#### 결과 서술 제약

프롬프트에 다음 지시를 반드시 포함합니다.

```
금지: OO기업 2차 면접은 PT입니다
사용: 후기에서 PT 형식이 언급되는 경우가 많습니다.
      확정 전형은 채용 공고를 확인하시기 바랍니다.

금지: 검색 결과에 없는 정보를 지어내기
사용: 확인되지 않으면 해당 부분을 생략하고
      일반적인 관점으로 대체

금지: 특정 기업의 부정적 평판 서술
금지: 설립일을 추측해서 궁합 계산
```

### 8.13 AI 모델 및 원가

모델 claude-sonnet-5

Sonnet 5의 백만 토큰당 가격은 입력 2달러, 출력 10달러입니다. 캐시 히트는 기본 입력의 10퍼센트입니다.

#### 필기 (검색 없음, 분량 축소 후)

```
입력  4,000 × $2  / 1,000,000 = $0.008
출력  7,000 × $10 / 1,000,000 = $0.070
────────────────────────────────────
합계                           = $0.078
환율 1,400원 기준              ≈ 110원
```

#### 면접 (검색 2회, 분량 축소 후)

```
입력  10,000 × $2  / 1,000,000 = $0.020
출력  11,000 × $10 / 1,000,000 = $0.110
─────────────────────────────────────
합계                            = $0.130
환율 1,400원 기준               ≈ 182원
```

#### 분량 축소 전 실측

상한 없이 최소치만 두었을 때의 실측값입니다. 목표의 130퍼센트를 생성해 시간과 원가가 크게 늘었습니다.

```
필기   217초  255원  (3,900원 대비 6.5퍼센트)
면접   308초  411원  (3,900원 대비 10.5퍼센트)
```

출력 토큰의 절반 이상이 thinking 분량입니다. 섹션마다 계산값을 대조하고 분량을 확인하는 과정에서 사고 과정이 길어집니다.

분량 상한을 추가하면 시간과 원가가 함께 내려갑니다. 목표는 필기 90초, 면접 130초입니다.

#### 최대 원가 (안전 상한)

```
입력  15,000 × $2  / 1,000,000 = $0.030
출력   8,000 × $10 / 1,000,000 = $0.080
─────────────────────────────────────
합계                            = $0.110
환율 1,400원 기준               ≈ 154원
```

3,900원 대비 최대 4.0퍼센트입니다.

#### 사고량 조절 — output_config.effort

출력 토큰에는 thinking 분량이 포함됩니다. 실측 결과 출력 토큰의 3분의 2가 thinking이었고, 시간과 원가의 병목은 검색이 아니라 이 thinking입니다. 검색은 2초이고 이미 병렬입니다.

Sonnet 5에서 사고량을 조절하는 지원되는 수단은 `output_config.effort` 하나입니다.

```
thinking 미설정                    OK (adaptive가 기본)
thinking.type = 'adaptive'         OK
thinking.type = 'enabled'          400
  budget_tokens는 Sonnet 5 · Opus 5 · Fable 5에서 제거됐습니다
output_config.effort = low         OK
```

`budget_tokens`를 넣으면 모든 리포트 생성이 400으로 실패합니다. 넣지 마십시오. 위 표는 이 프로젝트의 키로 실제 호출해 확인한 결과입니다.

effort는 `low` / `medium` / `high` / `xhigh` / `max` 다섯 단계이고 미지정 시 `high`입니다. 설정값은 아래와 같습니다.

```
AI_EFFORT_WRITTEN=medium
AI_EFFORT_INTERVIEW=medium
```

구현은 `lib/ai/provider.ts`의 `getEffort(type)`가 환경변수를 호출 시점에 읽고, `lib/ai/providers/anthropic.ts`가 `messages.stream`에 `output_config: { effort }`로 넘깁니다. 잘못된 값은 무시하고 기본값을 씁니다.

#### 확정 설정과 실측 (6회차 22건)

```
AI_EFFORT_WRITTEN=medium
AI_EFFORT_INTERVIEW=medium
AI_MAX_TOKENS=32000
클라이언트 타임아웃   240초   (14.11)
Vercel maxDuration    300초
```

| 구분 | 소요 | 원가 | 3,900원 대비 | 본문 |
|---|---|---|---|---|
| 필기 | 144초 | 201원 | 5.2% | 5,730자 |
| 면접 | 173초 | 233원 | 6.0% | 6,060자 |

**원가 목표는 건당 300원 이내입니다.** 지금 값이 그 안이므로 더 조정하지 않습니다.

#### effort 단계별 비교

| 구분 | effort | 소요 | 원가 | 출력 토큰 | 하한 미달 섹션 | 잘림 |
|---|---|---|---|---|---|---|
| 필기 | high (기본) | 204~288초 | 271~426원 | 17,708~28,823 | — | 0/2 |
| 필기 | low | 58~94초 | 85~132원 | 4,424~7,295 | 회당 1~3개 | 0/7 |
| 필기 | **medium** | 122~167초 | 164~237원 | 9,565~14,779 | **회당 0개** | 0/2 |
| 면접 | high (기본) | 289~308초 | 410원 | 27,407~32,000 | — | **1/2** |
| 면접 | low | 65~69초 | 97~101원 | 5,125~5,361 | 회당 3~4개 | 0/2 |
| 면접 | **medium** | 115~223초 | 159~307원 | 8,980~19,556 | 회당 1~4개 | 0/8 |

기본값 `high`에서 면접 2건 중 1건이 `max_tokens` 32000에 걸려 잘렸습니다. `medium` 이하에서는 22건 모두 `end_turn`입니다.

병목은 검색이 아니라 thinking입니다. 검색은 1.3~2.2초이고 이미 병렬입니다. `high`에서 출력 토큰이 27,000~32,000이던 것이 `medium`에서 9,000~19,000으로 내려갔고 시간과 원가가 그만큼 따라 내려갔습니다.

`low`에서도 오행 수치, 십신 매핑, 12지지 관계, 궁합 점수, 검색 결과 반영이 전부 계산값과 맞았습니다. 생성물을 직접 읽어 확인했습니다. `low`를 쓰지 않는 이유는 정확도가 아니라 섹션 하한 미달입니다.

되돌리려면 환경변수 한 줄입니다. 그때는 미달 섹션이 다시 생기고, 대기 화면 문구와 간격(`lib/content/chat-scripts.ts`)도 함께 되돌려야 합니다.

```
AI_EFFORT_WRITTEN=low      필기 85초 · 121원 · 미달 회당 1~3개
AI_EFFORT_INTERVIEW=low    면접 67초 ·  99원 · 미달 회당 3~4개
```

회차별 원본은 `test/report-output-round1.md`~`round6.md`, 정리본은 `test/report-output.md`입니다.

#### JSON 파싱 복구 3단

실측 12건 중 2건이 파싱에서 죽었습니다. 두 번 다 본문은 멀쩡하고 봉투만 깨졌습니다.

```
Bad control character in string literal          문자열 안 날것 줄바꿈
Unexpected non-whitespace character after JSON   이스케이프 안 된 따옴표로
                                                 객체가 일찍 닫힘
```

두 번째는 제어문자 이스케이프로 못 고칩니다. 따옴표가 어긋나면 문자열 안팎 판정 자체가 무너지기 때문입니다.

```
1. JSON.parse
2. 문자열 안 제어문자만 이스케이프하고 다시
3. 문법을 포기하고 "키": " 위치로 값만 긁어냄
```

3단은 기대한 키의 절반도 못 찾으면 포기합니다. 억지로 살린 반쪽짜리 리포트를 결제한 사용자에게 보여주는 것이 더 나쁩니다.

프롬프트에도 이스케이프 지시를 넣었고 이후 6건에서 복구 0회입니다. 실측 표 머리에 복구 횟수가 찍히므로 0이 아니면 지시가 안 먹히는 것입니다.

#### 원가 통제 장치

원가를 결정하는 것은 실제 생성된 토큰량이지 max_tokens가 아닙니다. max_tokens는 잘림 방지선이며 과금 기준이 아닙니다. 상한을 올려도 실제 생성량만큼만 청구됩니다.

```typescript
// 원가 통제
searchContext.slice(0, 6000)     // 입력 통제
// 프롬프트의 섹션별 분량 지시    // 출력 통제
output_config: { effort }        // thinking 통제

// 잘림 방지 (원가와 무관)
max_tokens: 32000
```

12000과 20000에서 모두 잘림이 발생해 max_tokens를 32000으로 설정했습니다. effort를 내린 뒤로는 출력이 그 절반 아래로 내려갔으나, 잘린 리포트는 결제한 사용자에게 보여줄 수 없어 상한은 넉넉히 둡니다.

응답의 stop_reason이 max_tokens이면 잘린 것이므로 실패로 처리하고 사용자에게 보여주지 않습니다.

#### 실측 원가 (확정)

```
필기   입력 10,633  출력  9,565~14,779   144초   5,730자   201원
면접   입력 11,914  출력  8,980~19,556   173초   6,060자   233원
```

3,900원 대비 5.2퍼센트와 6.0퍼센트입니다. 목표인 건당 300원 이내입니다.

프롬프트 캐싱을 적용하면 시스템 프롬프트 부분이 90퍼센트 절감됩니다. 절대액은 건당 3-4원으로 작지만 구현이 어렵지 않으므로 처음부터 적용합니다.

### 8.14 마진

```
판매가        3,900원
PG 수수료 3%   -117원
AI 원가        -233원 (면접 실측)
──────────────────────
남는 금액     3,550원 (91.0%)
```

원가 상한을 건당 300원으로 잡아도 남는 금액은 3,483원(89.3%)입니다.

### 8.15 모델 선택 계획

| 모델 | 백만 토큰당 | 건당 원가 | 3,900원 대비 |
|---|---|---|---|
| Haiku 4.5 | $1 / $5 | 약 55원 | 1.4% |
| claude-sonnet-5 | $2 / $10 | 약 112원 | 2.9% |
| Opus 5 | $5 / $25 | 약 280원 | 7.2% |

Sonnet 5로 시작하고, 결제 전환율이 확인되면 Opus 5로 A/B 테스트하여 재결제율 차이를 비교합니다. 280원이어도 마진의 7퍼센트라 부담이 크지 않습니다.

### 8.16 프롬프트 재료 구조

AI에 전달할 재료는 모두 코드가 계산한 확정값입니다. AI는 문장만 생성하며 숫자와 판정을 임의로 만들지 않도록 지시합니다.

```json
{
  "reportType": "면접",
  "ddayRange": "normal",
  "sectionSpec": ["saju", "answerType", "company", "compatibility",
                  "job", "questions", "cautions", "outfit",
                  "eve", "calendar", "after"],
  "user": {
    "name": "김민준",
    "dayStem": "병화",
    "pillars": {
      "year": "갑자", "month": "경오",
      "day": "병오", "hour": "무자"
    },
    "elements": { "목": 2, "화": 8, "토": 3, "금": 2, "수": 3 },
    "shipsin": { "비겁": 8, "식상": 3, "재성": 2, "관성": 3, "인성": 2 },
    "shipsinPosition": {
      "관성": "지지에만",
      "식상": "천간과 지지",
      "인성": "없음"
    },
    "strongElement": "화",
    "weakElement": "수",
    "luckyNumber": 1,
    "luckyColor": "남색",
    "luckyDirection": "북",
    "hasBirthTime": true
  },
  "exam": {
    "name": "삼성전자",
    "type": "면접",
    "examPeriod": null,
    "workType": "분석하고 만드는 일",
    "jobTitle": "반도체 공정기술",
    "date": "2026-04-11",
    "startTime": "14:30",
    "dday": 15,
    "hoursUntilStart": 362,
    "now": "2026-03-27T09:14:00+09:00"
  },
  "company": {
    "founded": "1969-01-13",
    "dayStem": "경금",
    "elements": { "목": 1, "화": 1, "토": 2, "금": 1, "수": 1 },
    "strongElement": "토"
  },
  "timeSlots": [
    { "range": "06:30-06:59", "branch": "묘시", "hanja": "卯時",
      "element": "목", "relation": "인성" },
    { "range": "07:00-08:59", "branch": "진시", "hanja": "辰時",
      "element": "토", "relation": "식상" },
    { "range": "09:00-10:59", "branch": "사시", "hanja": "巳時",
      "element": "화", "relation": "비겁" }
  ],
  "fortune": {
    "examDayScore": 78,
    "potentialScore": 108,
    "examDayRelation": "상생",
    "startTimeRelation": "비화",
    "compatibilityScore": 72,
    "compatibilityRelation": "아극",
    "methodFit": {
      "객관식필기": 74, "서술논술": 58, "면접": 81, "실기": 62
    },
    "weekFlow": [
      { "dday": 7, "date": "2026-04-04", "score": 68, "relation": "비화" },
      { "dday": 6, "date": "2026-04-05", "score": 42, "relation": "설기" }
    ],
    "monthFlow": [
      { "month": 1, "score": 55 }, { "month": 2, "score": 68 }
    ]
  },
  "fragments": {
    "compatibility": "내가 상대를 극하는 관계입니다...",
    "shipsin": "병화 일간에게 관성은 수(水)입니다...",
    "pattern": "초반 몰입, 중반 급락, 회복 지연..."
  },
  "search": {
    "companyInfo": "..."
  }
}
```

#### 재료 항목 설명

| 항목 | 용도 | 계산 위치 |
|---|---|---|
| user.shipsin | 섹션 2, 마지막 섹션 | lib/saju/shipsin.ts |
| user.shipsinPosition | 천간·지지 위치 구분 | lib/saju/shipsin.ts |
| timeSlots | 섹션 4 시간대별 운용 | lib/saju/fortune.ts |
| exam.hoursUntilStart | D-DAY 구성의 "지금부터" | 결제 시각 기준 계산 |
| exam.now | D-DAY 구성의 기준 시각 | 서버 시각 |
| fortune.potentialScore | 표지 잠재력 발휘 지수 | 8.7 |
| fragments.shipsin | 섹션 2 앞부분 | paid-fragments.json |
| fragments.pattern | 마지막 섹션 앞부분 | paid-fragments.json |

timeSlots는 시작 시각을 기준으로 기상부터 종료까지의 구간만 생성합니다. 각 구간의 지지 오행과 사용자 일간의 십신 관계를 함께 넣어, AI가 관계를 임의로 판단하지 않게 합니다.

exam.hoursUntilStart가 음수이면 시험이 이미 시작했거나 끝난 상태입니다. 이 경우 시험 이후 섹션 중심으로 구성하고 톤을 전환합니다.

시간을 모른다고 선택한 경우 timeSlots와 startTimeRelation을 넣지 않고, 섹션 4를 시간대 없는 구성으로 대체합니다.

### 8.17 리포트 저장 및 재접근

```
결제 완료
  Supabase reports 테이블에 생성 결과 저장
  같은 리포트 재열람 시 AI 재호출 없음

[카카오톡으로 나에게 보내기]
  카카오 나에게 보내기 API로 리포트 링크 전송
  구글, 이메일 로그인 사용자는 링크 복사로 대체
```

이미지 저장이나 인쇄 대신 링크 전송을 택한 이유는 모바일 사용성입니다. 저장한 이미지는 사진첩에서 다시 찾지 않지만, 카카오톡에 남은 링크는 시험 전날과 당일에 다시 열립니다.

### 8.18 유료 전용 문장 조각

| 조각 | 개수 |
|---|---|
| 기업 궁합 관계 해석 (일간 관계 5종) | 5 |
| 설립일 미확인 대체 (강한 오행 5종) | 5 |
| 십신 해석 (일간 10종) | 10 |
| 반복 패턴 (강한 오행 5종) | 5 |
| **합계** | **25** |

무료 조각과 별도 파일로 관리합니다.

```
/lib/content/fragments.json        무료 222개
/lib/content/paid-fragments.json   유료 25개
/lib/content/preset-exams.json     대분류 10개, 시험명 73개
/lib/content/quotes.json           응원 문구 17개
```

---

## 9. 공유 기능

### 9.1 무료 결과 공유 이미지

무료 결과에서만 이미지 생성 기능을 제공합니다. 저장용이 아니라 타인에게 보내는 용도입니다.

규격 1080 x 1920 (인스타그램 스토리)

캐릭터가 1종이므로 사용자 간 구분은 배경색, 유형 뱃지, 한 줄 요약 세 가지가 담당합니다.

```
┌─────────────────────┐
│                     │  배경: 오행 색 그라데이션
│     [캐릭터]         │
│                     │
│   ┌───────────┐     │
│   │   아이콘   │     │
│   │   몰입형   │     │
│   └───────────┘     │
│                     │
│  김민준 · 국가직 9급  │
│  D-15               │
│                     │
│  시험 당일 운   78   │
│                     │
│  숫자 1 · 색 남색    │
│  방위 북            │
│                     │
│ "몰입은 강하고       │
│  조절이 약한 유형"   │
│                     │
│   siheomsaju.com    │
└─────────────────────┘
```

배경 그라데이션은 오행 색상에서 파생합니다.

```css
background: linear-gradient(160deg,
  {elementColor}22 0%,
  {elementColor}08 60%,
  #FFFFFF 100%
);
```

캐릭터가 동일한 것은 브랜드 인식에 도움이 됩니다. 여러 사람의 공유 이미지가 돌아다닐 때 같은 캐릭터가 반복 노출되면 서비스 식별이 쉬워집니다.

### 9.2 유형 공유 이미지 (뱃지 모달 전용)

시험 정보 없이 유형만 노출하므로 시험이 끝난 뒤에도 공유 가치가 남습니다.

규격 1080 x 1080 (정사각형)

```
┌─────────────────────┐
│      아이콘          │
│                     │
│      몰입형          │
│                     │
│  화(火) 기운이 강한   │
│       유형           │
│                     │
│  전체 이용자 중 21%   │
│                     │
│   siheomsaju.com    │
└─────────────────────┘
```

### 9.3 구현

라이브러리 html-to-image

```typescript
import { toPng } from 'html-to-image'

const handleShare = async () => {
  const node = document.getElementById('share-card')
  const dataUrl = await toPng(node, {
    pixelRatio: 2,
    backgroundColor: '#ffffff'
  })

  if (isIOS()) {
    setPreviewImage(dataUrl)
  } else {
    const link = document.createElement('a')
    link.download = `시험사주_${name}.png`
    link.href = dataUrl
    link.click()
  }
}
```

주의사항:
- 웹폰트는 next/font로 self-host해야 캡처 시 깨지지 않습니다
- 캐릭터 PNG는 같은 도메인에서 서빙해야 CORS 오류가 나지 않습니다
- iOS 사파리는 프로그래매틱 다운로드가 동작하지 않으므로 이미지 표시 후 "길게 눌러 저장하세요" 안내를 노출합니다

---

## 10. 입력 데이터 처리

### 10.1 대분류 10개

```
1.  수능 · 모의고사
2.  공무원 · 고시
3.  자격증 · 어학        (2단계 분기)
4.  기업 필기
5.  기업 면접
6.  편입 · 대학원
7.  대학교 시험
8.  승진 · 사내시험
9.  오디션 · 실기
10. 기타                (자유 입력 전용)
```

3번은 하위 그룹이 있어 한 단계를 더 묻습니다. 자격증과 어학은 성격이 다르지만 각각을 대분류로 두면 개수가 늘어나므로 묶었습니다.

7번(대학교 시험)은 중간고사와 기말고사를 다룹니다. 여러 날에 걸쳐 보는 경우가 많아 시험 기간을 추가로 묻습니다.

8번(승진 · 사내시험)은 검색으로 정보를 얻을 수 없는 영역입니다. 다만 이 서비스는 시험 정보를 거의 쓰지 않으므로 문제가 되지 않습니다. 오히려 다른 곳에서 정보를 얻을 수 없어 대안이 없는 시장입니다.

9번(오디션 · 실기)은 방식이 오디션으로 자동 설정됩니다.

### 10.2 시험 방식 4분류

```
필기 / 면접 / 실기 / 오디션
```

대분류의 defaultType이 있으면 자동으로 설정하고, 없으면 사용자에게 묻습니다. 자동 설정된 경우에도 변경할 수 있습니다.

오디션을 실기와 분리한 이유는 평가 성격이 다르기 때문입니다.

| | 실기 | 오디션 |
|---|---|---|
| 평가 대상 | 절차와 결과물 | 인상과 표현 |
| 심사 기준 | 기준표 | 주관적 판단 |
| 긴장의 형태 | 손이 굳음 | 표현이 잠김 |
| 대기 시간 | 순서 대기 | 남을 보며 위축 |

오디션은 실기보다 면접에 가깝습니다. 실기 조언(절차, 순서, 도구)을 그대로 쓰면 오디션 준비생에게 맞지 않습니다.

공기업처럼 필기와 면접을 모두 보는 경우, 사용자가 지금 준비하는 단계를 선택합니다.

#### 시험명에서 방식 추론

```typescript
function inferType(examName: string, categoryDefault: string | null) {
  if (examName.includes('면접')) return '면접'
  if (examName.includes('오디션')) return '오디션'
  if (examName.includes('실기')) return '실기'
  return categoryDefault
}
```

어학은 전부 필기로 처리합니다. 토익 스피킹이나 오픽이 말하기 시험이지만, 심사 성격이 오디션과 다르고 사용자도 필기로 인식하는 경우가 많습니다.

### 10.3 시험명 처리

시험명은 프리셋 버튼과 자유 입력을 함께 제공합니다. 대화형 입력에서 타이핑은 흐름을 끊으므로, 버튼으로 고르게 하고 목록에 없을 때만 입력창을 띄웁니다.

목록은 /lib/content/preset-exams.json에서 관리합니다.

```
대분류 10개
시험명 73개
```

모든 대분류에 "직접 입력할게요" 버튼을 마지막에 붙입니다. 기타 분류는 freeInputOnly가 true이므로 버튼 없이 바로 입력창을 띄웁니다.

#### 하위 그룹

자격증 · 어학만 subGroups를 갖습니다.

```json
{
  "id": "cert-lang",
  "label": "자격증 · 어학",
  "defaultType": "필기",
  "subGroups": [
    { "id": "cert", "label": "자격증", "exams": [...] },
    { "id": "lang", "label": "어학", "exams": [...] }
  ]
}
```

코드에서 분기합니다.

```typescript
if (category.subGroups) {
  // 2단계 질문 후 시험명 버튼
} else {
  // 바로 시험명 버튼
}
```

#### 자유 입력이 쌓이는 구조

직접 입력된 시험명은 queries.exam_name에 저장됩니다. 관리자 화면에서 집계해 자주 나오는 것을 프리셋에 추가하면, 서비스가 쓸수록 목록이 정확해집니다.

```
1차   73개로 시작
       직접 입력을 queries에 저장
운영   자주 입력되는 시험명 집계 (22.14)
2차   상위 항목을 목록에 추가
```

정규화가 필요합니다. 같은 시험을 사람마다 다르게 입력합니다.

```typescript
function normalizeExamName(raw: string): string {
  return raw.trim().replace(/\s+/g, ' ').replace(/[()（）]/g, '')
}
```

원본과 정규화 값을 모두 저장합니다. queries에 exam_name_raw 컬럼을 둡니다.

#### 조사 처리

영문 시험명(GSAT, LEET 등)은 한글이 아니므로 받침 있음으로 처리합니다. 영문 약어는 대부분 자음으로 끝나 이 처리가 자연스럽습니다.

### 10.4 대학교 시험의 시험 기간

대학 중간고사와 기말고사는 여러 날에 걸쳐 봅니다.

```
6월 15일 (월)  전공A
6월 16일 (화)  전공B
6월 18일 (목)  교양
```

대분류가 대학교 시험인 경우에만 기간을 추가로 묻습니다. hasExamPeriod가 true인 분류입니다.

```
[합격이] 시험이 며칠 동안 이어져요?

[하루]  [2~3일]  [4~7일]  [일주일 이상]
```

기간을 받으면 7일 기운 흐름 차트에 시험 기간 전체를 표시할 수 있습니다. 어느 날 시험을 보는지는 묻지 않고, 첫날을 기준으로 계산합니다.

### 10.5 기업 정보 수집 (면접 유료 전용)

기업 DB도 두지 않습니다. 검색으로 조달합니다.

```
검색어 1  {기업명} 법인 설립일 연혁
검색어 2  {기업명} 사업 인재상 최근

수집 항목
  법인 설립일 (궁합 계산용)
  업종, 규모, 최근 동향 (기업 소개용)

설립일 수집 실패 시
  궁합 섹션을 생략하고 대체 섹션으로 전환 (8.7 참조)
  추측 날짜로 계산하지 않음
```

설립일 기준 규칙은 4.4를 따릅니다. 창립일과 법인 설립일이 다르면 법인 설립일을, 외국계 한국법인은 한국법인 설립일을 사용합니다.

기업 DB는 2차 확장 항목입니다. 검색 실패율과 응답 속도를 측정한 뒤 필요하면 주요 기업 100개를 등록합니다.

### 10.6 시험별 전용 문장 없음

시험별 특성 문장을 만들지 않습니다. 시험이 늘어도 문장 개수가 증가하지 않도록, 카드 2의 첫 문단은 방식별 문장 21개로 처리하고 시험명은 {exam} 변수로 치환합니다.

### 10.7 일의 성격 4분류 (면접 전용)

직무를 업종이 아니라 일의 성격으로 나눕니다. 8개 카테고리로는 특수 직무(반도체 공정기술, 임상시험 모니터링, 항공정비, 수의사 등)를 담을 수 없습니다.

```
사람을 만나는 일     영업, 상담, 서비스, 교육
분석하고 만드는 일   개발, 연구, 설계, 회계
조율하고 운영하는 일  기획, 인사, 총무, 관리
현장에서 움직이는 일  생산, 정비, 물류, 의료
```

어떤 특수 직무든 이 넷 중 하나에 들어갑니다. 오행이 업종이 아니라 성향을 다루므로 사주 해석과의 연결도 자연스럽습니다.

직무명은 별도 자유 입력으로 받고 문장에 변수로만 사용합니다.

### 10.8 기업 규모는 묻지 않습니다

전에는 면접에서 기업 규모를 6분류로 물었습니다(대기업 / 중견기업 / 공기업 / 금융권 / 스타트업 / 기타). 지금은 묻지 않습니다.

세 가지 이유입니다.

1. 선택지 6개를 보여주고 얻는 것이 문장 하나뿐입니다.
2. 취준생 대부분이 중소·중견인데 대기업이 첫 버튼이라 해당 없다고 느끼기 쉽습니다. 첫 화면에서 "나는 여기 없네" 싶은 목록을 보여주면 안 됩니다.
3. 유료에서 기업명을 받아 검색하면 규모까지 나옵니다. 무료에서 따로 물을 이유가 없습니다.

되살릴 여지는 남겨 둡니다.

- `fragments.json`의 `companyScale` 조각 6개는 파일에 그대로 둡니다. 조립 함수에서 호출하지 않을 뿐입니다.
- `queries.company_scale` 컬럼도 그대로 둡니다. 값이 `null`로 들어갈 뿐이고, 되살릴 때 마이그레이션이 필요 없습니다.

그래서 조립에 실제로 쓰이는 무료 조각은 **216개**입니다. 파일에는 222개가 있습니다.

---

## 11. 인증 및 계정

### 11.1 제공 방식

- 카카오 로그인
- 구글 로그인
- 이메일 로그인

Supabase Auth를 사용합니다.

### 11.2 로그인 시점

로그인은 강제하지 않습니다. 다음 시점에만 요청합니다.

```
[필수] 결제
[유도] 무료 결과 하단 "내 결과 저장하기"
[유도] 마이페이지 진입 시
```

무료 결과 조회까지는 비로그인으로 완주할 수 있어야 합니다. 사주 서비스는 결제부터 요구하는 곳이 많아 진입 저항이 큽니다.

### 11.3 카카오 스코프

나에게 보내기 기능을 위해 로그인 시 talk_message 스코프를 함께 요청합니다. 별도 시점에 추가 동의를 받으면 마찰이 커지므로 로그인 단계에서 한 번에 처리합니다.

### 11.4 이름 처리

```
우선순위
  1. 사용자가 입력 폼에서 직접 입력한 이름
  2. 카카오 닉네임 또는 구글 이름 (로그인 상태)
  3. 없음
```

이름이 없는 경우 3.8의 render 함수로 호명 부분을 제거합니다.

### 11.5 계정 정보 수정

경로 /my/profile

```
수정 가능 항목
  이름
  생년월일
  태어난 시간 (모름 상태에서 입력으로 전환 가능)

수정 시 처리
  profiles 테이블 갱신
  저장된 queries의 계산 결과 재산출
  이미 생성된 유료 리포트는 재생성하지 않음
```

태어난 시간을 모른다고 입력한 사용자가 나중에 알게 되는 경우가 있습니다. 이 경로가 4.3.3의 재입력 유도와 연결됩니다.

유료 리포트를 재생성하지 않는 이유는 AI 원가 때문입니다. 대신 안내를 노출합니다.

```
생년월일시를 수정하면 앞으로 보는 결과에 반영됩니다.
이미 구매하신 리포트는 구매 시점 기준으로 유지됩니다.
```

### 11.6 회원 탈퇴

경로 /my (하단)

개인정보보호법상 파기 요구권에 해당하므로 필수 기능입니다.

```
[탈퇴하기] 클릭
   ↓
확인 화면
  "탈퇴하면 저장된 결과와 구매한 리포트가
   모두 삭제되며 복구할 수 없습니다"
   ↓
[탈퇴 확정]
   ↓
1. payments.user_id를 null로 변경 (결제 이력 보존)
2. auth.users 삭제
3. profiles, queries, reports는 cascade로 삭제
```

결제 이력은 전자상거래법상 5년 보관 의무가 있습니다. 사용자 식별 정보를 제거한 상태로 남깁니다.

---

## 12. 결제

### 12.1 PG

포트원 또는 토스페이먼츠

소액 결제이므로 계약 전 건당 최소 수수료 유무를 반드시 확인합니다. 일부 PG는 정률 수수료 외에 건당 고정 수수료를 부과하며, 3,900원 결제에서는 마진에 영향이 큽니다.

### 12.2 결제 수단

- 카드
- 휴대폰 소액결제 (수수료 6-9퍼센트로 높으나 10-20대 전환율이 높음)
- 간편결제 (카카오페이, 토스)

### 12.3 법적 요건

```
1. 사업자 등록
2. 통신판매업 신고
3. 디지털 콘텐츠 청약철회 제한 고지
   결제 버튼 위에 동의 체크박스 배치
   "콘텐츠 제공이 시작되면 청약철회가 제한됩니다"
```

### 12.4 결제 내역 표시 항목

경로 /my/payments

전자상거래법상 다음 항목을 표시합니다.

```
상품명
결제 금액
결제 일시
결제 수단
거래 상태 (완료 / 취소)
```

취소된 건은 취소 일시를 함께 표시합니다.

```
시험 전 7일 상세 플랜
서울시 9급
2026.06.10  3,900원
결제 취소 (2026.06.11)
```

### 12.5 환불 정책

```
환불 가능
  리포트 생성 실패
  중복 결제
  서비스 오류로 열람 불가

환불 제한
  리포트를 정상 열람한 경우
  (12.3의 청약철회 제한 고지에 근거)
```

환불 처리는 관리자 화면에서 수행합니다. 22장을 참조하시기 바랍니다.

### 12.6 쿠폰

```
쿠폰 코드 입력란을 결제 화면에 배치
100퍼센트 할인 쿠폰으로 무료 지급 처리
발급은 관리자 화면에서 수행
```

용도는 인플루언서 제공, 이벤트 당첨, 사전 테스트입니다. CS 보상은 쿠폰이 아니라 관리자 직접 지급(22.6)으로 처리합니다.

### 12.7 목업 단계 처리

결제 연동 전 단계에서는 결제 버튼을 더미로 두고, 클릭 시 이메일 수집 화면으로 연결합니다.

```
"곧 오픈됩니다. 알림 받으시겠어요?"
[이메일 입력] [알림 받기]
```

실제 결제 없이 결제 의사를 측정할 수 있습니다.

### 12.8 가격 테스트

목업 단계에서 가격만 다르게 노출하여 CTA 클릭률을 비교합니다.

```
A안 1,900원
B안 3,900원
C안 5,900원
```

CTA 클릭률과 결제 의사 표시율을 곱해 기대 매출이 가장 큰 가격을 선택합니다.

---

## 13. 데이터베이스 (Supabase)

### 13.1 테이블 구조

```sql
-- 사용자 프로필 (auth.users 확장)
create table profiles (
  id uuid references auth.users on delete cascade primary key,
  name text,
  birth_date date,
  birth_time time,
  has_birth_time boolean default true,
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- 조회 기록 (무료 결과 포함)
create table queries (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users on delete cascade,
  session_id text,                          -- 비로그인 식별용

  exam_name text not null,                  -- 정규화된 값
  exam_name_raw text,                       -- 사용자 원본 입력
  exam_category text,
  exam_type text not null,                  -- 필기 / 면접 / 실기 / 오디션
  exam_date date not null,
  exam_start_time time,
  exam_period text,                         -- 대학교 시험 전용 (하루/2-3일/4-7일/일주일이상)

  -- 면접 전용
  company_scale text,                       -- 기업 규모. 지금은 묻지 않아 null (10.8)
  work_type text,                           -- 일의 성격 4분류
  job_title text,                           -- 자유 입력 직무명
  company_name text,                        -- 유료 결제 시 입력

  birth_date date not null,
  birth_time time,
  has_birth_time boolean default true,
  name text,

  -- 계산 결과 캐싱
  day_stem text,
  day_pillar_index int,
  strong_element text,
  weak_element text,
  exam_day_score int,

  created_at timestamptz default now()
);

-- 유료 리포트
create table reports (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users on delete cascade not null,
  query_id uuid references queries on delete cascade not null,
  report_type text not null,                -- 필기 / 면접
  dday_range text not null,                 -- normal / short / eve / dday

  content jsonb,                            -- 섹션별 생성 결과
  status text default 'pending',            -- pending / completed / failed / refunded
  error_message text,
  retry_count int default 0,
  started_at timestamptz,                   -- 생성 시작 시각, 좀비 레코드 판별용

  -- 원가 추적
  input_tokens int,
  output_tokens int,
  generation_ms int,
  total_chars int,                          -- 생성된 본문 글자 수

  -- 무료 지급
  granted_by text,                          -- 관리자 이메일, 지급 건만

  created_at timestamptz default now()
);

-- 결제 이력 (탈퇴해도 보존, FK 없음)
create table payments (
  id uuid primary key default gen_random_uuid(),
  user_id uuid,                             -- 탈퇴 시 null 처리
  report_id uuid,
  payment_id text,                          -- PG 거래번호
  amount int not null,
  product_type text not null,               -- 필기 / 면접
  payment_method text,                      -- 카드 / 간편결제 / 휴대폰
  coupon_code text,
  is_granted boolean default false,         -- 관리자 무료 지급 여부
  paid_at timestamptz not null,
  refunded_at timestamptz,
  refund_reason text,
  refunded_by text,                         -- 처리한 관리자 이메일
  created_at timestamptz default now()
);

-- 쿠폰
create table coupons (
  code text primary key,
  discount_type text not null,              -- percent / amount
  discount_value int not null,
  max_uses int,
  used_count int default 0,
  valid_until timestamptz,
  memo text,                                -- 발급 목적
  created_by text,
  created_at timestamptz default now()
);

-- 문의
create table inquiries (
  id uuid primary key default gen_random_uuid(),
  user_id uuid,
  category text not null,                   -- 결제 / 리포트 / 계정 / 오류 / 기타
  content text not null,
  email text not null,
  status text default 'open',               -- open / answered / closed
  reply text,
  replied_at timestamptz,
  replied_by text,
  created_at timestamptz default now()
);

-- 검색 로그 (2차 확장 판단 근거)
create table search_logs (
  id uuid primary key default gen_random_uuid(),
  query_type text not null,                 -- company / exam
  keyword text not null,
  success boolean not null,
  created_at timestamptz default now()
);

-- 공지 배너
create table notices (
  id uuid primary key default gen_random_uuid(),
  message text not null,
  is_active boolean default false,
  created_by text,
  created_at timestamptz default now()
);

-- 인덱스
create index idx_queries_user on queries(user_id);
create index idx_queries_created on queries(created_at desc);
create index idx_queries_exam on queries(exam_name, exam_date);
create index idx_reports_user on reports(user_id);
create index idx_reports_status on reports(status) where status != 'completed';
create index idx_payments_paid on payments(paid_at desc);
create index idx_inquiries_status on inquiries(status) where status = 'open';
create index idx_search_logs_keyword on search_logs(keyword, success);
```

### 13.2 RLS 정책

```sql
alter table profiles enable row level security;
alter table queries enable row level security;
alter table reports enable row level security;
alter table payments enable row level security;
alter table inquiries enable row level security;

create policy "own profile" on profiles
  for all using (auth.uid() = id);

create policy "own queries" on queries
  for all using (auth.uid() = user_id or user_id is null);

create policy "own reports" on reports
  for select using (auth.uid() = user_id);

create policy "own payments" on payments
  for select using (auth.uid() = user_id);

create policy "own inquiries" on inquiries
  for all using (auth.uid() = user_id or user_id is null);
```

coupons, search_logs, notices는 클라이언트에서 직접 접근하지 않습니다. 서버 라우트에서 service_role 키로만 다룹니다.

관리자 화면도 service_role 키를 사용하는 서버 라우트를 통해 접근하며, RLS를 우회합니다. 권한 확인은 미들웨어와 각 API Route에서 이중으로 수행합니다.

### 13.3 개인정보 처리

생년월일과 출생시간은 민감한 정보입니다.

- 비로그인 사용자는 서버 저장을 최소화하고 세션에서 처리합니다
- 로그인 사용자는 명시적 동의 후 저장합니다
- 개인정보 처리방침 페이지를 필수로 두고 수집 항목과 보관 기간을 기재합니다
- 탈퇴 시 결제 이력을 제외한 모든 데이터를 삭제합니다

---

## 14. 화면

### 14.1 하단 탭 구조

탭은 3개입니다.

```
┌──────────────────────────────┐
│                              │
│         (콘텐츠 영역)         │
│                              │
├──────────────────────────────┤
│    홈        새 시험   마이페이지│
└──────────────────────────────┘
```

| 탭 | 아이콘 (lucide-react) | 내용 |
|---|---|---|
| 홈 | Home | 비로그인은 랜딩, 로그인은 오늘의 운과 D-day |
| 새 시험 | Plus | 입력 9단계 시작 |
| 마이페이지 | User | 계정, 리포트 목록, 설정, 문의 |

우측 상단 프로필 아이콘은 두지 않습니다. 마이페이지가 탭에 있으므로 중복입니다.

### 14.2 탭바 표시 규칙

| 화면 | 탭바 |
|---|---|
| 홈 | 보임 |
| 마이페이지 | 보임 |
| 무료 결과 | 보임 |
| 유료 리포트 | 보임 |
| 입력 진행 중 (9단계) | 숨김 |
| 로그인 | 숨김 |
| 결제 | 숨김 |
| 마이페이지 하위 화면 | 숨김 |

기준은 이탈하면 안 되는 화면에서 숨기는 것입니다. 입력 중이거나 결제 중에 다른 탭을 누르면 진행이 손실됩니다.

마이페이지 하위 화면에서 숨기는 이유는 뒤로가기로 마이페이지에 돌아오는 흐름이 자연스럽기 때문입니다.

### 14.3 라우트

```
/                    홈 (탭 1)
/start               새 시험, 입력 (탭 2)
/my                  마이페이지 (탭 3)

/my/profile          내 정보 수정
/my/payments         결제 내역
/my/inquiry          문의하기
/my/faq              자주 묻는 질문
/my/settings         알림 설정

/result              무료 결과
/report/[id]         유료 리포트
/login               로그인
/terms               이용약관
/privacy             개인정보 처리방침

/admin               관리자 (22장)
```

### 14.4 홈 (비로그인)

```
┌──────────────────────────────┐
│        [합격이 크게]            │
│                              │
│      시험 보는 날,            │
│      내 기운은 어떨까?         │
│                              │
│   생년월일과 시험 날짜로       │
│   그날의 흐름을 봅니다         │
│                              │
│   ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄   │
│   시험을 앞둔 당신을 위한       │
│   응원 한 마디                 │
│                              │
│   떨리는 건 진심이라는          │
│   뜻입니다                     │
│   ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄   │
│                              │
│      내 시험운은 어떨까?       │
│  ┌────────────────────────┐  │
│  │   합격이랑 이야기하기      │  │
│  └────────────────────────┘  │
│      1분이면 끝나요            │
├──────────────────────────────┤
│  다른 사주와 뭐가 다른가요?    │
│                              │
│  ┌────────────────────────┐  │
│  │ 시험 날짜를 계산합니다   │  │
│  │ 나의 사주 오행과 십신    │  │
│  │ 흐름을 시험 날짜에       │  │
│  │ 맞춰 봅니다             │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ 필기와 면접이 다릅니다   │  │
│  │ 공무원, 어학, 자격증,    │  │
│  │ 기업 면접까지 시험       │  │
│  │ 특성에 맞춰 알려드려요   │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ 시험 전 7일을 봅니다     │  │
│  │ 어느 날 집중이 붙고      │  │
│  │ 어느 날 쉬어야 하는지    │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ AI와 대화하며 봅니다     │  │
│  │ 사주 명리에 기초해       │  │
│  │ 합격이가 묻고 답해드려요 │  │
│  └────────────────────────┘  │
├──────────────────────────────┤
│  이런 걸 알려드려요            │
│  ┌────────────────────────┐  │
│  │ [합격이] [몰입형] 당일 78│  │
│  │                        │  │
│  │ 시험 전 7일 기운 흐름    │  │
│  │  ▁▁▂▅▃▂▃█             │  │
│  │  D-7            당일    │  │
│  │                        │  │
│  │ ┌────────┬───────────┐ │  │
│  │ │행운의   │피해야 할   │ │  │
│  │ │숫자  3  │색  ● ●    │ │  │
│  │ └────────┴───────────┘ │  │
│  │                        │  │
│  │ 나에게 맞는 시험 유형    │  │
│  │  면접        81 ████▌  │  │
│  │  객관식 필기  74 ███▉   │  │
│  │                        │  │
│  │ ░블러 처리한 본문 한 줄░ │  │
│  │                        │  │
│  │      🔒 카드 8개 전부 보기│  │
│  └────────────────────────┘  │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │   합격이랑 이야기하기      │  │
│  └────────────────────────┘  │
│  로그인 없이 바로 볼 수 있어요  │
├──────────────────────────────┤
│  이 서비스는 사주 명리 해석에   │
│  기반한 참고 자료이며, 시험     │
│  결과를 예측하거나 보장하지     │
│  않습니다.                    │
├──────────────────────────────┤
│    홈       새 시험   마이페이지│
└──────────────────────────────┘
```

랜딩에서 확보해야 할 세 가지:

1. 무료라는 사실을 두 번 명시합니다
2. 990원대 사주와의 차별점을 결과 전에 답합니다
3. 결과 미리보기로 완주 동기를 만듭니다

#### 대화창을 첫 화면으로 두지 않는 이유

입력은 대화형이지만 홈은 랜딩을 유지합니다.

첫째, 이 서비스의 트래픽은 검색에서 옵니다. 첫 화면이 대화창이면 크롤러가 읽을 텍스트가 없어 SEO가 불리해집니다.

둘째, 990원대 사주와의 차별점을 결과를 보기 전에 전달해야 합니다. 대화를 끝내고 결과를 봐야 차이를 알게 되면 완주율이 떨어집니다.

#### 응원 문구

캐릭터 아래, 버튼 위에 배치합니다. 구분선으로 감싸면 인용처럼 보여 눈에 들어옵니다.

문구는 /lib/content/quotes.json의 17개 중 방문마다 무작위로 하나를 표시합니다. 헤더는 "시험을 앞둔 당신을 위한 응원 한 마디"입니다.

같은 문구를 무료 결과 하단과 리포트 생성 대기 화면에도 사용합니다. 공유 이미지에 넣으면 캡션 역할을 합니다.

버튼은 첫 화면에서 스크롤 없이 보이는 위치에 둡니다. 설명을 읽고 싶은 사용자만 아래로 내려갑니다. 버튼 문구를 "합격이랑 이야기하기"로 하면 다음 화면에 무엇이 나올지 예고가 됩니다.

#### 버튼 바로 위 한 줄

```
내 시험운은 어떨까?
[ 합격이랑 이야기하기 ]
```

"생년월일과 시험 날짜로 그날의 흐름을 봅니다"는 서비스가 무엇을 하는지 말합니다. 이 줄은 그래서 지금 누르라는 신호입니다. 설명이 아니라 질문이고, 사용자가 속으로 하던 말을 대신 꺼내 줍니다.

버튼과 한 덩어리로 읽혀야 하므로 간격을 좁게 둡니다. 위 문구와는 벌리고 버튼과는 붙입니다.

#### 차별점 카드 네 번째 — 표현 주의

네 번째 카드는 "AI와 대화하며 봅니다"입니다.

**"챗GPT처럼"이라고 쓰지 않습니다.** 실제 입력은 버튼을 고르는 방식이라, 자유 대화를 기대하고 들어오면 첫 화면에서 실망합니다. 기대를 실제보다 높이는 카피는 유입을 늘리는 대신 이탈을 만듭니다.

#### 미리보기는 목록이 아니라 결과 조각으로

전에는 카드 제목 8개를 번호와 함께 나열했습니다. "행운의 숫자는?"만 읽어서는 무엇을 받는지 감이 오지 않습니다. 제목은 정보가 아니라 목차입니다.

지금은 결과의 조각을 그대로 보여줍니다.

| 요소 | 무엇을 전달하나 |
|---|---|
| 7일 기운 흐름 막대 8개 | 날짜별로 다르게 본다는 것. 990원대 사주와의 차이를 말없이 증명합니다 |
| 행운의 숫자 · 피해야 할 색 | 구체적인 행동 지침이 나온다는 것 |
| 시험 유형 궁합 막대 | 계산 기반이라는 것 |
| 블러 처리한 문장 한 줄 | 글도 이만큼 나온다는 것. 읽히지는 않지만 분량과 문체는 전달됩니다 |

블러 문장은 `filter: blur(3.5px)`, `user-select: none`으로 두고 `aria-hidden`을 붙입니다. 읽으라고 두는 것이 아니므로 스크린리더에는 숨기고, 무엇이 가려졌는지는 아래 "카드 8개 전부 보기"가 말합니다.

**숫자와 데이터는 실제 계산값이 아니라 고정 예시입니다.** 랜딩은 비로그인 화면이라 계산할 입력이 없습니다.

### 14.5 홈 (로그인)

```
┌──────────────────────────────┐
│  안녕하세요, 김민준님          │
│  2026년 8월 23일             │
│                              │
│  ┌────────────────────────┐  │
│  │  [합격이]  시험까지 15일   │  │
│  │         남았어요!        │  │
│  │                        │  │
│  │  국가직 9급 공무원       │  │
│  │  오늘의 운      64      │  │
│  │              [보러가기] │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 서울시 9급              │  │
│  │ D-78 · 오늘의 운 71     │  │
│  └────────────────────────┘  │
│                              │
│  (이하 랜딩 콘텐츠 축약)       │
├──────────────────────────────┤
│    홈       새 시험   마이페이지│
└──────────────────────────────┘
```

오늘의 운이 매일 바뀌므로 이 블록이 재방문 요인이 됩니다. 입력을 다시 할 필요가 없어집니다.

공지 배너가 활성화된 경우 최상단에 표시합니다.

### 14.6 입력 화면 (대화형)

입력은 폼이 아니라 대화 형식으로 진행합니다. 합격이가 질문하고 사용자가 답하는 구조이며, 탭바는 숨깁니다.

```
┌──────────────────────────────┐
│  ←                           │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 안녕하세요, 합격이예요│ │
│        │ 어떤 시험 준비하세요│ │
│        └──────────────────┘ │
│                              │
│              ┌────────────┐  │
│              │ 공무원·고시  │  │
│              └────────────┘  │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 공무원이시군요!    │ │
│        │ 어떤 시험이에요?   │ │
│        └──────────────────┘ │
│                              │
│  ┌────────────┬───────────┐  │
│  │ 9급 공채    │ 지방직 9급 │  │
│  ├────────────┼───────────┤  │
│  │ 7급 공채    │ 서울시 9급 │  │
│  ├────────────┼───────────┤  │
│  │ 경찰공무원   │ 소방공무원 │  │
│  ├────────────┼───────────┤  │
│  │ 5급 공채    │ 군무원     │  │
│  └────────────┴───────────┘  │
│  ┌────────────────────────┐  │
│  │    직접 입력할게요       │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

#### 대화형을 택한 이유

1. 9단계 진행 표시줄은 남은 단계 수가 보여 부담을 만듭니다. 대화는 한 번에 하나만 물어 끝이 보이지 않습니다
2. 합격이가 결과 화면에서 말풍선으로 말을 거는데, 입력만 폼이면 캐릭터가 따로 놉니다
3. 사주는 원래 묻고 답하는 형식이라 서비스 맥락과 맞습니다
4. 이전 답변이 위에 쌓여 무엇을 골랐는지 확인할 수 있습니다

#### AI인 척하지 않습니다

자유 대화가 아니라 합격이가 안내하는 형식입니다. 다음 세 가지를 지킵니다.

```
1. 자유 텍스트 입력창을 상시 노출하지 않습니다
   필요한 단계에서만 입력 위젯을 띄웁니다

2. 사용자가 예상 밖의 입력을 할 수 없는 구조입니다
   버튼, 달력, 시간 선택기만 제공합니다

3. 타이핑 애니메이션을 쓰지 않습니다
   말풍선은 즉시 표시하고 페이드인만 적용합니다
```

AI처럼 보이게 만들면 사용자가 자유 입력을 시도하고, 그것을 처리하려면 무료 구간에 AI를 붙여야 합니다. 원가 0원과 즉시 응답이라는 설계 전제가 무너집니다.

#### 입력 방식별 위젯

| 단계 | 위젯 | 아이콘 |
|---|---|---|
| 대분류 | 버튼 8개 | 없음 |
| 시험명 | 버튼 6-8개 + 직접 입력 | 키보드 (직접 입력만) |
| 방식 | 버튼 3개 | 없음 |
| 일의 성격 | 버튼 4개 | 없음 |
| 직무명 | 텍스트 입력 + 건너뛰기 | 키보드 |
| 날짜 | 달력 선택기 | 캘린더 |
| 시간 | 시간 선택기 + 모름 | 시계 |
| 이름 | 텍스트 입력 + 건너뛰기 | 키보드 |

아이콘을 버튼에 붙이면 다음에 어떤 입력이 나올지 미리 알 수 있습니다.

#### 모션 값

framer-motion을 사용합니다. 21.11의 컴포넌트 사양과 함께 참조합니다.

```javascript
// 합격이 말풍선 등장
animate(bubble,
  { opacity: [0, 1], y: [8, 0] },
  { duration: 0.26, ease: [0.22, 1, 0.36, 1] })

// 선택지 순차 등장
animate(option,
  { opacity: [0, 1], y: [6, 0] },
  { duration: 0.2, delay: index * 0.045, ease: [0.22, 1, 0.36, 1] })

// 사용자 답변 말풍선
animate(answer,
  { opacity: [0, 1], y: [8, 0] },
  { duration: 0.26, ease: [0.22, 1, 0.36, 1] })
```

```
타이밍
  말풍선 표시 후 260ms 뒤 선택지 등장
  선택 후 320ms 뒤 다음 질문
```

0.26초가 상한입니다. 더 느리면 9단계를 거치는 동안 기다리는 느낌이 누적됩니다.

#### 스크롤 처리

새 말풍선이 추가될 때마다 하단으로 자동 스크롤합니다. 텍스트 입력 위젯이 뜰 때 키보드가 올라오면 입력창이 가려지므로, visualViewport API로 높이 변화를 감지해 보정합니다.

```javascript
window.visualViewport?.addEventListener('resize', () => {
  container.scrollTop = container.scrollHeight
})
```

#### 답변 수정

이전 사용자 답변 말풍선을 탭하면 해당 단계로 돌아갑니다. 그 이후 답변은 초기화됩니다.

### 14.7 방식별 입력 단계

#### 공통 흐름

```
1. 대분류 (버튼 10개)
2. (자격증·어학인 경우) 하위 그룹 (버튼 2개)
3. 시험명 (버튼 + 직접 입력)
4. 방식 확인 (defaultType 있으면 생략)
5. (대학교 시험인 경우) 시험 기간 (버튼 4개)
6. 시험 날짜 (달력)
7. 시작 시간 (시간 선택기 + 모름)
8. 생년월일 (달력)
9. 태어난 시간 (시간 선택기 + 모름)
10. 이름 (선택, 건너뛰기 가능)
```

대부분의 사용자는 7단계에서 8단계를 거칩니다. 하위 그룹과 시험 기간은 해당 분류에서만 나옵니다.

#### 면접 추가 단계

대분류가 기업 면접이거나 방식이 면접인 경우, 시험명 다음에 두 단계가 추가됩니다.

```
3-1. 일의 성격 (버튼 4개)
3-2. 직무명 (자유 입력, 건너뛰기 가능)
```

기업 규모는 묻지 않습니다 (10.8).

면접 무료 단계에서 기업명을 받지 않습니다. 기업명 입력이 유료 결제의 트리거가 되어야 CTA가 성립합니다.

#### 시작 시간을 모르는 경우

7단계에서 모름을 선택하면 카드 4(시작 시간 궁합)를 표시하지 않고 안내 문구로 대체합니다.

```
시험 시간이 나오면 다시 입력해보세요.
시간대에 따라 유리한 조건이 달라집니다.
```

면접은 개인별로 시간을 통보받으므로 이 안내가 재방문 요인이 됩니다.

### 14.8 상태 유지

대화 중간에 새로고침하거나 뒤로 가면 입력이 손실됩니다. sessionStorage에 저장합니다. 개인정보이므로 localStorage 대신 세션 범위를 사용합니다.

```typescript
// 답변마다 저장
sessionStorage.setItem('chat', JSON.stringify({
  step: currentStep,
  answers: answers
}))

// 진입 시 복원, 대화 전체를 다시 그림
const saved = sessionStorage.getItem('chat')
```

복원할 때 모션 없이 즉시 표시합니다. 저장된 대화를 애니메이션으로 다시 재생하면 기다림이 반복됩니다.

### 14.9 무료 결과

#### 대화에서 결과로 전환

입력이 끝나면 대화창 마지막에 전환 버튼이 나옵니다.

```
┌──────────────────────────────┐
│  [합격이] ┌──────────────────┐ │
│        │ 다 됐어요!         │ │
│        │ 김민준님 결과를    │ │
│        │ 보여드릴게요.      │ │
│        └──────────────────┘ │
│                              │
│  ┌────────────────────────┐  │
│  │       결과 보기         │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

결과는 대화창 안에 말풍선으로 표시하지 않고 별도 화면으로 전환합니다. 이유는 세 가지입니다.

첫째, 카드 8개에 20문장이 넘습니다. 말풍선은 폭이 좁아 세로로 길게 늘어지고 스크롤이 두 배가 됩니다.

둘째, 7일 기운 흐름과 방식 궁합 차트가 말풍선 안에 들어가면 읽기 어렵습니다.

셋째, 공유 이미지를 캡처할 범위가 명확해야 합니다. 대화 흐름에서는 어디까지가 결과인지 애매해집니다.

전환된 결과 화면 상단에서 합격이가 말풍선으로 인사하므로 흐름은 이어집니다.

#### 구성

3.2의 구성을 따릅니다. 카드 뒤에 전체 잠금 목록과 CTA를 배치합니다.

목록 항목은 3.4를 참조합니다. 필기 9개, 면접 10개입니다.

```
[필기]
━━ 유료 리포트에서만 볼 수 있어요 ━━

🔒 잠재력 발휘 지수
   실력 대비 몇 퍼센트가 나오는 날인지

🔒 내 사주가 말하는 시험 패턴
   십신으로 본 반복되는 패턴

🔒 놓치기 쉬운 3가지
   사주에서 나오는 취약점

🔒 시험 전날 상세 타임라인
   18시부터 취침까지 시간 단위로

🔒 남은 기간 어떻게 쓸까
   남은 날짜를 어떻게 배분할지

🔒 좌석과 방위
   어느 자리에 앉아야 하는지

🔒 이 기간 피해야 할 것
   먹는 것, 만나는 사람, 하지 말 것

🔒 2026년 시험운 캘린더
   올해 어느 달이 유리한지

🔒 내 사주로 본 시험 전략
   어떤 시험을 봐도 반복되는 패턴

┌────────────────────────────┐
│    3,900원으로 전체 보기     │
└────────────────────────────┘

[면접]
━━ 실제 면접 보는 기업을 알려주시면 ━━

🔒 기업 설립일로 본 궁합
🔒 이 직무와 나
🔒 들어올 가능성이 높은 질문
🔒 잠재력 발휘 지수
🔒 내 사주가 말하는 면접 패턴
🔒 내가 조심해야 할 3가지
🔒 복장과 소지품
🔒 면접 전날 상세 타임라인
🔒 2026년 면접운 캘린더
🔒 내 사주로 본 면접 전략

┌────────────────────────────┐
│    3,900원으로 전체 보기     │
└────────────────────────────┘

[실기 · 오디션]
┌────────────────────────────┐
│ 실기와 오디션 상세 리포트는  │
│ 준비 중입니다.              │
│                            │
│      [알림 받기]            │
└────────────────────────────┘
```

부분 잠금 4곳에도 각각 [더 자세히 보기] 버튼을 둡니다. 전부 같은 결제 화면으로 연결됩니다. 어디서 읽다가 멈춰도 결제 경로가 있어야 합니다.

### 14.10 유료 전 추가 입력 (면접 전용)

CTA 클릭 후 로그인 전에 배치합니다. 입력 구간이므로 대화형을 유지합니다.

```
┌──────────────────────────────┐
│  ←                           │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 어느 기업 면접이   │ │
│        │ 에요?             │ │
│        └──────────────────┘ │
│                              │
│  ┌────────────────────────┐  │
│  │                        │  │
│  └────────────────────────┘  │
│           [확인]              │
│                              │
│              ┌────────────┐  │
│              │ 삼성전자    │  │
│              └────────────┘  │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 직무는 반도체 공정 │ │
│        │ 기술 맞으시죠?     │ │
│        └──────────────────┘ │
│                              │
│  ┌────────────────────────┐  │
│  │    네, 맞아요           │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │    수정할게요           │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

무료 단계에서 직무명을 입력한 경우 확인만 받습니다. 건너뛴 경우에는 입력을 요청합니다.

기업 설립일은 묻지 않습니다. 지원자가 법인 설립일을 알 가능성이 낮고, 물어보면 요구가 많은 서비스라는 인상을 줍니다.

### 14.11 유료 리포트

```
┌──────────────────────────────┐
│  ←   시험 전 7일 상세 플랜    │
│                              │
│  김민준님 · 국가직 9급        │
│  2026년 4월 11일             │
│                              │
│  [섹션 1] 내 사주 명식        │
│  [섹션 2] 나의 학습 유형      │
│  ...                         │
│  [섹션 11] 시험 이후          │
│                              │
│  ┌────────────────────────┐  │
│  │ 카카오톡으로 나에게 보내기│  │
│  └────────────────────────┘  │
│                              │
│  이 서비스는 사주 명리 해석에  │
│  기반한 참고 자료이며...       │
├──────────────────────────────┤
│    홈       새 시험   마이페이지│
└──────────────────────────────┘
```

"다른 시험 추가하기" 버튼은 두지 않습니다. 탭바의 새 시험과 중복입니다.

생성에는 90초에서 130초가 걸립니다. 분량 축소 전 실측은 필기 217초, 면접 308초였으며, 섹션별 분량 상한을 추가해 목표를 필기 90초, 면접 130초로 잡았습니다.

이 시간 동안 합격이가 말풍선으로 진행 상황을 알립니다. 빈 화면이나 스피너만 두면 이탈합니다. 21.11의 문구 9개를 필기는 10초, 면접은 14초 간격으로 표시하고, 8초와 18초 지점에 계산이 끝난 명식과 오행 분포를 실제 컴포넌트로 노출합니다.

스트리밍이 필수입니다. 비스트리밍으로는 타임아웃에 걸립니다. 클라이언트 타임아웃은 240초로 두고, 초과하면 14.12의 실패 화면으로 전환합니다.

```
┌──────────────────────────────┐
│  [합격이] ┌──────────────────┐ │
│        │ 잠깐만요, 사주를   │ │
│        │ 보고 있어요        │ │
│        └──────────────────┘ │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 삼성전자 설립일을  │ │
│        │ 확인하는 중이에요  │ │
│        └──────────────────┘ │
│                              │
│  [합격이] ┌──────────────────┐ │
│        │ 거의 다 됐어요!    │ │
│        └──────────────────┘ │
└──────────────────────────────┘
```

메시지를 3-4초 간격으로 순차 표시합니다. 실제로 AI가 동작하는 중이므로 내용이 사실과 어긋나지 않습니다. 필기와 면접에 따라 문구가 다릅니다.

### 14.12 생성 중 이탈 처리

생성에 90초에서 130초가 걸리므로 사용자가 브라우저를 닫거나 다른 앱으로 이동할 수 있습니다. 결제한 사용자이므로 이 경우에도 결과물을 받을 수 있어야 합니다.

#### 서버 완결 구조

클라이언트 연결이 끊겨도 생성이 완료되고 저장되어야 합니다.

```
결제 완료
   |
reports 레코드 생성 (status: pending, started_at 기록)
   |
API Route가 생성을 끝까지 수행
   | (사용자가 나가도 서버는 계속 실행)
완료 시 content 저장, status: completed
   |
사용자가 다시 들어오면 완성된 리포트 표시
```

클라이언트는 상태만 확인하며, 생성 자체를 클라이언트가 붙잡고 있는 구조로 만들지 않습니다.

#### 재진입 처리

/report/[id] 진입 시 status로 분기합니다.

| status | 화면 |
|---|---|
| completed | 리포트 표시 |
| pending | 대기 화면 + 5초 폴링 |
| failed | 재시도 화면 (14.13) |
| refunded | 접근 차단 |

pending인 동안 5초마다 상태를 확인해, 완성되면 자동으로 리포트로 전환합니다. 나갔다 들어와도 대기 화면이 이어집니다.

#### 좀비 레코드 처리

서버가 중단되면 pending인 채로 남습니다. started_at으로 판별합니다.

```typescript
if (report.status === 'pending') {
  const elapsed = Date.now() - new Date(report.started_at).getTime()
  if (elapsed > 10 * 60 * 1000) {
    // 10분 초과, 실패로 간주하고 재시도 버튼 표시
  }
}
```

#### 이탈 경고와 안내

생성 중 beforeunload 경고를 띄웁니다. 다만 모바일에서는 동작하지 않는 경우가 많으므로 화면 안내가 더 중요합니다.

```
┌──────────────────────────────┐
│  [합격이] 리포트를 만들고      │
│          있어요               │
│                              │
│  약 2분 정도 걸려요.           │
│  이 화면을 닫으셔도            │
│  완성되면 계속 볼 수 있어요.    │
│                              │
│  [명식 표]                    │
│  [오행 분포]                  │
└──────────────────────────────┘
```

이 문구가 없으면 사용자가 나가면 결제 금액을 잃는다고 생각해 억지로 기다립니다.

#### 마이페이지 표시

생성 중인 리포트를 목록에 표시합니다.

```
┌────────────────────────┐
│ 국가직 9급 공무원        │
│ 만들고 있어요...         │
└────────────────────────┘
```

여기서도 폴링해 완성 시 자동 갱신합니다.

### 14.13 리포트 생성 실패 화면

```
┌──────────────────────────────┐
│      [합격이 char-02]           │
│                              │
│   리포트 생성에 실패했어요      │
│                              │
│   결제는 정상 처리되었습니다.   │
│   아래 버튼으로 다시 시도하거나 │
│   문의를 남겨주세요.           │
│                              │
│  ┌────────────────────────┐  │
│  │      다시 시도하기      │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │      문의하기           │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

재시도는 이미 결제된 건이므로 추가 과금 없이 AI를 다시 호출합니다. retry_count가 3을 넘으면 재시도 버튼을 숨기고 문의만 안내합니다.

이 화면이 있으면 관리자가 개입할 일이 크게 줄어듭니다.

### 14.14 마이페이지 (로그인)

```
┌──────────────────────────────┐
│  마이페이지                   │
│                              │
│  김민준                       │
│  1990년 5월 15일 14:30       │
│  카카오 계정                  │
│                              │
├──────────────────────────────┤
│  내 리포트                    │
│                              │
│  ┌────────────────────────┐  │
│  │ 국가직 9급 7일 플랜      │  │
│  │ 2026.04.11 · D-15      │  │
│  │ 운 지수 78              │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │ 서울시 9급 (무료)        │  │
│  │ 2026.06.13 · D-78      │  │
│  │           [리포트 보기] │  │
│  └────────────────────────┘  │
│                              │
├──────────────────────────────┤
│  내 정보 수정              >  │
│  결제 내역                 >  │
├──────────────────────────────┤
│  문의하기                  >  │
│  자주 묻는 질문             >  │
├──────────────────────────────┤
│  알림 설정                 >  │
│  이용약관                  >  │
│  개인정보 처리방침          >  │
├──────────────────────────────┤
│  로그아웃                     │
│  회원 탈퇴                    │
├──────────────────────────────┤
│    홈       새 시험   마이페이지│
└──────────────────────────────┘
```

무료로 본 결과도 함께 표시합니다. 결제하지 않은 사용자에게도 볼 내용이 있어야 하고, 무료 결과 옆에 결제 유도 버튼이 붙습니다.

### 14.15 마이페이지 (비로그인)

```
┌──────────────────────────────┐
│  마이페이지                   │
│                              │
│        [합격이 char-03]         │
│                              │
│    로그인하고 결과를 저장하세요 │
│    다음에 입력 없이 볼 수 있어요│
│                              │
│  ┌────────────────────────┐  │
│  │      로그인하기         │  │
│  └────────────────────────┘  │
│                              │
├──────────────────────────────┤
│  문의하기                  >  │
│  자주 묻는 질문             >  │
│  이용약관                  >  │
│  개인정보 처리방침          >  │
├──────────────────────────────┤
│    홈       새 시험   마이페이지│
└──────────────────────────────┘
```

빈 상태가 로그인 유도 지점이 됩니다.

### 14.16 결제 내역

경로 /my/payments

```
┌──────────────────────────────┐
│  ←   결제 내역                │
│                              │
│  ┌────────────────────────┐  │
│  │ 시험 전 7일 상세 플랜    │  │
│  │ 국가직 9급 공무원        │  │
│  │                        │  │
│  │ 2026.08.20  3,900원    │  │
│  │ 카카오페이               │  │
│  │                        │  │
│  │        [리포트 보기]    │  │
│  └────────────────────────┘  │
│                              │
│  결제 관련 문의는            │
│  문의하기를 이용해 주세요.     │
└──────────────────────────────┘
```

### 14.17 문의하기

경로 /my/inquiry

```
┌──────────────────────────────┐
│  ←   문의하기                 │
│                              │
│  어떤 문의인가요?             │
│  ┌────────────────────────┐  │
│  │ 결제 · 환불             │  │
│  └────────────────────────┘  │
│   결제 · 환불                 │
│   리포트 내용                 │
│   계정 · 로그인               │
│   오류 신고                   │
│   기타                       │
│                              │
│  내용                        │
│  ┌────────────────────────┐  │
│  │                        │  │
│  └────────────────────────┘  │
│                              │
│  답변받을 이메일              │
│  ┌────────────────────────┐  │
│  │ minjun@example.com     │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │       보내기            │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

비로그인 상태에서도 문의할 수 있어야 합니다. 결제 전에 궁금한 점이 생길 수 있습니다.

문의가 등록되면 Supabase Database Webhook으로 Slack에 알림을 보냅니다.

### 14.18 자주 묻는 질문

경로 /my/faq

문의를 줄이는 장치입니다. 초기 문항은 다음과 같습니다.

```
결제했는데 리포트가 안 보여요
태어난 시간을 모르면 결과가 부정확한가요?
환불이 되나요?
같은 시험을 다시 볼 수 있나요?
결과가 매일 바뀌는 이유가 뭔가요?
사주 계산은 어떤 기준인가요?
```

마지막 항목이 중요합니다. 자시 처리와 경도 보정 방식을 밝혀두면 다른 만세력과 결과가 다르다는 문의를 미리 줄일 수 있습니다.

```
사주 계산은 어떤 기준인가요?

절기를 기준으로 월주를 정하고, 한국 표준시와 실제 경도
차이를 반영해 출생 시각에서 30분을 조정합니다. 밤 11시
이후 출생은 당일 기준으로 계산합니다. 서머타임 시행
기간에는 1시간을 추가 조정합니다.

계산 방식에 따라 다른 곳과 결과가 다를 수 있습니다.
```


## 15. 기술 스택

```
프레임워크    Next.js 15 (App Router)
언어          TypeScript
스타일        Tailwind CSS
폰트          Pretendard (next/font로 self-host)
아이콘        lucide-react
모션          framer-motion
배포          Vercel Pro
DB / Auth     Supabase
AI            Anthropic API (claude-sonnet-5)
검색          Serper
이미지 생성   html-to-image
결제          포트원 또는 토스페이먼츠
알림          Slack Incoming Webhook (문의, 장애)

관리자 전용
테이블        @tanstack/react-table
차트          recharts
```

### 15.1 Vercel Pro가 필요한 이유

리포트 생성에 78초에서 100초가 걸립니다. Hobby 플랜은 서버리스 함수 실행이 60초로 제한되므로 결제한 사용자가 리포트를 받지 못합니다.

maxDuration 상한은 플랜마다 다릅니다. Hobby는 60초, 그 위는 300초입니다. 상한을 넘는 값을 주면 배포 자체가 실패합니다.

```json
{
  "regions": ["icn1"],
  "functions": {
    "app/api/report/route.ts": { "maxDuration": 500 }
  }
}
```

300초로 둡니다. 실측 소요는 필기 144초, 면접 173초이고 가장 오래 걸린 건이 223초입니다. 클라이언트 타임아웃(240초)이 먼저 걸리므로 사용자 대기 시간은 그 안에서 통제됩니다.

Hobby(60초)에서는 생성이 중간에 죽어 리포트가 pending으로 남습니다. 유료 리포트를 여시려면 플랜을 올려야 합니다.

월 비용은 결제 100건 기준 매출의 8퍼센트 수준입니다. 결제가 늘면 비율이 내려갑니다.

### 15.2 리전 설정

Supabase 리전은 서울(Northeast Asia)을 권합니다. 리전은 프로젝트 생성 후 변경할 수 없으므로 초기에 결정해야 합니다.

Vercel 함수 리전도 같은 지역(icn1)으로 맞춥니다. 서버와 DB가 가까운 것이 사용자와 가까운 것보다 응답 속도에 크게 작용합니다.

Supabase를 다른 리전에 만든 경우 Vercel 리전도 그쪽으로 맞추는 편이 낫습니다.

### 15.3 검색 API

Serper를 사용합니다.

```
무료      2,500 크레딧, 신용카드 불필요
유료      $50에 50,000 크레딧, 6개월 만료
1크레딧    검색 1회 (결과 10개까지)
```

면접 리포트에만 검색이 발생하므로 무료 2,500 크레딧이면 면접 결제 1,250건까지 커버합니다. 초기에는 무료로 시작하고, 소진 후 결제합니다.

크레딧이 6개월 만료이므로 트래픽이 검증되기 전에 미리 결제하면 대부분을 사용하지 못합니다.

num 파라미터를 10 이하로 고정합니다. 11 이상 요청하면 1회당 2크레딧이 소모됩니다.

---

## 16. 파일 구조

```
/app
  page.tsx                    홈 (탭 1)
  start/page.tsx              입력 (탭 2)
  my/
    page.tsx                  마이페이지 (탭 3)
    profile/page.tsx          내 정보 수정
    payments/page.tsx         결제 내역
    inquiry/page.tsx          문의하기
    faq/page.tsx              자주 묻는 질문
    settings/page.tsx         알림 설정
  result/page.tsx             무료 결과
  report/[id]/page.tsx        유료 리포트
  login/page.tsx              로그인
  privacy/page.tsx
  terms/page.tsx
  admin/
    page.tsx                  대시보드
    reports/page.tsx          리포트 관리
    payments/page.tsx         결제 관리
    users/page.tsx            회원 관리
    inquiries/page.tsx        문의 관리
    grant/page.tsx            무료 지급, 쿠폰
    stats/page.tsx            지표
  api/
    report/route.ts           유료 리포트 생성
    report/retry/route.ts     사용자 재시도
    payment/route.ts          결제 콜백
    inquiry/route.ts          문의 등록
    admin/
      report/regenerate/route.ts
      payment/refund/route.ts
      grant/route.ts
      coupon/route.ts
      user/[id]/route.ts
      inquiry/[id]/reply/route.ts
      notice/route.ts

middleware.ts                 관리자 접근 제어

/lib
  admin/
    auth.ts                   requireAdmin
    pg.ts                     PG 취소 API
  saju/
    solarterms.json           절기 테이블 (사전 생성)
    constants.ts              60갑자, 오행 매핑, 가중치
    calculate.ts              4기둥 계산 (기업 3기둥 포함)
    elements.ts               오행 분포 산출
    fortune.ts                운 지수, 7일 흐름, 월별, 방식 궁합, 발휘 지수
    shipsin.ts                십신 계산
    compatibility.ts          기업 궁합 계산
    particle.ts               한글 조사 처리
  content/
    fragments.json            무료 문장 조각 222개
    paid-fragments.json       유료 문장 조각 25개
    preset-exams.json         대분류 10개, 시험명 73개
    quotes.json               응원 문구 17개
    chat-scripts.ts           대화 질문 문구 39개
    characters.ts             캐릭터, 유형 뱃지 정의
  supabase/
    client.ts
    server.ts
  ai/
    prompt.ts                 프롬프트 구성
    spec.ts                   D-day 구간별 섹션 정의
    generate.ts               API 호출 및 파싱
    search.ts                 검색 API 호출

/components
  landing/
    Hero.tsx                  애니메이션 캐릭터
    QuoteBox.tsx              응원 문구
    DiffCards.tsx
    Preview.tsx
    UserBlock.tsx             로그인 시 개인화 블록
  chat/
    ChatThread.tsx            대화 컨테이너, 자동 스크롤
    BotBubble.tsx             합격이 말풍선 + 아바타
    UserBubble.tsx            사용자 답변 말풍선
    OptionButtons.tsx         선택지 버튼 (1열 / 2열)
    DatePickerWidget.tsx      달력
    TimePickerWidget.tsx      시간 + 모름
    TextInputWidget.tsx       텍스트 입력
    GeneratingChat.tsx        리포트 생성 중 말풍선
  result/
    CharacterDisplay.tsx      캐릭터 + 말풍선
    TypeBadge.tsx             유형 뱃지
    TypeModal.tsx             유형 설명 모달
    ScorePair.tsx             당일 운 + 오늘의 운
    ResultCard.tsx
    WeekFlowChart.tsx
    MethodFitChart.tsx
    ShareCard.tsx             1080x1920
    TypeShareCard.tsx         1080x1080
    PartialLock.tsx           카드 내 부분 잠금
    LockedList.tsx            유료 전용 항목 목록
    LockedCTA.tsx
  report/
    ReportSection.tsx
    SajuTable.tsx             명식 표
    ElementBar.tsx            오행 분포 막대
    MonthCalendar.tsx         연간 시험운
    KakaoShareButton.tsx
    FailedState.tsx           생성 실패, 재시도
  my/
    AccountHeader.tsx
    ReportList.tsx
    MenuList.tsx
    EmptyState.tsx            비로그인 빈 상태
  layout/
    TabBar.tsx                하단 탭 3개
    NoticeBanner.tsx          공지 배너
  admin/
    AdminLayout.tsx
    AdminSidebar.tsx
    DataTable.tsx
    StatCard.tsx
    RefundButton.tsx
    RegenerateButton.tsx
    GeneratingState.tsx       생성 중 표시

/public
  character/
    char-01.png ~ char-05.png
    char-profile.png
    hero-body.png             애니메이션용 몸통
    hero-arm.png              애니메이션용 팔

/styles
  tokens.css

/scripts
  gen-solarterms.py           절기 테이블 생성 (1회)
```

---

## 17. 개발 순서

### Phase 1: 계산 코어

```
1. gen-solarterms.py 실행하여 절기 테이블 생성
2. lib/saju/constants.ts 상수 정의
3. lib/saju/calculate.ts 4기둥 계산 + 단위 테스트
4. lib/saju/elements.ts 오행 분포
5. lib/saju/fortune.ts 운 지수, 7일 흐름, 월별, 방식 궁합, 발휘 지수
6. lib/saju/shipsin.ts 십신 계산 (PRD 5.6)
7. lib/saju/compatibility.ts 기업 궁합
```

검증 방법: 알려진 사주 사례 10건을 넣어 기존 만세력 서비스 결과와 대조합니다. 다음 사례를 반드시 포함합니다.

```
1.  2026-02-03 12:00        입춘 전날, 전년도 처리
2.  2026-02-04 05:00        입춘 직전 (KST 원본 비교)
3.  2026-02-04 05:05        입춘 직후
4.  1995-06-15 22:59        보정 후 22:29, 해시
5.  1995-06-15 23:31        보정 후 23:01, 자시
6.  1995-06-16 00:29        보정 후 23:59, 자시, 일주는 6월 16일
7.  1988-06-15 14:30        서머타임, 보정 후 13:00
8.  1990-05-15 14:30        일반
9.  1990-05-15 (시간 없음)   3기둥
10. 1969-01-13 (시간 없음)  기업 3기둥
```

6번이 중요합니다. 조자시 방식이므로 00:29 출생은 보정 후 23:59가 되어도 일주가 6월 16일이어야 합니다. 보정 때문에 날짜가 전날로 넘어가면 안 됩니다.

2번과 3번은 절기 비교에 보정을 적용하지 않는지 확인하는 사례입니다. 4.2의 순서를 지키면 원본 KST 시각으로 입춘과 비교하므로 05:00은 전년도, 05:05는 당년도가 나와야 합니다.

이 단계에서 검증을 건너뛰면 이후 전부를 다시 확인해야 합니다.

### Phase 2: 콘텐츠 데이터

```
7.  content/fragments.json 무료 222개 배치
8.  content/paid-fragments.json 유료 25개 배치
9.  content/preset-exams.json 대분류 10개, 시험명 73개 배치
10. content/quotes.json 응원 문구 17개 배치
11. content/chat-scripts.ts 대화 질문 문구 39개 배치
12. lib/saju/particle.ts 조사 처리
13. styles/tokens.css 디자인 토큰 적용
```

캐릭터 이미지는 별도 제작하여 /public/character/에 배치합니다. 21.13의 목록을 따릅니다. 준비되지 않은 단계에서는 플레이스홀더로 진행합니다.

hero-body.png와 hero-arm.png는 애니메이션용 분리 이미지입니다. 없으면 hero 전체 이미지로 표시하고 애니메이션 코드는 미리 작성해둡니다.

### Phase 3: 레이아웃과 대화형 입력

```
14. 하단 탭바 3개 (홈 / 새 시험 / 마이페이지)
15. 탭바 표시 규칙 적용
16. 홈 (비로그인 랜딩) + 응원 문구 랜덤 표시
17. 캐릭터 CSS 애니메이션 (21.6)
18. 대화 컴포넌트 (말풍선, 선택지, 위젯)
19. framer-motion 모션 적용
20. 방식별 대화 흐름 분기 (필기/면접/실기/오디션)
21. 대분류 10개, 자격증·어학 2단계 분기
22. 대학교 시험 기간 질문
23. 자동 스크롤, 키보드 대응
24. sessionStorage 상태 유지 및 복원
25. 답변 수정 (이전 말풍선 탭)
```

대화 컴포넌트가 입력의 전부이므로 이 단계가 이전보다 무겁습니다. 모션 값과 타이밍은 21.6에 정의되어 있습니다.

### Phase 4: 무료 결과

```
26. 대화에서 결과로 전환
27. 상단 요약 (캐릭터, 뱃지, 말풍선, 점수 2개 + 발휘 지수 잠금)
28. 카드 8개 (3.2 순서)
29. 카드 2에 SajuTable, ElementBar 표시
30. 7일 흐름 차트, 방식 궁합 차트
31. 부분 잠금 4곳 (PartialLock)
32. 전체 잠금 목록 (LockedList)
33. 유형 모달
34. 공유 이미지 생성 (스토리, 정사각형)
35. 응원 문구 표시
36. 방식별 CTA 영역
```

### Phase 5: 인증 및 마이페이지

```
37. Supabase 스키마 적용 (13.1 전체), RLS 설정
38. 카카오 / 구글 / 이메일 로그인
39. 마이페이지 (로그인 / 비로그인)
40. 내 정보 수정, 회원 탈퇴
41. 문의하기, 자주 묻는 질문
42. 홈 로그인 상태 개인화 블록
43. 공지 배너 표시
```

13.1의 DB 필드는 이 단계에서 전부 반영합니다. reports의 status, retry_count, 토큰 필드와 payments 테이블을 나중에 추가하면 기존 데이터에 값이 없어 처리가 번거로워집니다.

### Phase 6: 유료

```
44. 결제 CTA 및 더미 결제 흐름
45. 유료 전 추가 입력 (면접, 대화형)
46. 가격 테스트 계측
47. 검색 API 연동 (면접만)
48. AI 프롬프트 및 리포트 생성 API
49. D-day 구간별 구성 분기
50. 리포트 화면, 생성 중 대화 표시 (문구 9개 + 명식/오행)
51. 생성 실패 화면 및 사용자 재시도
51-1. 생성 중 이탈 처리 (14.12)
      서버 완결 구조, 재진입 폴링, 좀비 레코드 판별
52. 카카오 나에게 보내기
53. 결제 내역 화면
54. 실제 PG 연동, 쿠폰 처리
```

### Phase 7: 관리자

```
55. 미들웨어 접근 제어, 레이아웃
56. 대시보드
57. 리포트 관리 + 재생성 API
58. 결제 관리 + 환불 API
59. 회원 관리
60. 문의 관리 + Slack 알림
61. 무료 지급, 쿠폰 발급
62. 지표
```

22.19의 우선순위에 따라 나눠서 진행해도 됩니다. 14.12의 사용자 재시도 버튼이 있으면 41번의 시급성이 낮아집니다.

---

## 18. 콘텐츠 작성 규칙

문장 조각 177개(무료 167 + 유료 10)와 AI 프롬프트에 공통으로 적용합니다.

### 18.1 표현 금지

```
합격을 보장하거나 암시하는 표현
  금지: 이렇게 하면 붙습니다 / 합격이 보장된 날입니다

결과를 단정하는 표현
  금지: 이 시험은 떨어집니다 / 운이 나쁜 날입니다

최상급 수식
  금지: 가장 강력한 / 매우 효과적인 / 훨씬 유리합니다

검색 결과의 단정
  금지: OO기업 2차 면접은 PT입니다
  사용: 후기에서 PT 형식이 언급되는 경우가 많습니다

기업 평판
  금지: 특정 기업의 부정적 평판 서술
```

### 18.2 낮은 점수 처리

점수가 낮게 나온 경우에도 불안을 키우지 않고 행동으로 마무리합니다.

```
사용 예시
시험일 기운은 밀어주는 쪽은 아닙니다. 다만 이런 날은
컨디션이 아니라 준비량이 결과를 만드는 날이라, 평소보다
30분 일찍 도착해서 몸을 먼저 적응시키면 차이가 거의
없어집니다.
```

### 18.3 톤 분리

```
말풍선     캐릭터 톤, 구어체, 한 줄
카드 본문   전문가 톤, 격식체, 5-6문장
유형 설명   중간 톤
유료 리포트 전문가 톤
```

본문까지 구어체로 작성하면 가벼워 보여 결제로 이어지기 어렵습니다. 캐릭터는 인상을 부드럽게 만드는 역할이고, 내용의 신뢰는 본문이 담당합니다.

### 18.4 오락 목적 고지

무료 결과와 유료 리포트 하단에 고정 노출합니다.

```
이 서비스는 사주 명리 해석에 기반한 참고 자료이며,
시험 결과를 예측하거나 보장하지 않습니다.
```

---

## 19. 검증 지표

```
1. 랜딩 이탈률       시작하기 클릭 비율
2. 입력 완주율       단계별 이탈 지점
3. 공유 버튼 클릭률   바이럴 가능성
4. CTA 클릭률        결제 화면 진입 비율
5. 결제 전환율       CTA 클릭 대비 실제 결제
6. 재방문율          시험일 입력자의 D-day 경과 재방문
7. 재결제율          마이페이지에서 시험 추가
8. 방식별 분포       필기 / 면접 / 실기 / 오디션 비율
```

4번과 5번을 분리해서 봐야 합니다. CTA 클릭이 낮으면 무료 결과의 품질 또는 CTA 문구 문제이고, CTA 클릭은 높은데 결제가 낮으면 가격 또는 결제 흐름 문제입니다.

8번은 실기와 오디션 유료 상품 개발 여부를 판단하는 근거가 됩니다.

이 지표들은 22.12의 관리자 지표 화면에서 확인합니다.

---

## 20. 향후 확장

1차 출시 이후 검토할 항목입니다.

```
1.  친구와 궁합 보기 (같은 시험 응시자 간 비교)
2.  여러 기업 궁합 비교 (지원한 5개 기업 중 나와 맞는 곳)
3.  캐릭터 표정 세분화, 시즌 한정 스킨
4.  시험 DB 구축 및 자동완성 (날짜, 시작시간 자동 입력)
5.  기업 DB 구축 (설립일 검색 실패율이 높을 경우)
6.  같은 시험 응시자 대비 운 지수 백분위 표시
7.  실기 유료 상품
8.  시험명 기반 제휴 광고 (인강, 교재, 스터디카페)
9.  커머스 연결 (행운의 색 기반 문구, 액세서리 추천)
10. 웹푸시 알림 (D-7, D-1, 당일 아침)
11. 유형 설명에 이용자 분포 노출
12. 문장 조각을 DB로 이전, 관리 화면에서 수정 (22.17)
13. 관리자 권한 세분화 (현재는 이메일 화이트리스트 단일 권한)
```

8번과 9번이 실제 수익의 중심입니다. 시험명을 사용자가 직접 입력하므로 광고 플랫폼보다 타겟팅 정확도가 높습니다. 3,900원 결제 데이터는 제휴 협상에서 지갑을 여는 트래픽이라는 증명으로 사용됩니다.

2번은 재결제 요인이 큽니다. 취준생이 여러 기업에 지원하므로 실제 수요가 있습니다.

---

## 21. 디자인 시스템

### 21.1 방향

캐릭터를 화면의 주인공으로 두고, 크고 둥근 카드가 떠 있는 구조를 사용합니다. 브랜드 색상 하나로 화면 전체를 통일하고, 오행 색상은 유형 뱃지와 공유 이미지에만 제한적으로 사용합니다.

### 21.2 색상 토큰

```
브랜드 (화면 전체)
  --bg            #E8F0FF   연한 브랜드 틴트
  --surface       #FFFFFF   카드
  --primary       #4B7BF5   브랜드
  --button        #0F1729   주 버튼
  --text          #1A1D26   본문
  --text-sub      #7A8394   보조 텍스트
  --border        #E3E8F2   구분선

오행 (뱃지, 공유 이미지 전용)
  --wood          #4CAF7D
  --fire          #E5533D
  --earth         #C9A227
  --metal         #8E9AAF
  --water         #3D5AE5

상태
  --score-high    #3DAF6E   80 이상
  --score-mid     #4B7BF5   50-79
  --score-low     #E5A03D   50 미만
```

낮은 점수에도 붉은 경고색을 사용하지 않습니다. 불안을 키우지 않기 위해 주황 계열까지만 사용합니다.

### 21.3 타이포그래피

폰트 Pretendard (next/font로 self-host)

self-host가 필요한 이유는 두 가지입니다. 첫째, 공유 이미지 캡처 시 외부 폰트가 로드되지 않아 글자가 깨집니다. 둘째, 초기 로딩 속도가 개선됩니다.

```
헤드라인    700 / 22px / 행간 1.4
카드 제목    600 / 17px / 행간 1.5
본문        400 / 15px / 행간 1.7
라벨        500 / 13px / 행간 1.4
숫자 강조    700 / 32px / 행간 1.2
```

한글 본문은 행간 1.7 이상을 유지합니다. 카드 하나에 5-6문장이 들어가므로 행간이 좁으면 읽기 어렵습니다.

### 21.4 레이아웃 토큰

```
모서리
  카드        24px
  버튼        16px
  칩 · 뱃지    20px
  원형 칩      999px

간격
  화면 좌우 패딩   20px
  카드 사이       12px
  카드 안쪽 패딩   20px
  섹션 사이       28px

그림자
  카드   0 2px 12px rgba(26, 29, 38, 0.06)
  버튼   0 4px 16px rgba(15, 23, 41, 0.16)
```

### 21.5 캐릭터 사양

캐릭터 이름은 합격이입니다. 확정된 디자인은 다음과 같습니다.

```
색상      연한 하늘색 (#8ECDF5 계열)
재질      3D 무광 렌더링, 부드러운 표면
형태      둥근 서양배 형태, 아래가 넓고 위가 좁음
머리      머리 위에 짧고 뾰족한 뿔 두 개 (좌우)
눈        검은색 구체, 광택 있음, 흰색 하이라이트 없음
입        아래로 볼록한 곡선 미소, 진한 파랑
팔        짧고 뭉툭, 손가락 4개 구분됨
다리      짧고 뭉툭한 두 다리
목        없음, 머리와 몸이 하나로 이어짐
코        없음
눈썹      없음
```

눈썹이 없으므로 표정 변화는 눈 모양, 입 모양, 자세, 소품으로 만듭니다.

#### 파일 사양

```
생성      1024 x 1024 PNG, 투명 배경
색공간    sRGB
경로      /public/character/
용량      장당 500KB 이하 권장

캔버스 배치
  상하좌우 여백 확보
  뿔이 잘리지 않도록 상단 여백을 넉넉히
  말풍선은 CSS로 겹치므로 상단 여백 추가 확보 불필요

표시 크기
  결과 화면      200 - 240px
  홈 히어로      280 - 320px
  대화창 아바타   30px
  마이페이지     64px
```

64px까지 축소되므로 세부 디테일이 많으면 형태가 뭉개집니다. 실루엣과 명암 대비가 뚜렷해야 합니다.

#### 브랜드 색상과의 관계

21.2의 브랜드 색상(#4B7BF5)은 UI에 사용하고, 캐릭터는 더 밝은 하늘색을 유지합니다. UI가 진하고 캐릭터가 밝으면 화면에서 캐릭터가 돋보입니다.

실제 배치 후 대비가 부족하면 배경 틴트(#E8F0FF)를 조정합니다.

### 21.6 캐릭터 애니메이션

홈 히어로의 캐릭터를 움직이게 합니다. GIF 대신 CSS 애니메이션을 사용합니다.

GIF는 알파 채널이 없어 투명 배경 가장자리가 계단처럼 각집니다. 3D 렌더링 캐릭터는 가장자리가 부드러운데 GIF로 만들면 그것이 깨집니다. 그리고 1024px 기준으로 2~3MB가 되어 첫 화면 로딩이 늦어집니다.

#### 이미지 분리

```
hero-body.png   흔드는 팔을 제외한 전신
                팔이 붙어 있던 자리는 어깨선으로 마감
hero-arm.png    흔드는 팔만
                어깨 관절이 캔버스 하단 중앙에 오도록 배치
```

#### CSS

```css
.hero-arm {
  position: absolute;
  transform-origin: bottom center;
  animation: wave 1.4s ease-in-out infinite;
}

@keyframes wave {
  0%, 100% { transform: rotate(0deg); }
  25%      { transform: rotate(-14deg); }
  75%      { transform: rotate(14deg); }
}

.hero-body {
  animation: breathe 3s ease-in-out infinite;
}

@keyframes breathe {
  0%, 100% { transform: translateY(0) scale(1); }
  50%      { transform: translateY(-4px) scale(1.01); }
}

@media (prefers-reduced-motion: reduce) {
  .hero-arm, .hero-body { animation: none; }
}
```

char-05의 반짝임도 별도 파일로 분리하면 같은 방식으로 깜빡이게 할 수 있습니다.

```css
.sparkle {
  animation: twinkle 1.8s ease-in-out infinite;
}
@keyframes twinkle {
  0%, 100% { opacity: 0.4; transform: scale(0.9); }
  50%      { opacity: 1;   transform: scale(1.1); }
}
```

용량이 늘지 않고 투명 배경이 유지되며 어느 크기에서도 선명합니다.

더 복잡한 동작이 필요해지면 Lottie를 검토합니다. 다만 3D 렌더링 캐릭터를 Lottie로 만들려면 벡터로 다시 그려야 하므로 작업량이 큽니다.

### 21.7 이미지 제작 사양

표정 변화는 눈 모양, 입 모양, 자세, 소품으로 만듭니다. 눈썹이 없는 디자인이므로 눈썹 각도로 감정을 표현할 수 없습니다.

#### char-03 (기준형, 운 지수 50-64)

가장 먼저 제작합니다. 이 결과물이 나머지 이미지의 기준이 됩니다.

```
자세    정면, 양팔을 몸 옆에 자연스럽게 내림
눈      기본 검은 구체, 정면 응시
입      작은 곡선 미소, 원본보다 작게
소품    없음
분위기  차분하고 중립적
```

#### char-04 (운 지수 65-79) 및 hero

원본 캐릭터 이미지가 손을 흔드는 자세이므로 그대로 사용합니다. 홈 히어로와 동일 파일로 씁니다.

별도 제작하는 경우:

```
자세    한쪽 팔을 들어 인사
눈      기본 검은 구체
입      확실한 미소
소품    볼에 옅은 분홍 홍조, 노란 반짝임 1-2개
분위기  기분 좋음, 환영
```

#### char-05 (운 지수 80-100)

```
자세    양팔을 위로 들어올린 만세
눈      아래로 볼록한 초승달 모양 (웃는 눈), 검은색 유지
입      크게 벌린 미소, 입안이 보임
소품    볼에 분홍 홍조, 노란 반짝임 3-4개
분위기  신남, 기쁨
```

눈을 초승달로 바꾸는 것이 핵심입니다. 검은 구체를 유지한 채 자세만 바꾸면 표정 차이가 나타나지 않습니다.

#### char-02 (운 지수 35-49)

```
자세    양팔을 앞으로 모아 살짝 움츠린 자세
눈      아래쪽이 살짝 처진 타원형, 검은색 유지
입      작게 오므린 입, 살짝 물결 모양
소품    머리 옆에 작은 땀방울 하나
분위기  걱정되지만 괜찮음

금지    눈물, 우는 표정, 크게 꺾인 입
```

#### char-01 (운 지수 0-34)

```
자세    한쪽 주먹을 가슴 앞으로 들어올린 파이팅 자세
눈      위쪽이 살짝 각진 또렷한 눈, 검은색 유지
입      작게 다문 일자 또는 살짝 벌려 외치는 형태
소품    없음
분위기  결의, 함께 힘내자는 응원

금지    슬픈 표정, 눈물, 축 처진 자세
```

가장 중요한 이미지입니다. 낮은 점수를 받은 사용자가 보는 화면이므로 슬픈 표정이면 공유하지 않고 서비스 인상도 나빠집니다.

#### char-profile (대화창 아바타)

```
표정    char-03과 동일 (차분한 미소)
구도    머리와 상반신만, 얼굴이 화면 대부분을 차지
        어깨 아래에서 크롭
        뿔이 잘리지 않도록 상단 여백 확보
        원형 크롭을 전제로 구성
        여백 최소, 캔버스를 꽉 채움
저장    512 x 512 PNG, 투명 배경
```

전신 이미지를 30px로 축소하면 얼굴이 10px 미만이 되어 형태를 알아볼 수 없습니다. 얼굴을 크게 잡은 별도 파일이 필요합니다.

크롭 처리는 CSS에서 수행합니다.

```css
.avatar {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  object-fit: cover;
  background: #8ECDF5;
}
```

#### 제작 순서

```
1. char-03        기준형
2. char-05        char-03을 기준으로
3. char-02        char-03을 기준으로
4. char-01        char-03을 기준으로
5. char-profile   char-03을 기준으로
```

#### 제작 후 확인

```
1. 배경이 실제로 투명한지
2. 1024 x 1024로 리사이즈
3. 64px로 축소해 형태가 유지되는지 (썸네일 크기)
4. 5장을 함께 놓고 동일 캐릭터로 보이는지
   몸통 비율, 뿔 크기, 눈 크기가 흔들리면 재작업
5. 뿔이 잘리지 않았는지
6. 이미지 압축
7. 지정 파일명으로 저장
```

### 21.8 말풍선 문구 (D-day 구간 5개)

```
D-30 이상   아직 시간이 있어요. 천천히 가요!
D-8 ~ D-29  슬슬 정리할 때가 왔어요
D-2 ~ D-7   이제 컨디션이 중요해요
D-1         오늘은 푹 쉬어요
D-DAY       지금까지 한 거 다 믿어요!
```

면접인 경우 "시험"을 "면접"으로 치환합니다.

### 21.9 유형 설명 문구 (뱃지 모달 5개)

```
목 · 흡수형
  새로운 내용을 빠르게 받아들이는 유형이에요. 처음 보는
  과목도 금방 감을 잡는 편입니다. 대신 한 과목을 오래
  파고드는 힘은 따로 만들어야 합니다.

화 · 몰입형
  몰입이 붙으면 끝까지 밀어붙이는 힘이 있어요. 대신 한 번
  흐트러지면 원래 속도로 돌아오는 데 시간이 걸리는 편입니다.

토 · 지구력형
  꾸준함이 강점인 유형이에요. 계획을 세우면 지키는 쪽이라
  긴 준비 기간이 필요한 시험에 잘 맞습니다.

금 · 정리형
  정리하고 요약하는 능력이 있어요. 원칙이 뚜렷한 문제에
  강한 대신, 예외적인 유형에서 당황하지 않는 훈련이
  필요합니다.

수 · 응용형
  이해가 빠르고 응용에 강한 유형이에요. 대신 반복 암기를
  지루해하는 경향이 있어 이 부분만 관리하면 됩니다.
```

### 21.10 접근성 최소 기준

```
- 모바일 우선, 최소 폭 320px 대응
- 키보드 포커스 표시 유지
- prefers-reduced-motion 존중
- 본문 텍스트 대비비 4.5:1 이상
- 점수를 색으로만 구분하지 않고 숫자를 항상 함께 표시
- 캐릭터 이미지에 alt 텍스트 제공
- 대화 선택지 버튼 최소 높이 44px 유지
```

### 21.11 대화 질문 문구

입력 단계별로 합격이가 던지는 질문입니다.

**빈 줄로 나뉜 덩어리 하나가 말풍선 하나입니다.** 전에는 한 단계가 말풍선 하나였고 그 안에 여러 줄이 들어갔습니다. 세 문장을 말하면 말풍선이 길어져 읽기 부담이 생깁니다. 순차로 뜨는 말풍선 세 개가 대화에 가깝습니다.

한 말풍선은 두 줄을 넘기지 않습니다. 질문과 그 질문에 딸린 안내는 한 말풍선에, 성격이 다른 말은 따로 띄웁니다.

#### 공통

```
대분류
  안녕하세요, 여러분의 합격을 응원하는 합격이예요.

  꼭 합격하시길 바라며,
  시험 보는 날의 운을 봐드릴게요.

  어떤 시험 준비하세요?

하위 그룹 (자격증·어학)
  자격증이신가요, 어학이신가요?

시험명 (대분류 선택 후)
  → 아래 "대분류별 공감 문구" 참조

시험 기간 (대학교 시험)
  시험이 며칠 동안 이어져요?

시험명 직접 입력
  시험 이름을 알려주세요.

방식 확인
  필기 시험 맞으시죠?

시험 날짜
  시험 날짜가 언제예요?

시작 시간
  몇 시에 시작해요?

생년월일
  이제 사주와 그날의 기운 흐름을 볼게요.
  생년월일이 어떻게 되세요?

태어난 시간
  태어난 시간도 아세요?
  모르셔도 결과는 나와요.

이름
  결과에 이름을 넣어드릴까요?

완료
  다 됐어요!
  {name}님 결과를 보여드릴게요.
```

이름을 건너뛴 경우 완료 문구는 "다 됐어요! 결과를 보여드릴게요."로 바꿉니다.

#### 대분류별 공감 문구

전에는 "{대분류}시군요!" 한 줄이었습니다. "공무원시군요"처럼 조사가 어색해지는 경우가 있고, 무엇보다 고른 것을 되읊는 것뿐이라 아무것도 더해주지 않습니다.

각 분류가 어떤 시간을 보내고 있는지 한 줄 알아주고 다음 질문으로 갑니다. **두 문장을 넘기지 않습니다.** 길어지면 위로가 아니라 서론이 됩니다.

```
수능 · 모의고사
  수능 준비하시는군요.
  그동안 고생 많으셨죠?

  어떤 시험이에요?

공무원 · 고시
  공무원 시험 준비하시는군요.
  긴 싸움이라 힘드시죠?

  어떤 시험이에요?

자격증 · 어학
  어떤 시험이에요?

기업 필기
  기업 필기 준비하시는군요.
  인적성은 준비가 까다롭죠.

  어떤 시험이에요?

기업 면접
  네! 기업 면접을 앞두고 있군요.
  많이 긴장되시죠?

  어떤 시험이에요?

편입 · 대학원
  편입이나 대학원 준비하시는군요.
  쉽지 않은 길 택하셨네요.

  어떤 시험이에요?

대학교 시험
  학교 시험이시군요.
  시험 기간이 제일 정신없죠.

  어떤 시험이에요?

승진 · 사내시험
  승진 시험 준비하시는군요.
  일하면서 준비하시느라 힘드시겠어요.

  어떤 시험이에요?

오디션 · 실기
  오디션 준비하시는군요.
  무대에 서기까지가 제일 떨리죠.

  어떤 오디션이에요?

기타
  어떤 시험인지 알려주시겠어요?
```

자격증 · 어학만 공감 없이 바로 묻습니다. 하위 그룹을 먼저 물어야 해서 말풍선이 하나 더 끼면 늘어집니다.

#### 면접 전용

```
일의 성격
  어떤 업무 성격에 가까운지 물어봐도 될까요?

직무명
  직무 이름도 알려주시겠어요?
  영업관리처럼 적어주시면 돼요.

면접 날짜
  면접 날짜가 언제예요?

면접 시간
  몇 시에 면접이에요?

완료
  다 됐어요!
  면접운을 보여드릴게요.
```

#### 유료 추가 입력 (면접)

```
기업명
  어느 기업 면접이에요?

직무 확인 (무료에서 입력한 경우)
  직무는 {jobTitle} 맞으시죠?

직무 입력 (무료에서 건너뛴 경우)
  직무 이름을 알려주세요.
```

#### 리포트 생성 중

목표 소요가 필기 90초, 면접 130초이므로 간격을 다르게 둡니다.

```
필기   9개 문구를 10초 간격  (총 90초)
면접   9개 문구를 14초 간격  (총 126초)
```

```
필기
  1  잠깐만요, 사주를 보고 있어요
  2  {name}님 명식이 나왔어요
  3  오행 분포를 계산하는 중이에요
  4  {examDate} 기운을 보고 있어요
  5  시험 전 7일 흐름을 정리하는 중이에요
  6  {exam} 정보를 확인하는 중이에요
  7  남은 기간 배분을 계산하는 중이에요
  8  거의 다 됐어요
  9  마무리하는 중이에요

면접
  1  잠깐만요, 사주를 보고 있어요
  2  {name}님 명식이 나왔어요
  3  오행 분포를 계산하는 중이에요
  4  {company} 설립일을 확인하는 중이에요
  5  기업 사주를 계산하는 중이에요
  6  {name}님과의 궁합을 보고 있어요
  7  {jobTitle} 직무를 살펴보는 중이에요
  8  거의 다 됐어요
  9  마무리하는 중이에요
```

마지막 문구 이후에도 완료되지 않으면 그 문구를 유지합니다. 완료되면 남은 문구를 건너뛰고 즉시 결과로 전환합니다.

2번과 3번 문구 뒤에는 계산이 끝난 실제 결과물을 노출합니다.

```
2번 뒤   SajuTable 컴포넌트 (명식 4기둥)
3번 뒤   ElementBar 컴포넌트 (오행 분포 막대)
```

초반 20초에서 40초 구간을 실제 결과물로 채웁니다.

두 컴포넌트는 리포트 섹션 1에 쓰이는 것을 재사용합니다. 결제한 사용자가 첫 20초 안에 실제 결과물을 받게 되어 불안이 줄어듭니다.

중간에 응원 문구를 하나 끼워 넣어도 좋습니다.

문구 개수는 공통 12개, 면접 6개, 유료 3개, 생성 중 18개로 총 39개입니다.

### 21.12 대화 UI 컴포넌트 사양

```
합격이 말풍선
  아바타      30 x 30 원형, char-profile.png (21.6 참조)
  간격        아바타와 말풍선 사이 8px
  배경        var(--surface-1)
  모서리      2px 12px 12px 12px (왼쪽 위만 각짐)
  패딩        10px 12px
  글자        14px, 행간 1.6
  최대 폭     250px

사용자 답변
  정렬        오른쪽
  배경        #4B7BF5
  글자색      #FFFFFF
  모서리      12px 2px 12px 12px (오른쪽 위만 각짐)
  패딩        9px 13px
  글자        14px
  최대 폭     220px

선택지 버튼
  정렬        세로 나열 또는 2열 그리드
  패딩        11px 14px
  글자        14px
  최소 높이   44px (터치 영역)
  아이콘      입력 위젯이 열리는 버튼에만 표시

메시지 간격  14px
```

선택지가 6개를 넘으면 2열 그리드로 배치합니다. 380px 화면에서 3열은 사용하지 않습니다.

#### 화면 전반의 모션

대화 화면 밖에도 같은 원칙으로 모션을 넣습니다. 값은 `lib/motion.ts` 한 곳에 모읍니다. 여기저기 흩어지면 화면마다 다른 속도로 움직여 서비스가 산만해집니다.

```
공통 이징   [0.22, 1, 0.36, 1]     감속이 강해 끝에서 부드럽게 멈춥니다
```

**스크롤 리빌** — 카드가 화면에 들어올 때 아래에서 떠오릅니다.

```
initial     opacity 0, y 24
whileInView opacity 1, y 0
viewport    once: true, margin: '-60px'
transition  duration 0.4, delay = index × 0.06
```

`once: true`가 핵심입니다. 없으면 위아래로 스크롤할 때마다 다시 재생됩니다. 카드가 8개 늘어선 결과 화면에서 조금만 되돌려도 전부 다시 떠올라 읽던 자리를 놓칩니다.

`margin: '-60px'`은 화면 아래 경계에 닿는 순간이 아니라 60px 들어온 뒤에 시작하라는 뜻입니다. 경계에서 바로 시작하면 스크롤이 빠를 때 이미 다 보이는 카드가 뒤늦게 떠오릅니다.

적용 대상은 랜딩 차별점 카드 4개, 랜딩 미리보기, 무료 결과 카드 8개, 유료 리포트 섹션입니다. **호출부가 아니라 카드 컴포넌트 안쪽(`ResultCard`, `ReportSection`)에 넣습니다.** 호출부가 카드 종류에 따라 여러 갈래로 분기해서 렌더하므로, 바깥에 넣으면 한 갈래를 빠뜨리기 쉽습니다.

**눌림 반응** — 모든 버튼과 탭 가능한 요소.

```
whileTap    scale 0.97
```

모바일에는 hover가 없습니다. 손가락이 화면을 덮고 있어서 눌렸다는 것을 알려줄 시각 신호가 이것뿐입니다. 반응이 없으면 안 눌렸다고 생각하고 한 번 더 누릅니다.

0.97은 눈에 보이되 레이아웃이 흔들리지 않는 선입니다. 적용 대상은 히어로·하단 CTA, 대화 선택지, 탭바, 유형 뱃지, 잠금 영역의 더 자세히 보기, 리포트 재시도입니다.

**페이지 전환** — `app/template.tsx`.

```
들어오는 화면  opacity 0 → 1, y 12 → 0, duration 0.3
```

`layout.tsx`가 아니라 `template.tsx`인 이유는, layout은 라우트가 바뀌어도 인스턴스를 유지해서 마운트 애니메이션이 한 번만 돌기 때문입니다. template은 라우트마다 새로 마운트됩니다.

**나가는 화면은 애니메이션하지 않습니다.** App Router에서 나가는 화면을 붙잡아 두려면 `AnimatePresence`로 감싸고 라우트를 직접 들고 있어야 하는데, 그러면 서버 컴포넌트 스트리밍과 프리페치가 어긋나고 뒤로 가기에서 화면이 겹칩니다. 라우팅이 끊기는 것보다 들어오는 쪽만 부드럽게 하는 편이 낫습니다. 나가는 느낌은 누른 버튼의 `whileTap`이 대신합니다.

y가 24가 아니라 12인 것은, 24px은 카드 하나가 움직이는 거리라 화면 전체가 그만큼 움직이면 멀미가 나기 때문입니다.

#### prefers-reduced-motion

위 셋을 전부 `useReducedMotion()`으로 제어합니다. 켜져 있으면 애니메이션 없이 최종 상태로 바로 그립니다.

**스크롤 리빌은 `initial` 자체를 걸지 않습니다.** 애니메이션만 끄고 `initial: opacity 0`을 남기면 화면이 영원히 비어 보입니다. 모션을 줄이려던 설정이 내용을 통째로 가리는 사고가 됩니다.

`app/globals.css`의 `@media (prefers-reduced-motion: reduce)`가 CSS 애니메이션을 잡고, `useReducedMotion()`이 framer-motion 쪽을 잡습니다. 두 층 모두 필요합니다.

### 21.13 전체 이미지 목록

| 파일 | 크기 | 용도 | 제작 |
|---|---|---|---|
| hero-body.png | 1024 x 1024 | 홈 히어로 몸통 (팔 제외) | 원본에서 분리 |
| hero-arm.png | 1024 x 1024 | 홈 히어로 팔 (애니메이션용) | 원본에서 분리 |
| char-01.png | 1024 x 1024 | 운 지수 0-34, 응원 | 신규 생성 |
| char-02.png | 1024 x 1024 | 운 지수 35-49, 걱정 | 신규 생성 |
| char-03.png | 1024 x 1024 | 운 지수 50-64, 기본 | 신규 생성 (기준형) |
| char-04.png | 1024 x 1024 | 운 지수 65-79, 미소 | hero와 동일 파일 사용 가능 |
| char-05.png | 1024 x 1024 | 운 지수 80-100, 활짝 | 신규 생성 |
| char-profile.png | 512 x 512 | 대화창 아바타, 썸네일 | 신규 생성 |
| icon-512.png | 512 x 512 | 파비콘, 앱 아이콘 | char-profile 크롭 |
| apple-icon.png | 180 x 180 | iOS 홈 화면 | char-profile 크롭, 배경 #E8F0FF 채움 |
| og-image.png | 1200 x 630 | 링크 공유 썸네일 | hero + 텍스트 합성 |

신규 생성은 char-01, char-02, char-03, char-05, char-profile 다섯 장입니다. hero는 원본 참조 이미지를 그대로 사용하고 char-04와 동일 파일로 씁니다.

#### 아이콘 처리

```
icon-512.png     char-profile을 정사각형으로 크롭, 투명 배경
apple-icon.png   같은 크롭에 배경 #E8F0FF 채움
                 iOS는 투명 배경을 검은색으로 표시합니다
favicon.ico      32 x 32, 눈 두 개와 뿔만 식별되면 충분
                 세부는 제거합니다
```

#### OG 이미지

한글 텍스트를 생성형 AI로 이미지에 넣으면 글자가 깨집니다. 캐릭터만 배치하고 텍스트는 코드로 합성합니다.

```
구성
  배경    브랜드 색 그라데이션 (#E8F0FF에서 #FFFFFF)
  왼쪽    합격이 캐릭터 (hero)
  오른쪽  "시험 보는 날, 내 기운은 어떨까?"
          "시험사주"

방법
  Figma에서 합성 후 PNG 저장
  또는 next/og로 동적 생성 (이미지 파일 불필요)
```

## 22. 관리자 시스템

### 22.1 구현 방식

같은 Next.js 프로젝트 안에 /admin 라우트를 추가합니다. Retool, Basedash 같은 외부 도구를 사용하지 않습니다.

이유는 필요한 기능 대부분이 DB 조회가 아니라 API 호출이기 때문입니다.

| 기능 | 필요한 처리 | 외부 도구로 가능 |
|---|---|---|
| 환불 | PG 취소 API 호출 | 불가 |
| 리포트 재생성 | AI API 호출 | 불가 |
| 무료 지급 | 리포트 생성 로직 실행 | 불가 |
| 회원 관리 | DB 조회, 삭제 | 가능 |
| 결제 현황 | DB 조회 | 가능 |

외부 도구를 쓰더라도 API Route는 만들어야 하므로, 화면까지 함께 만드는 편이 효율적입니다.

데이터를 훑어보거나 즉석 쿼리를 실행할 때는 Supabase 대시보드나 별도 DB 클라이언트를 병행해 사용하면 됩니다.

### 22.2 기술 구성

```
프레임워크   Next.js (본 프로젝트와 동일)
경로         /admin
인증         Supabase Auth + 이메일 화이트리스트
UI           Tailwind (본 프로젝트와 동일)
테이블       @tanstack/react-table
차트         recharts
```

새로 추가하는 라이브러리는 두 개입니다.

### 22.3 접근 제어

별도 관리자 계정 테이블을 만들지 않습니다. 일반 로그인 후 이메일로 판별합니다.

```typescript
// middleware.ts
const ADMIN_EMAILS = process.env.ADMIN_EMAILS?.split(',') ?? []

export async function middleware(req: NextRequest) {
  if (!req.nextUrl.pathname.startsWith('/admin')) {
    return NextResponse.next()
  }

  const supabase = createServerClient(...)
  const { data: { user } } = await supabase.auth.getUser()

  if (!user || !ADMIN_EMAILS.includes(user.email!)) {
    return NextResponse.redirect(new URL('/', req.url))
  }
  return NextResponse.next()
}
```

```bash
# .env.local
ADMIN_EMAILS=admin@example.com
```

각 API Route에서 권한을 한 번 더 확인합니다. 미들웨어만 신뢰하지 않습니다.

```typescript
async function requireAdmin() {
  const supabase = createServerClient(...)
  const { data: { user } } = await supabase.auth.getUser()
  if (!user || !ADMIN_EMAILS.includes(user.email!)) {
    throw new Error('Unauthorized')
  }
  return user
}
```

관리자 화면의 DB 접근은 service_role 키를 사용하는 서버 라우트를 통합니다. RLS를 우회하므로 권한 확인이 필수입니다.

### 22.4 화면 목록

```
/admin              대시보드
/admin/reports      리포트 관리
/admin/payments     결제 관리
/admin/users        회원 관리
/admin/inquiries    문의 관리
/admin/grant        무료 지급 및 쿠폰
/admin/stats        지표
```

### 22.5 대시보드

```
┌────────────────────────────────────────┐
│  오늘                                   │
│  ┌──────┬──────┬──────┬──────┐        │
│  │ 조회  │ 결제  │ 매출  │ 환불  │        │
│  │ 142  │  3   │11,700│  0   │        │
│  └──────┴──────┴──────┴──────┘        │
│                                        │
│  처리 필요                              │
│  ┌────────────────────────────────┐    │
│  │ 리포트 생성 실패      2건  >    │    │
│  │ 미처리 문의          5건  >    │    │
│  └────────────────────────────────┘    │
│                                        │
│  최근 7일                               │
│  [일별 조회수 · 결제 건수 라인 차트]      │
│                                        │
│  AI 원가                                │
│  오늘 336원 · 이번 달 8,240원            │
└────────────────────────────────────────┘
```

처리 필요 항목이 0이 아니면 상단에 강조 표시합니다.

### 22.6 리포트 관리

가장 먼저 필요해지는 화면입니다. 결제는 됐는데 결과물이 없는 상태를 방치하면 안 됩니다.

```
┌────────────────────────────────────────┐
│  리포트 관리                             │
│  [전체] [실패] [대기] [완료]              │
│                                        │
│  ┌────────────────────────────────┐    │
│  │ 2026-08-23 14:32               │    │
│  │ minjun@example.com             │    │
│  │ 면접 · 삼성전자                  │    │
│  │ 실패 · AI API 타임아웃           │    │
│  │ 재시도 1회                       │    │
│  │                                │    │
│  │ [재생성]  [환불]  [사용자 보기]   │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

표시 항목:

```
결제 시각, 사용자 이메일
리포트 종류 (필기 / 면접), 대상
상태 (pending / completed / failed / refunded)
실패 사유, 재시도 횟수
토큰 사용량, 생성 소요 시간
```

실패 사유 분류:

```
AI API 타임아웃
검색 API 실패
응답 파싱 오류
토큰 한도 초과
알 수 없는 오류
```

실패가 단시간에 몰리면 알림을 보냅니다. 1시간에 3건을 넘으면 Slack 알림을 발송합니다.

### 22.7 결제 관리

```
┌────────────────────────────────────────┐
│  결제 관리                               │
│                                        │
│  기간  [2026-08-01] ~ [2026-08-23]      │
│                                        │
│  결제 47건 · 183,300원                   │
│  환불 2건 · 7,800원                      │
│  순매출 175,500원                        │
│                                        │
│  [일별 매출 차트]                         │
│                                        │
│  ┌────────────────────────────────┐    │
│  │ 2026-08-23 14:32               │    │
│  │ minjun@example.com             │    │
│  │ 면접 리포트 · 3,900원            │    │
│  │ 카카오페이                       │    │
│  │ PG 취소 가능                     │    │
│  │              [환불]  [상세]     │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

통계 항목:

```
일별 / 월별  건수, 금액, 환불, 순매출
상품별       필기 / 면접 비율
수단별       카드 / 간편결제 / 휴대폰
쿠폰 사용    건수, 할인액
무료 지급    건수
```

PG 취소 가능 여부를 표시합니다. 카드는 당일 취소와 이후 취소 처리가 다르고, 휴대폰 소액결제는 당월 내에만 취소가 가능합니다. 기간이 지난 건은 계좌 이체로 처리해야 하므로 화면에서 구분이 되어야 합니다.

### 22.8 환불 처리

DB 상태 변경만으로는 실제 환불이 되지 않습니다. PG API를 호출해야 합니다.

```
[환불] 클릭
   ↓
사유 입력 (선택)
   ↓
PG 취소 API 호출
   ↓
성공
  payments.refunded_at 기록
  payments.refund_reason, refunded_by 기록
  reports.status = 'refunded'
  리포트 접근 차단
  사용자에게 안내 발송
   ↓
실패
  오류 메시지 표시
  수동 처리 안내
```

부분 환불은 지원하지 않습니다. 3,900원 단일 상품이므로 전액 취소만 있으면 됩니다.

### 22.9 회원 관리

```
┌────────────────────────────────────────┐
│  회원 관리                               │
│  [검색: 이메일 또는 이름          ]      │
│                                        │
│  ┌────────────────────────────────┐    │
│  │ 김민준                          │    │
│  │ minjun@example.com             │    │
│  │ 카카오 · 2026-08-01 가입         │    │
│  │ 조회 5회 · 결제 2건              │    │
│  │              [상세]  [삭제]     │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

상세 화면에서는 해당 사용자의 조회 이력, 결제 이력, 문의 이력을 함께 표시합니다.

관리자에 의한 삭제는 사용자 자발적 탈퇴와 동일하게 처리합니다. 11.6의 절차를 따릅니다.

### 22.10 문의 관리

```
┌────────────────────────────────────────┐
│  문의 관리                               │
│  [미처리] [처리중] [완료]                 │
│                                        │
│  ┌────────────────────────────────┐    │
│  │ 결제 · 환불                     │    │
│  │ minjun@example.com             │    │
│  │ 2026-08-23 15:10               │    │
│  │                                │    │
│  │ 결제했는데 리포트가 안 보입니다.   │    │
│  │                                │    │
│  │ 이 사용자: 결제 2건, 실패 1건 >  │    │
│  │                                │    │
│  │ [답변 작성]  [완료 처리]         │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

해당 사용자의 결제 이력과 리포트 상태를 문의 옆에 함께 표시합니다. "결제했는데 리포트가 안 보여요" 문의가 들어오면 상태를 바로 확인할 수 있어야 합니다.

답변을 작성하면 이메일로 발송하고 status를 answered로 변경합니다.

### 22.11 무료 지급

용도는 CS 보상, 지인 테스트, 인플루언서 제공, 이벤트 당첨입니다. 두 가지 방식을 모두 제공합니다.

#### 직접 지급 (CS용)

```
┌────────────────────────────────────────┐
│  무료 지급                               │
│                                        │
│  사용자 검색                             │
│  [minjun@example.com          ]         │
│                                        │
│  대상 조회 결과 선택                      │
│  ┌────────────────────────────────┐    │
│  │ 국가직 9급 · 2026.04.11         │    │
│  │ 무료 결과만 있음                 │    │
│  └────────────────────────────────┘    │
│                                        │
│  지급 사유                               │
│  [ 리포트 생성 실패 보상        ]        │
│                                        │
│  ┌────────────────────────────────┐    │
│  │          지급하기               │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

지급하면 즉시 리포트를 생성하고 payments에 is_granted true로 기록합니다. 매출 집계에서 제외됩니다.

#### 쿠폰 발급 (마케팅용)

```
┌────────────────────────────────────────┐
│  쿠폰 발급                               │
│                                        │
│  코드      [ FREE2026        ]          │
│  할인      [100] [퍼센트]                │
│  사용 한도  [ 50 ] 회                    │
│  유효 기간  [2026-12-31]                 │
│  메모      [ 인플루언서 제공     ]        │
│                                        │
│  ┌────────────────────────────────┐    │
│  │          발급하기               │    │
│  └────────────────────────────────┘    │
│                                        │
│  발급된 쿠폰                             │
│  ┌────────────────────────────────┐    │
│  │ FREE2026 · 100% · 12/50 사용    │    │
│  │ 2026-12-31까지                  │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

### 22.12 지표

19장의 검증 지표를 화면으로 표시합니다.

```
입력 완주율      단계별 이탈 지점
CTA 클릭률       무료 결과에서 결제 화면 진입
결제 전환율      CTA 클릭 대비 실제 결제
재방문율         시험일 입력자의 D-day 경과 재접속
재결제율         두 번째 시험 구매
방식별 분포      필기 / 면접 / 실기 / 오디션
유형별 분포      오행 5종 비율
D-day 구간별 결제율
```

유형별 분포는 서비스 기능에도 사용됩니다. 뱃지 모달의 "전체 이용자 중 N퍼센트" 표시입니다. 실시간 계산은 부하가 걸리므로 주 1회 값을 갱신해 상수로 반영합니다.

### 22.13 AI 원가 및 품질 모니터링

reports 테이블의 기록을 집계합니다.

```
원가
  일별 AI 호출 횟수
  평균 입력 / 출력 토큰
  건당 평균 원가
  누적 API 비용
  방식별 원가 비교 (필기 vs 면접)

품질
  total_chars 분포
  목표 미달 건수 (70퍼센트 미만)
  stop_reason이 max_tokens인 건수
  평균 생성 시간
```

total_chars가 목표에 계속 미달하면 프롬프트의 분량 지시를 조정해야 한다는 신호입니다. stop_reason이 max_tokens로 나오면 상한을 더 올려야 합니다.

방식별 원가 비교가 필요한 이유는, 면접 리포트가 검색 2회를 포함해 필기보다 원가가 높은데 가격이 같기 때문입니다. 차이가 크면 가격 분리를 검토할 근거가 됩니다.

방식별 원가 비교가 필요한 이유는, 면접 리포트가 검색 2회를 포함해 필기보다 원가가 높은데 가격이 같기 때문입니다. 차이가 크면 가격 분리를 검토할 근거가 됩니다.

### 22.14 검색 및 입력 통계

2차 확장 판단 근거로 사용합니다.

```
기업명 검색 성공률
  실패율이 높으면 기업 DB 구축 필요
  자주 입력되는 기업명 순위 (우선 등록 대상)

시험명 입력 통계
  exam_name_raw와 exam_name을 함께 조회
  프리셋에 없는 시험명 순위
  같은 시험의 다른 표기 병합 대상 확인
  상위 항목을 preset-exams.json에 추가할지 판단

방식별 분포
  필기 / 면접 / 실기 / 오디션 비율
  실기와 오디션 유료 상품 개발 여부 판단

대학교 시험 기간 분포
  exam_period 값 분포
  기간별 리포트 차별화 필요 여부 판단
```

프리셋에 없는 시험명 조회 쿼리입니다.

```sql
select
  exam_name,
  exam_name_raw,
  exam_category,
  count(*) as 입력횟수
from queries
where exam_name is not null
group by 1, 2, 3
order by 4 desc
limit 50;
```

### 22.15 공지 배너

점검이나 장애 시 사용자에게 알리는 수단입니다.

```
┌────────────────────────────────────────┐
│  공지 배너                               │
│                                        │
│  문구                                   │
│  [ 8월 25일 새벽 2시~4시 점검 예정 ]      │
│                                        │
│  [ ] 활성화                             │
│                                        │
│  ┌────────────────────────────────┐    │
│  │          저장                   │    │
│  └────────────────────────────────┘    │
└────────────────────────────────────────┘
```

활성화하면 홈 화면 최상단에 표시됩니다. 장애 발생 시 배너 하나로 문의 대부분을 줄일 수 있습니다.

### 22.16 API Route 목록

```
POST   /api/admin/report/regenerate     리포트 재생성
POST   /api/admin/payment/refund        PG 취소 및 DB 반영
POST   /api/admin/grant                 무료 지급
POST   /api/admin/coupon                쿠폰 발급
DELETE /api/admin/user/[id]             회원 삭제
POST   /api/admin/inquiry/[id]/reply    문의 답변
POST   /api/admin/notice                공지 배너 설정
```

모든 Route에서 22.3의 requireAdmin을 먼저 호출합니다.

### 22.17 문장 조각 수정에 대하여

문장 조각을 관리 화면에서 수정하는 기능은 만들지 않습니다.

조각을 DB로 옮기면 매 요청마다 DB 조회가 발생해 무료 구간의 즉시 응답이 깨집니다. 캐싱을 붙이면 해결되지만 복잡도가 올라갑니다.

1차에서는 JSON 파일을 직접 수정하고 재배포합니다. 조각 수정 빈도가 높아지면 2차에서 검토합니다.

### 22.18 개발 규모

```
1. 미들웨어, 레이아웃, 사이드바        2시간
2. 대시보드                          3시간
3. 리포트 관리 + 재생성 API           4시간
4. 결제 관리 + 환불 API               4시간
5. 회원 관리                         2시간
6. 문의 관리                         3시간
7. 무료 지급 + 쿠폰                   3시간
8. 지표                              3시간
─────────────────────────────────────
합계                                 3일
```

Phase 6(유료) 완료 후에 착수합니다. 결제와 리포트 생성이 있어야 관리 대상이 생깁니다.

다만 13.1의 DB 필드는 Phase 5에서 미리 반영합니다. 나중에 추가하면 기존 데이터에 값이 없어 처리가 번거로워집니다.

### 22.19 우선순위

관리자 화면을 한 번에 다 만들 필요는 없습니다. 다음 순서로 필요해집니다.

| 순위 | 화면 | 필요해지는 시점 |
|---|---|---|
| 1 | 리포트 관리 | 첫 생성 실패 발생 시 |
| 2 | 결제 관리 및 환불 | 첫 환불 요청 시 |
| 3 | 문의 관리 | 문의 주 5건 이상 |
| 4 | 무료 지급 | CS 보상 필요 시 |
| 5 | 회원 관리 | 계정 문의 발생 시 |
| 6 | 지표 | 개선 판단 시점 |
| 7 | 공지 배너 | 첫 점검 또는 장애 |

14.12의 사용자 재시도 버튼이 있으면 1순위 화면의 필요성이 늦춰집니다.
