# Claude Code 수정 지시

PRD가 v3.0으로 갱신됐다. 아래 항목을 순서대로 수정하고,
각 항목마다 검증한 뒤 다음으로 넘어가라.

시작 전에 "시험사주_PRD_v3.md"를 다시 읽어라. 구조가 크게 바뀌었다.
그리고 lib/content/ 아래 fragments.json, paid-fragments.json,
preset-exams.json, quotes.json을 새 파일로 교체해라.

===========================================================
[0] 콘텐츠 파일 교체
===========================================================

새로 받은 파일 4개를 lib/content/에 배치한다.

  fragments.json        무료 조각 222개 (기존 167개에서 확대)
  paid-fragments.json   유료 조각 25개 (기존 10개에서 확대)
  preset-exams.json     대분류 10개, 시험명 73개 (구조 변경)
  quotes.json           응원 문구 17개 (신규)

preset-exams.json은 스키마가 바뀌었다.
  - 대분류가 8개에서 10개로
  - 자격증·어학에 subGroups 추가
  - 대학교 시험에 hasExamPeriod 추가
  - defaultType에 "오디션" 값이 생김

기존 코드에서 preset-exams를 읽는 부분을 새 스키마에 맞게 수정한다.

===========================================================
[0-2] 만세력 계산 규칙 확정 확인
===========================================================

PRD 4.2가 확정 규칙에 맞게 갱신됐다. 이미 수정했다면
아래 세 가지가 코드에 반영되어 있는지 확인만 하고 넘어간다.

1. 경도 보정과 서머타임 보정은 시주 계산에만 적용한다
   년주와 월주는 원본 KST 시각으로 절기와 비교한다
   일주는 원본 날짜를 쓴다

2. 보정 후 시각으로 시지를 판정한다
   실질 자시 시작이 23:30이 되는 것은 의도한 동작이다

3. 오행 가중치는 일간 3, 월지 3, 나머지 각 1을 유지한다
   지장간은 반영하지 않는다

PRD 17장 Phase 1의 검증 사례가 10건으로 갱신됐다.
test/saju-output.md를 새 사례로 다시 생성한다.

특히 6번(1995-06-16 00:29)이 중요하다. 보정 후 23:59가 되어도
일주가 6월 16일이어야 한다.

===========================================================
[1] 캐릭터 이름 변경
===========================================================

"운이"를 "합격이"로 전면 교체한다.

코드, 주석, UI 문구, chat-scripts.ts 전부 해당한다.
단 "기운이", "오늘의 운이" 같은 오행 문맥은 건드리지 않는다.

===========================================================
[2] max_tokens 재설정
===========================================================

PRD 8.13이 수정됐다. max_tokens는 원가 통제 장치가 아니라
잘림 방지선이다. 실제 청구는 생성된 토큰만큼만 발생한다.

  코드 기본값을 12000으로 올린다
  .env.local의 AI_MAX_TOKENS도 12000으로 맞춘다
  덮어쓰기 구조는 유지한다

응답의 stop_reason이 "max_tokens"이면 출력이 잘린 것이다.
reports.error_message에 "출력 잘림"을 기록하고 실패로 처리한다.
잘린 리포트를 사용자에게 보여주지 않는다.

원가 통제는 아래 두 가지로만 한다.
  1. 검색 결과 6000토큰 상한 (입력 통제)
  2. 프롬프트의 섹션별 분량 지시 (출력 통제)

===========================================================
[3] 유료 리포트 섹션 확대
===========================================================

PRD 8.3, 8.4가 전면 교체됐다.

필기 14개 섹션, 면접 15개 섹션이다. 신규 섹션 3개가 있다.

  섹션 2   내 사주가 말하는 시험/면접 패턴   (십신, 신규)
  섹션 4   시험/면접 당일 시간대별 운용      (12지지 기반으로 재작성)
  섹션 14/15  내 사주로 본 시험/면접 전략    (반복 패턴, 신규)

3-1. 섹션별 최소 글자 수를 프롬프트에 명시한다

  문장 수 지시("5-6문장")를 전부 글자 수로 바꾼다.
  모델이 짧은 문장으로 개수만 채우는 것을 막기 위함이다.

  분량은 PRD 8.3, 8.4의 표를 그대로 쓴다.
  필기 합계 6,900자 이상, 면접 합계 7,900자 이상.

3-2. 프롬프트 끝에 자가 점검 지시를 넣는다

  "모든 섹션을 작성한 뒤 각 섹션의 분량이 최소 기준을
   충족하는지 확인하고, 부족한 섹션은 내용을 더 채운 뒤
   최종 JSON을 출력할 것. 분량을 채우기 위해 같은 말을
   반복하거나 일반론을 늘어놓지 말고, 계산 결과에 근거한
   구체적인 내용으로 채울 것."

  마지막 문장이 중요하다. 없으면 물타기로 채운다.

3-3. 서버에서 분량을 검증한다

  생성 결과를 파싱한 뒤 각 섹션의 글자 수를 센다.
  전체가 목표의 70퍼센트 미만이면 실패로 처리하고
  retry_count를 올린다.

  reports 테이블에 total_chars 컬럼을 추가해 기록한다.

3-4. 십신 계산 함수를 만든다

  PRD 5.6이 신규 추가됐다. lib/saju/shipsin.ts를 만든다.

  ```typescript
  type Shipsin = '비겁' | '식상' | '재성' | '관성' | '인성'

  function getShipsin(dayElement: Element, target: Element): Shipsin {
    if (target === dayElement) return '비겁'
    if (GENERATES[dayElement] === target) return '식상'
    if (OVERCOMES[dayElement] === target) return '재성'
    if (OVERCOMES[target] === dayElement) return '관성'
    return '인성'
  }
  ```

  5.2의 오행 가중치를 그대로 써서 십신 분포를 산출한다.

  프롬프트 재료에 십신 분포와 함께 천간에 있는지 지지에만
  있는지도 넣는다. 위치에 따라 해석이 달라진다.

3-5. 프롬프트 재료에 신규 항목을 추가한다

  PRD 8.16이 갱신됐다. 아래 항목이 추가됐다.

    user.shipsin           십신 분포
    user.shipsinPosition   천간에 있는지 지지에만 있는지
    timeSlots              12지지 시간대 배열 (섹션 4용)
    exam.hoursUntilStart   시험까지 남은 시간
    exam.now               서버 기준 현재 시각
    exam.examPeriod        대학교 시험 기간
    fortune.potentialScore 잠재력 발휘 지수
    fragments.shipsin      십신 조각
    fragments.pattern      반복 패턴 조각

  timeSlots는 시작 시각 기준으로 기상부터 종료까지의 구간만
  생성한다. 각 구간에 지지 이름, 한자, 오행, 사용자 일간과의
  십신 관계를 함께 넣는다. AI가 관계를 임의로 판단하지 않게
  하기 위함이다.

  hoursUntilStart가 음수이면 시험이 이미 시작했거나 끝난
  상태다. 이 경우 시험 이후 섹션 중심으로 구성하고 톤을
  전환한다.

  시간을 모른다고 선택한 경우 timeSlots와 startTimeRelation을
  넣지 않고, 섹션 4를 시간대 없는 구성으로 대체한다.

3-6. 신규 섹션의 조각 사용

  섹션 2는 paid-fragments.json의 shipsinByDayStem(일간 10종)을
  먼저 두고 AI가 확장한다.

  섹션 14/15는 patternByStrong(강한 오행 5종)을 먼저 두고
  AI가 확장한다.

  조각을 앞에, AI 생성분을 뒤에 붙인다. 순서를 바꾸면
  사주 해석의 일관성이 무너진다.

===========================================================
[4] 모든 섹션에 오행 근거 명시
===========================================================

PRD 8.5가 신규 추가됐다.

각 섹션이 어떤 계산값을 근거로 삼는지 표로 정리되어 있다.
이 매핑을 프롬프트에 그대로 넣는다.

프롬프트에 아래 지시를 추가한다.

  모든 섹션에서 조언의 근거를 사주 계산 결과로 제시하라.
  "화가 8인데 조절할 수가 3뿐입니다" 처럼 실제 수치를
  언급하라. "화 기운이 강해서" 같은 추상적 표현만
  반복하지 마라.

  단, 재료에 없는 오행 수치나 관계를 만들지 마라.

특히 섹션 4(시간대별 운용)는 12지지 오행과 사용자 오행의
관계로 각 시간 구간을 해석해야 한다. PRD 8.6의 12지지 표를
프롬프트 재료에 넣는다.

===========================================================
[5] 잠재력 발휘 지수 추가
===========================================================

PRD 8.7이 신규 추가됐다.

lib/saju/fortune.ts에 계산 함수를 추가한다.

  기본 100
  시험일 일진 관계: 상생 +10 / 비화 +5 / 아극 +3 / 설기 -5 / 상극 -10
  시작 시간 궁합:   상생 +8 / 비화 +4 / 아극 +2 / 설기 -3 / 상극 -8
  방식 궁합:        (해당 방식 궁합점수 - 70) / 5
  70~120 클램프

무료에서는 잠금으로 표시하고 숫자를 "???%"로 가린다.
유료 리포트 표지에 당일 운, 오늘의 운과 함께 3개를 나란히 둔다.

===========================================================
[6] D-day 구간별 구성 변화
===========================================================

PRD 8.8이 갱신됐다. 섹션 번호가 바뀌었다.

  섹션 5   D-8 이상: 7일 데일리 플랜
           D-2~D-7: 남은 날짜만
           D-1: 오늘 밤부터 내일 입실까지
           D-DAY: 지금부터 시험 종료까지

  섹션 8   D-8 이상: 남은 기간 어떻게 쓸까
           D-2~D-7: 지금 무엇을 버릴까
           D-1: 오늘 밤 시간표
           D-DAY: 섹션 5에 통합

D-DAY 구성은 순서가 완전히 다르다. 최상단에 "지금 바로 할
3가지"를 3문장으로 넣고, 긴 글을 앞에 두지 않는다.

"지금부터"를 만들려면 결제 시각과 시험 시작 시각의 차이가
필요하다. 프롬프트 재료에 현재 시각과 남은 시간을 넣는다.

시험이 이미 시작했거나 끝난 경우도 처리한다. 이 경우 시험
이후 섹션 중심으로 구성하고 톤을 전환한다.

구간 판정은 코드가 한다. AI에게 맡기지 않는다.

===========================================================
[7] 필기 검색 제거
===========================================================

PRD 8.12가 갱신됐다.

  필기          검색 0회
  실기·오디션    검색 0회
  면접          검색 2회 (기업 설립일, 기업 정보)

과목별 시간 배분 섹션이 없어지고 "남은 기간 어떻게 쓸까"로
대체됐다. 이 섹션은 과목명 대신 학습의 성격(암기, 이해, 반복)
으로 배분을 제시하며 사주 계산 결과만으로 작성된다.

lib/ai/search.ts에서 필기 검색 호출을 제거한다.
프롬프트 재료에서 subjects 항목을 제거한다.

===========================================================
[8] 대분류 10개, 방식 4분류
===========================================================

PRD 10.1, 10.2가 갱신됐다.

8-1. 대분류가 10개로 바뀌었다

  수능 · 모의고사 / 공무원 · 고시 / 자격증 · 어학 /
  기업 필기 / 기업 면접 / 편입 · 대학원 / 대학교 시험 /
  승진 · 사내시험 / 오디션 · 실기 / 기타

8-2. 자격증 · 어학은 하위 그룹이 있다

  subGroups가 있으면 2단계 질문을 한 뒤 시험명 버튼을 띄운다.

  ```typescript
  if (category.subGroups) {
    // 자격증인가 어학인가 질문
  } else {
    // 바로 시험명 버튼
  }
  ```

8-3. 대학교 시험은 시험 기간을 묻는다

  hasExamPeriod가 true인 분류에서만 이 단계가 나온다.

  [하루] [2~3일] [4~7일] [일주일 이상]

  queries에 exam_period 컬럼을 추가한다.

8-4. 방식이 4개가 됐다

  필기 / 면접 / 실기 / 오디션

  오디션 조각은 fragments.json에 이미 들어 있다.
  methodIntro, methodByStrong, methodByWeak,
  numberUseByMethod, outfitByMethod, eveByMethod,
  startTimeByRelation에 "오디션" 키가 추가됐다.

  시험명에서 방식을 추론하는 함수를 만든다.

  ```typescript
  function inferType(examName: string, categoryDefault: string | null) {
    if (examName.includes('면접')) return '면접'
    if (examName.includes('오디션')) return '오디션'
    if (examName.includes('실기')) return '실기'
    return categoryDefault
  }
  ```

  어학은 전부 필기로 처리한다.

8-5. 시험명 자유 입력을 정규화해서 저장한다

  queries에 exam_name_raw 컬럼을 추가한다.
  원본과 정규화 값을 모두 저장한다.

  ```typescript
  function normalizeExamName(raw: string): string {
    return raw.trim().replace(/\s+/g, ' ').replace(/[()（）]/g, '')
  }
  ```

===========================================================
[9] 무료 결과 재구성
===========================================================

PRD 3.2부터 3.7까지 전면 교체됐다. 가장 큰 변경이다.

9-1. 카드 순서가 바뀌었다

  1. 4월 11일, OOO님에게 어떤 날인가     전체 공개
  2. OOO님의 사주                       부분 잠금
  3. 시험 전 7일 기운 흐름               부분 잠금
  4. 오전 10시는 맞는 시간일까            부분 잠금
  5. OOO님은 어떤 시험에 강한가           전체 공개
  6. 시험장에서 주의할 점                 전체 공개
  7. 행운의 숫자와 피해야 할 색            부분 잠금
  8. 시험 전날 밤에는                     전체 공개

  차별화 카드(시험 정보를 계산에 쓴 것)를 앞으로 올렸다.

9-2. 카드 2에 명식 표시를 추가한다

  SajuTable과 ElementBar 컴포넌트를 무료 결과에서도 쓴다.
  리포트 섹션 1에 쓰이는 것을 재사용한다.

  아래에 elementSummary 조각(강한 오행 5종)을 붙인다.

9-3. 부분 잠금 4곳을 구현한다

  카드 2  🔒 십신으로 본 시험 패턴
  카드 3  🔒 날짜별 상세 플랜
  카드 4  🔒 하루 전체 시간대별 운용
  카드 7  🔒 좋은 색, 방위, 시간대

  잠긴 영역에 lockTeaser 조각(4개)으로 한두 줄 설명을 넣는다.
  제목만 있으면 무엇인지 모른다.

  각 잠금에 [더 자세히 보기] 버튼을 두고 결제 화면으로 보낸다.

9-4. 카드 7의 무료와 유료를 나눈다

  무료   행운 숫자 1개 + 방식별 활용 + 피해야 할 색
  유료   좋은 색 + 적용 위치 + 방위 + 시간대 + 숫자 2개 전부

  피해야 할 색은 강한 오행 기준이다.
  avoidColorByStrong 조각(5개)을 쓴다.

  luckyColorByWeak, outfitByMethod, directionByWeak,
  timeSlotByWeak는 유료 리포트 섹션 10으로 이동한다.

9-5. 전체 잠금 목록을 무료 카드 뒤에 둔다

  필기 9개, 면접 10개 항목이다.
  제목과 한 줄 설명으로 나열한다. 이 목록이 상품 설명 역할을 한다.

  면접의 헤더는 "실제 면접 보는 기업을 알려주시면"으로 둔다.

9-6. 상단에 잠재력 발휘 지수를 잠금으로 표시한다

  당일 운과 오늘의 운은 숫자를 보여주고,
  발휘 지수만 "???%"로 가린다.
  숫자 하나가 가려져 있는 것이 궁금증을 만든다.

9-7. 응원 문구를 넣는다

  공유 버튼 위에 배치한다.
  quotes.json에서 무작위로 하나를 표시한다.
  헤더는 "시험을 앞둔 당신을 위한 응원 한 마디"

===========================================================
[10] 대기 화면 재설계
===========================================================

PRD 14.11과 21.11이 갱신됐다.

생성에 78초에서 100초가 걸린다. 기존 4개 문구는 14초 만에
소진된다.

10-1. 문구를 9개로 늘린다

  chat-scripts.ts의 생성 중 문구를 PRD 21.11의 목록으로 교체한다.
  8초에서 10초 간격으로 표시한다.

  78초 이후에도 완료되지 않으면 마지막 문구를 유지한다.
  완료되면 남은 문구를 건너뛰고 즉시 결과로 전환한다.

10-2. 계산 결과를 대기 중에 먼저 보여준다

  8초 문구 뒤    SajuTable 컴포넌트
  18초 문구 뒤   ElementBar 컴포넌트

  사주 계산과 오행 분포는 코드가 즉시 산출하므로 AI를
  기다리는 동안 이미 값이 있다. 결제한 사용자가 첫 20초 안에
  실제 결과물을 받게 되어 불안이 줄어든다.

10-3. 타임아웃

  스트리밍은 유지한다. 자동 재시도는 끈 상태를 유지한다.
  클라이언트 타임아웃을 150초로 둔다.
  초과하면 PRD 14.12의 실패 화면으로 전환한다.

===========================================================
[11] 랜딩 캐릭터 애니메이션
===========================================================

PRD 21.6이 신규 추가됐다.

GIF 대신 CSS 애니메이션을 쓴다. 이미지 두 장이 필요하다.

  hero-body.png   흔드는 팔을 제외한 전신
  hero-arm.png    흔드는 팔만

이미지가 아직 없으면 기존 hero.png로 몸통만 표시하고,
애니메이션 코드는 미리 작성해둔다. 이미지가 준비되면
파일만 교체하면 되도록 한다.

CSS는 PRD 21.6의 코드를 그대로 쓴다.
prefers-reduced-motion 대응을 반드시 넣는다.

===========================================================
[12] 배포 환경
===========================================================

Vercel Pro로 결제했다. maxDuration을 최대 800초까지 설정할 수 있다.

vercel.json을 확인하고 없으면 만든다.

  {
    "regions": ["icn1"],
    "functions": {
      "app/api/report/route.ts": { "maxDuration": 300 }
    }
  }

300초로 두는 이유는 여유를 두기 위함이다. 실제 소요가 100초여도
검색 API가 느리거나 AI 응답이 지연되는 경우가 있다.
클라이언트 타임아웃(150초)이 먼저 걸리므로 사용자 대기 시간은
그 안에서 통제된다.

Supabase 리전과 Vercel 함수 리전을 맞춘다. Supabase가 서울이면
icn1, 싱가포르면 sin1이다. 서버와 DB가 가까운 것이 응답 속도에
크게 작용한다.

[13] DB 변경
===========================================================

아래 컬럼을 추가하는 마이그레이션을 만든다.

  alter table queries add column exam_name_raw text;
  alter table queries add column exam_period text;
  alter table reports add column total_chars int;

  exam_name_raw   사용자 원본 입력 (정규화 전)
  exam_period     대학교 시험 전용 (하루/2-3일/4-7일/일주일이상)
  total_chars     생성된 본문 글자 수, 품질 모니터링용

  기존 exam_type의 CHECK 제약이 있으면 '오디션'을 추가한다.

supabase/migrations/ 아래에 파일로 두고,
내가 대시보드에서 붙여넣을 수 있게 정리한다.

===========================================================
[14] 검증
===========================================================

각 항목마다 아래를 확인한다.

  1. npx tsc --noEmit 통과
  2. npm run build 통과

전체 수정 후 아래를 확인한다.

  3. 목업 모드로 무료 결과가 새 카드 순서로 나오는지
  4. 부분 잠금 4곳이 표시되는지
  5. 전체 잠금 목록이 표시되는지
  6. 대기 화면이 90초 동안 문구 9개와 명식, 오행 분포를
     순서대로 표시하는지
  7. 대분류 10개가 나오고, 자격증·어학에서 2단계가 나오는지
  8. 대학교 시험에서 시험 기간을 묻는지
  9. 오디션 방식이 선택되고 오디션 조각이 나오는지
  10. 잠재력 발휘 지수가 무료에서 잠금으로 표시되는지
  11. 십신 계산이 동작하고 섹션 2에 반영되는지
  12. 대분류 10개가 나오고 자격증·어학에서 2단계가 나오는지
  13. 대학교 시험에서 시험 기간을 묻는지
  14. 응원 문구가 랜딩과 결과에 표시되는지

실제 API로 필기와 면접을 각각 1회 생성해 확인한다.

  15. stop_reason이 max_tokens가 아닌지
  16. 전체 글자 수가 목표의 70퍼센트를 넘는지
  17. 모든 섹션에 오행 수치 언급이 있는지
  18. 섹션 2에 십신(관성, 식상, 비겁) 언급이 있는지
  19. 섹션 4가 12지지 시간대로 구성되는지
  20. total_chars가 reports에 기록되는지
  21. 소요 시간과 실제 토큰 사용량

결과를 test/report-output.md에 기록한다.
섹션별 글자 수를 표로 정리한다.

PROGRESS.md에 수정 내역과 검증 결과를 기록한다.
