# Claude Code 수정 지시 2차

보고받은 세 가지 결정과 추가 확인 사항을 전달한다.
아래 항목을 순서대로 처리하고 각 항목마다 검증한 뒤
다음으로 넘어가라.

내 확인을 기다리지 말고 [0]번부터 [7]번까지 끝까지 진행해라.

===========================================================
[0] next dev에서 /result가 멈추는 문제
===========================================================

프로덕션에서 된다고 넘어가지 마라. dev에서 멈춘다는 것은
코드에 문제가 있다는 뜻이고, 조건이 바뀌면 프로덕션에서도
터질 수 있다. 그리고 개발 중에 dev 서버를 못 쓰면
앞으로 화면을 고칠 때마다 프로덕션 빌드를 돌려야 한다.

아래 순서로 원인을 찾아라.

1. 브라우저 콘솔과 터미널 에러를 전부 확인한다
   에러가 없으면 무한 루프일 가능성이 높다

2. React Strict Mode를 임시로 끄고 테스트한다
   next.config.js의 reactStrictMode를 false로

   이때 정상 동작하면 useEffect 클린업 누락이나 이중 마운트
   문제다. 원인을 고치고 Strict Mode는 반드시 다시 켜라.
   끈 채로 두지 마라.

3. sessionStorage 접근이 useEffect 밖에 있는지 확인한다
   서버 렌더링 시점에는 sessionStorage가 없다

4. /result에서 실행되는 계산 함수에 console.time을 붙여
   어디서 멈추는지 찾는다
   특히 getWeekFlow, calculateSaju, 조각 조립 함수

5. useState와 useEffect의 의존성 배열을 전부 점검한다
   상태 변경이 같은 effect를 다시 트리거하는 구조가 있는지

원인을 찾으면 PROGRESS.md에 무엇이 문제였고 어떻게 고쳤는지
기록해라. Strict Mode를 끄는 것으로 덮지 마라.

.claude/launch.json의 siheomsaju-prod는 dev가 정상화된 뒤에도
유지해도 된다.

===========================================================
[1] max_tokens 32000
===========================================================

승인한다. 32000으로 올린다.

  코드 기본값 32000
  .env.local의 AI_MAX_TOKENS도 32000

PRD 8.13의 12000을 32000으로 수정하고, "상한은 과금 기준이
아니라 잘림 방지선"이라는 설명은 유지한다.

[2]에서 분량을 줄이면 32000까지 쓰지 않게 되지만, 상한은
넉넉하게 둔다. 실제 사용량은 reports 테이블로 확인한다.

===========================================================
[2] 분량 축소 (가장 중요)
===========================================================

현재 소요 시간이 필기 217초, 면접 308초로 너무 길다.
목표는 필기 90초, 면접 130초다.

원인은 두 가지다. 첫째, 분량 목표가 과하다. 둘째, 최소치만
있고 상한이 없어 목표의 130퍼센트를 쓰고 있다.

필기 9,000자면 A4로 6장이다. 모바일에서 끝까지 읽는 사용자가
거의 없다. 분량보다 밀도가 중요하다.

2-1. PRD 8.3, 8.4의 분량 표를 아래로 교체한다

필기 (합계 4,000~4,800자)

```
1.  명식과 오행 분포        180~250자
2.  시험 패턴 (십신)        350~450자
3.  학습 유형 진단          350~450자
4.  당일 시간대별 운용       500~650자
5.  7일 데일리 플랜         600~800자
6.  놓치기 쉬운 3가지        300~400자
7.  전날 상세 타임라인       300~400자
8.  남은 기간 어떻게 쓸까     300~400자
9.  좌석과 방위             200~280자
10. 행운 색과 방위          250~330자
11. 이 기간 피해야 할 것     200~280자
12. 시험운 캘린더           180~250자
13. 시험 이후와 다음 기회    280~360자
14. 내 사주로 본 시험 전략   300~400자
```

면접 (합계 4,500~5,300자)

```
1.  명식과 오행 분포        180~250자
2.  면접 패턴 (십신)        350~450자
3.  답변 유형 진단          380~480자
4.  당일 시간대별 운용       500~650자
5.  이 기업은 어떤 곳인가    280~380자
6.  기업과 나의 궁합        380~480자
7.  이 직무와 나            380~480자
8.  들어올 가능성 높은 질문   550~700자
9.  조심해야 할 3가지        300~400자
10. 행운 색과 방위          250~330자
11. 복장과 소지품           250~330자
12. 면접 전날 밤            300~400자
13. 면접운 캘린더           180~250자
14. 면접 이후와 다음 기회    280~360자
15. 내 사주로 본 면접 전략   300~400자
```

2-2. 프롬프트에 상한을 명시한다

  각 섹션은 지정된 범위 안에서 작성하라. 상한을 넘기지 마라.
  분량을 채우기 위해 같은 말을 반복하거나 일반론을 늘어놓지
  말고, 계산 결과에 근거한 구체적인 내용으로 채워라.
  범위 안이면 짧은 쪽이 낫다.

2-3. 자가 점검 문구를 바꾼다

  모든 섹션을 작성한 뒤 각 섹션이 지정 범위 안에 있는지
  확인하라. 부족하면 채우고 넘치면 줄여라.

2-4. 서버 검증에 상한을 추가한다

  전체가 목표 하한의 70퍼센트 미만이면 실패 처리
  전체가 목표 상한의 150퍼센트 초과면 경고 로그 기록

2-5. 면접이 필기보다 90초 더 걸리는 원인을 분석한다

  검색 API 응답 시간과 AI 생성 시간을 분리해서 측정하고
  어느 쪽이 병목인지 보고해라.

  검색이 원인이면 두 검색을 병렬로 호출하도록 바꾼다.

===========================================================
[3] 타임아웃 조정
===========================================================

Vercel Pro로 결제 확정됐다. 분량 축소 후 기준으로 설정한다.

  클라이언트 타임아웃   240초
  Vercel maxDuration   500초

vercel.json

```json
{
  "regions": ["icn1"],
  "functions": {
    "app/api/report/route.ts": { "maxDuration": 500 }
  }
}
```

PRD 14.11과 15.1을 수정한다.

여유를 두는 이유는 검색 API가 느린 날이나 AI 응답이 지연되는
경우가 있기 때문이다. 목표 소요가 90~130초여도 상한은 넉넉해야
한다.

===========================================================
[4] 대기 화면 간격 조정
===========================================================

목표 소요가 90~130초이므로 말풍선 간격을 맞춘다.

  필기   9개 문구를 10초 간격 (총 90초)
  면접   9개 문구를 14초 간격 (총 126초)

8초와 18초 지점의 명식·오행 분포 노출은 그대로 유지한다.
초반 20초를 채우는 역할이다.

마지막 문구 이후에도 완료되지 않으면 유지한다.
완료되면 남은 문구를 건너뛰고 즉시 결과로 전환한다.

===========================================================
[5] 생성 중 이탈 처리 (신규)
===========================================================

90~130초는 사용자가 나갈 수 있는 시간이다. 결제한 사용자가
브라우저를 닫았을 때 어떻게 되는지 확인하고 보완해야 한다.

5-1. 현재 동작을 실제로 테스트해서 보고한다

  결제 후 생성 중에 탭을 닫는다
  DB에서 reports 레코드 상태를 확인한다
  다시 /report/[id]로 들어간다

  무슨 일이 일어나는지 정확히 기록해라.
    서버 함수가 계속 실행되는지 중단되는지
    status가 무엇으로 남는지
    재진입 시 화면이 어떻게 되는지

5-2. 서버 백그라운드 완결을 보장한다

  클라이언트 연결이 끊겨도 생성이 완료되고 DB에 저장되어야 한다.

  현재 구조가 클라이언트 대기 방식이면 서버 완결 방식으로 바꾼다.

    결제 완료 시 reports 레코드를 pending으로 생성
    API Route가 생성을 끝까지 수행
    완료 시 content 저장, status를 completed로
    클라이언트는 상태만 확인

5-3. DB 컬럼을 추가한다

  alter table reports add column started_at timestamptz;

  생성 시작 시각을 기록해 좀비 레코드를 판별한다.

5-4. 재진입 처리

  /report/[id] 진입 시 status를 확인해 분기한다.

    completed  리포트 표시
    pending    대기 화면 + 5초 폴링
    failed     재시도 화면
    refunded   접근 차단

  pending인데 started_at으로부터 10분이 지났으면 실패로
  간주하고 재시도 버튼을 표시한다.

5-5. 이탈 경고와 안내

  생성 중 beforeunload 경고를 띄운다.

  ```typescript
  useEffect(() => {
    const handler = (e: BeforeUnloadEvent) => {
      if (generating) { e.preventDefault(); e.returnValue = '' }
    }
    window.addEventListener('beforeunload', handler)
    return () => window.removeEventListener('beforeunload', handler)
  }, [generating])
  ```

  모바일에서는 동작하지 않을 수 있으므로 화면에 안내를 넣는다.

    "약 2분 정도 걸려요.
     이 화면을 닫으셔도 완성되면 계속 볼 수 있어요."

  이 문구가 없으면 사용자가 나가면 돈만 날린다고 생각해
  억지로 기다린다.

5-6. 마이페이지에 생성 중 상태를 표시한다

  ┌────────────────────────┐
  │ 국가직 9급 공무원        │
  │ 만들고 있어요...         │
  └────────────────────────┘

  폴링으로 완성 시 자동 갱신한다.

===========================================================
[6] 랜딩 캐릭터 팔 애니메이션
===========================================================

이미지가 준비될 때까지 현재 상태(몸통만 애니메이션)를
그대로 유지한다. 추가 작업하지 마라.

PROGRESS.md의 "내가 해야 할 일"에 아래를 남겨라.

  hero-body.png, hero-arm.png 제작 (선택 사항)
  없어도 서비스는 정상 동작함
  파일을 넣으면 팔 애니메이션으로 자동 전환됨

===========================================================
[7] 검증
===========================================================

각 항목마다 아래를 확인한다.

  npx tsc --noEmit 통과
  npm run build 통과

전체 수정 후 아래를 확인한다.

  1. next dev에서 /result가 정상 동작하는지
     (Strict Mode 켠 상태로)
  2. 대기 화면 말풍선 간격이 목표 소요에 맞는지
  3. 생성 중 탭을 닫았다가 재진입하면 대기 화면이 이어지는지
  4. 생성 완료 후 재진입하면 리포트가 보이는지
  5. 10분 넘은 pending이 실패로 처리되는지
  6. 마이페이지에 생성 중 상태가 보이는지

실제 API로 필기와 면접을 각각 2회씩 생성해서 기록한다.

  소요 시간 (검색 시간과 AI 생성 시간 분리)
  입력 토큰 / 출력 토큰
  섹션별 글자 수와 범위 준수 여부
  전체 글자 수
  실제 원가
  stop_reason

목표

  필기   90초 이내, 4,000~4,800자, 원가 110원 내외
  면접   130초 이내, 4,500~5,300자, 원가 180원 내외

목표를 못 맞추면 무엇이 원인인지 분석해서 보고해라.
분량이 초과하면 프롬프트의 상한 지시를 강화하고,
시간이 초과하면 어느 단계가 병목인지 찾아라.

결과를 test/report-output.md에 표로 정리하고,
PROGRESS.md에 수정 내역과 검증 결과를 기록해라.
