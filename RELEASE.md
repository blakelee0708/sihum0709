# 출시 체크리스트

작성 2026-08-26 · 기준 커밋 `6579239`

이 문서는 "지금 배포하면 무엇이 되고 무엇이 안 되는가"를 정리합니다.
명세는 `시험사주_PRD_v3.md`, 개발 경과는 `PROGRESS.md`를 보십시오.

---

## 0. 한 줄 결론

**무료 구간만 여는 것은 오늘 배포 가능합니다.** 회원님 작업 두 가지(Vercel
배포, 도메인)만 하시면 됩니다. 사업자 등록도 PG 계약도 필요 없습니다.

**결제 포함 정식 출시는 회원님 쪽 계약과 등록이 끝나야 합니다.** 코드는
준비돼 있고, 막고 있는 것은 전부 회원님 계정·계약 건입니다.

---

## 1. 지금 배포 가능한 것과 불가능한 것

### 되는 것

| 기능 | 상태 | 비고 |
|---|---|---|
| 대화형 입력 (`/start`) | 완료 | 로그인·API 호출 없음 |
| 무료 결과 (`/result`) | 완료 | 코드가 즉시 계산, AI 호출 없음 |
| 만세력 계산 | 완료 | 절기 경계 검증 케이스 포함, 테스트 112건 |
| 결과 저장 · 마이페이지 | 완료 | 이메일 로그인 필요 |
| 유료 리포트 생성 | 완료 | 실측 23건, 잘림 0건 |
| 리포트 화면 · 생성 중 이탈 처리 | 완료 | 브라우저로 검증함 |
| 실패 → 재시도 흐름 | 완료 | 강제 재현으로 확인 |
| 관리자 화면 | 완료 | `ADMIN_EMAILS` 화이트리스트 |
| 문의 · 공지 · 쿠폰 | 완료 | |
| 접근성 색상 대비 | 완료 | |

### 안 되는 것

| 항목 | 왜 | 막는 사람 |
|---|---|---|
| **실제 결제** | PG 계약 전. 지금은 더미 결제이고 PG 거래 검증이 없습니다 | 회원님 |
| **카카오 · 구글 로그인** | OAuth 앱 등록 전. 이메일 로그인은 됩니다 | 회원님 |
| **카카오 나에게 보내기** | 개발자 앱 등록과 `talk_message` 스코프 동의 전. 지금은 링크 복사로 대체 | 회원님 |
| **60초 넘는 리포트 생성** | Vercel Hobby는 함수 실행 60초가 상한입니다. 생성이 144~173초라 중간에 죽습니다 | 회원님 (Pro 전환) |
| 실기 · 오디션 유료 리포트 | 1차 출시 범위 밖 (PRD 8.2). 화면은 "준비 중 + 알림 받기" | — |

### 위험 요소

**더미 결제를 그대로 열면 안 됩니다.** `app/api/payment/route.ts`가 클라이언트
말을 그대로 믿고 `payments` 행을 만듭니다. PG 거래 검증이 없어서, 누구든
API를 직접 호출하면 결제 없이 리포트를 받습니다. 결제를 여는 순간 이
라우트를 PG 검증으로 바꿔야 합니다.

**Vercel Hobby에서 결제를 열면 리포트가 안 만들어집니다.** 생성은 응답을
보낸 뒤 `after()`로 이어지는데, Hobby의 60초 상한에 걸려 중간에 죽습니다.
`status`가 `pending`으로 남고 10분 뒤 좀비로 판정돼 재시도 버튼이 뜹니다.
결제는 됐는데 리포트가 안 나오는 상태가 반복됩니다.

---

## 2. 무료 구간만 먼저 여는 경우

### 필요한 작업

| # | 할 일 | 담당 | 소요 |
|---|---|---|---|
| 1 | Vercel 프로젝트 연결 · 환경변수 입력 | 회원님 | 30분 |
| 2 | 도메인 연결 | 회원님 | 30분 (DNS 전파 별도) |
| 3 | 결제 진입점을 알림 받기로 교체 | Claude Code | 1시간 |
| 4 | 배포 후 실기기 확인 | 회원님 | 30분 |

**Hobby 플랜으로 충분합니다.** 무료 구간은 AI를 부르지 않아 60초 상한에
걸릴 일이 없습니다.

### 환경변수 (무료만)

```
NEXT_PUBLIC_SUPABASE_URL        필수
NEXT_PUBLIC_SUPABASE_ANON_KEY   필수
SUPABASE_SERVICE_ROLE_KEY       필수
NEXT_PUBLIC_SITE_URL            필수 (OAuth 리다이렉트)
ADMIN_EMAILS                    관리자 화면을 쓰실 거면

ANTHROPIC_API_KEY               넣지 마십시오
SEARCH_API_KEY                  넣지 마십시오
DEV_MOCK_LOGIN                  절대 넣지 마십시오
```

`ANTHROPIC_API_KEY`가 없으면 리포트가 목업으로 나갑니다. 유료를 안 여는
동안에는 어차피 도달할 수 없는 경로이지만, 키를 안 넣는 편이 사고를
막습니다.

`DEV_MOCK_LOGIN`은 프로덕션 빌드에서 값과 무관하게 404이지만 넣지
마십시오. 이중 잠금의 한 겹을 스스로 벗기는 일입니다.

### 결제 버튼을 어떻게 처리할지

**"준비 중 + 알림 받기"로 바꿉니다.** 새로 만들 것이 없습니다. 실기·오디션에
이미 쓰고 있는 구조를 그대로 씁니다 (`components/result/WaitlistForm.tsx`).

```
지금   [ 3,900원으로 전체 보기 ]  →  /checkout
바꾼 뒤 상세 리포트는 준비 중입니다.
       준비되면 메일로 알려드릴게요.
       [ 이메일 입력 ] [ 알림 받기 ]
```

**잠금 목록은 그대로 둡니다.** 필기 9개 / 면접 10개 제목과 한 줄 설명은
상품 설명 역할을 하고, 이것이 알림 신청의 동기가 됩니다. 자물쇠 아이콘도
그대로입니다.

바뀌는 것은 CTA 버튼 하나입니다. `LockedCTA`에 스위치를 하나 두고
환경변수로 켜고 끕니다.

```
NEXT_PUBLIC_PAID_ENABLED=0    무료만. CTA가 알림 받기로 바뀝니다
NEXT_PUBLIC_PAID_ENABLED=1    결제 진입점 노출
```

`/checkout`과 `/api/payment`도 같은 값을 보고 막습니다. 화면만 가리고
라우트를 열어 두면 주소를 아는 사람이 그냥 들어갑니다.

`waitlist` 테이블의 `reason`은 지금 `practical`(실기·오디션)과 `price`가
있습니다. 유료 대기용으로 `paid`를 하나 더 받아야 합니다. 어느 시험에서
얼마나 신청했는지가 유료 오픈 시점 판단의 근거가 됩니다.

### 나중에 결제를 붙일 때 바꿀 것

무료 먼저 여는 선택이 나중 작업을 늘리지 않습니다. 바꿀 것은 셋뿐입니다.

| 바꿀 것 | 내용 |
|---|---|
| `NEXT_PUBLIC_PAID_ENABLED=1` | CTA와 라우트가 함께 열립니다 |
| `app/api/payment/route.ts` | 더미 기록 → PG 거래 검증. **이건 원래 해야 하는 일이고 무료 선출시와 무관합니다** |
| Vercel Pro | 생성이 60초를 넘기 때문입니다 |

`ANTHROPIC_API_KEY`와 `SEARCH_API_KEY`를 그때 넣습니다.
DB 스키마는 그대로입니다. `payments`, `reports` 테이블은 이미 있고
무료 기간에는 행이 안 쌓일 뿐입니다.

**알림 받기로 모은 이메일에 오픈 안내를 보내는 흐름은 아직 없습니다.**
메일 발송 수단(Resend 등)을 붙이거나, 수가 적으면 손으로 보내셔도 됩니다.

---

## 3. 결제 포함 정식 출시

### 회원님이 하실 것

| # | 할 일 | 왜 | 선행 |
|---|---|---|---|
| 1 | **사업자 등록** | 통신판매업 신고와 PG 계약의 선행 조건 | — |
| 2 | **통신판매업 신고** | 온라인 유료 판매의 법적 요건 | 1 |
| 3 | **PG 계약** (포트원 / 토스페이먼츠) | 실제 결제 | 1, 2 |
| 4 | **Vercel Pro 전환** | 생성 144~173초. Hobby는 60초 상한 | — |
| 5 | 카카오 · 구글 OAuth 앱 등록 | 로그인 전환율. 이메일만으로도 출시는 가능 | — |
| 6 | 카카오 개발자 앱 + `talk_message` 스코프 | 나에게 보내기 | 5 |

**3번 PG 계약 시 반드시 확인하실 것.** 소액 결제입니다. 건당 최소 수수료가
있는지 보십시오. 3,900원에 건당 고정 수수료가 붙으면 마진 계산이 달라집니다.
PRD 8.14는 정률 3퍼센트를 가정하고 있습니다.

### Claude Code가 할 것

| # | 할 일 | 선행 | 소요 |
|---|---|---|---|
| 1 | PG 연동 — `app/api/payment/route.ts`를 거래 검증으로 교체 | 회원님 3 | 1일 |
| 2 | 결제 위젯 — `app/checkout/CheckoutView.tsx` | 회원님 3 | 0.5일 |
| 3 | 환불 처리 — `lib/admin/pg.ts`가 지금 목업입니다 | 회원님 3 | 0.5일 |
| 4 | 카카오 · 구글 로그인 버튼 활성화 | 회원님 5 | 0.5일 |
| 5 | 카카오 나에게 보내기 | 회원님 6 | 0.5일 |
| 6 | 결제 실패 · 중복 결제 · 환불 후 리포트 접근 시나리오 검증 | 1~3 | 0.5일 |

**1~3번은 회원님 PG 계약이 끝나야 시작할 수 있습니다.** PG마다 SDK와
검증 방식이 달라서 미리 써 두면 대부분 버립니다.

### 순서

```
사업자 등록 ─→ 통신판매업 신고 ─→ PG 계약 ─→ [PG 연동 · 위젯 · 환불] ─→ 시나리오 검증 ─→ 결제 오픈
                                                      ↑
Vercel Pro 전환 ──────────────────────────────────────┘
OAuth 등록 ─→ [로그인 버튼 · 나에게 보내기]  (병렬, 결제와 무관)
```

회원님 쪽 절차가 대부분의 시간을 차지합니다. 코드 작업은 PG 계약 이후
3일 안쪽입니다.

---

## 4. 캐릭터 이미지 — 플레이스홀더가 아닙니다

**배포에 문제없습니다.** `public/character/`의 7장 모두 완성된 3D 렌더
아트입니다. 자리 채움용 회색 박스 같은 것이 아닙니다.

```
hero.png          1024×1024  100KB   손 흔드는 전신
char-01~05.png    1024×1024  112~199KB
char-profile.png   512×512   366KB
```

미완인 것은 **팔 애니메이션용 분리 이미지 두 장**입니다.

```
hero-body.png   흔드는 팔을 뺀 전신
hero-arm.png    흔드는 팔만 (어깨 관절이 캔버스 하단 중앙)
```

`lib/content/characters.ts`의 `CHARACTER_HERO_BODY` / `CHARACTER_HERO_ARM`이
`null`이라 지금은 `hero.png` 한 장으로 몸통만 숨쉬듯 움직입니다. 두 장을
넣고 경로만 채우면 팔 애니메이션으로 자동 전환됩니다. 애니메이션 코드는
이미 들어가 있습니다.

**없어도 서비스는 정상 동작합니다. 출시를 막지 않습니다.**

한 가지 손볼 것: `char-profile.png`가 512×512인데 366KB입니다. 1024×1024인
다른 장들보다 큽니다. 압축이 덜 됐습니다. 급하지 않지만 모바일 첫 로딩에
불필요한 부담입니다.

---

## 5. 출시 전 마지막 확인

| 확인할 것 | 방법 |
|---|---|
| `DEV_MOCK_LOGIN`이 프로덕션에 없는지 | Vercel 환경변수 목록 |
| `/api/dev/*`가 404인지 | 배포 후 직접 열어 보기 |
| `ADMIN_EMAILS`에 본인 메일만 있는지 | 환경변수 |
| `/admin`이 비로그인·비관리자에게 막히는지 | 시크릿 창 |
| Supabase 004 · 005 적용 | `node scripts/dev-check.mjs` |
| 이용약관 · 개인정보 처리방침 내용 | `/terms`, `/privacy` 직접 읽기 |
| 실기기 확인 | iOS Safari · Android Chrome |

마지막 둘은 회원님만 하실 수 있습니다. 약관은 사업자 정보가 들어가야
하고, 실기기는 제가 못 만집니다.

---

## 6. 지금 상태 요약

```
테스트     346건 통과 (2건 skip — 실제 과금 건)
타입 검사  통과
빌드       통과
DB         004 · 005 적용 완료

리포트 생성 실측 (23건)
  필기  144초  201원  하한 미달 0개
  면접  143초  184원  하한 미달 0개
  잘림 0건 · JSON 파싱 복구 최근 7건 0회
```

리포트 품질과 원가 관련 작업은 여기서 마감했습니다. 실사용 데이터를 보고
다시 판단합니다 (`test/report-output.md`).
